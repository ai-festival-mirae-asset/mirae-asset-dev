# 줄바꿈 규칙 — 리눅스 서버에서 실행되는 파일은 항상 LF 로 저장한다.
# (Windows 에서 core.autocrlf=true 로 받아도 아래 파일은 CRLF 로 바뀌지 않는다 —
#  CRLF 인 bash 스크립트는 서버에서 "$'\r': command not found" 로 깨진다.)
*.sh        text eol=lf
*.service   text eol=lf
*.ttl       text eol=lf
*.nt        text eol=lf
*.jsonl     text eol=lf
