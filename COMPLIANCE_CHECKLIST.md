# 주최 조건 대조표 (8/28 검증 ① — 공지·문답·평가안내 전수 대조)

> 무엇: 주최가 문서로 밝힌 요구사항을 한 줄씩 표로 만들고, 우리 시스템이 지키는지
> 코드 위치·시험 결과로 대조한 결과. **어긋남 0건**, 예정(마감 전 완료 필요) 4건.
> 출처 약칭 — [공지]=데이터·LLM 공지(8/26) · [문답]=문의답변(8/26) · [평가]=평가·제출 방법 ·
> [API]=API 호출 참고 · [PDF]=과제설명 PDF(내용은 ROADMAP에 이관, 원문 저장소 미포함).

## 1. 데이터 관련 — 전부 지킴

| # | 요구사항 (출처) | 우리 상태 | 근거 |
|---|---|---|---|
| 1 | 8/26 재배포 데이터만 사용, 07/11 구본 폐기 [공지] | 지킴 | 전처리가 새 파일명(`prbd01n001_data.xlsx` 형식)만 읽고 행 수 검증으로 구본이면 즉시 멈춤 — `preprocessing/preprocess.py` |
| 2 | 기준일: 국내 영업일 8/22 · 해외 8/23 (배포 8/24) [공지] | 지킴 | `pipeline/evidence.py` 기준일 3원화 · 모든 답변 끝에 기준일 표기 |
| 3 | 코드값 테이블 제공 불가 → 코드값 질문 없음 [공지] | 지킴 | 코드값 해석에 의존하지 않음 — 위험등급은 코드 대신 이름 컬럼(PD_RISK_NM)과 교차검증(불일치 0) |
| 4 | 0·빈 값은 의도된 것: 조회 시 빼고 보여주거나 "없다"고 응답 [공지] | 지킴 | 모든 순위·정렬 조회에서 값 0·결측 행 제외를 일반 원칙으로(`etp_top_return`·`etp_metric_rank` 등 `<>0` 필터) + 결측은 한계 문구로 명시 |
| 5 | 값 0인 행은 "아예 포함하지 않도록" [문답] | 지킴 | 위와 동일 — PLAN §5 8/27 ③ 결정으로 전 조회 컬럼에 일반화 |
| 6 | 추가 데이터 활용 자율 (구성종목–상품 등) [공지] | 지킴 | KRX 공식 공시에서 ETF 구성종목 수집(기준일 명시) — 계열사·펀드 보유종목은 수집 예정 |
| 7 | 공시·시장데이터는 8/24 발행분까지 사용 가능 [공지] | 지킴 | 수집기에 날짜 상한 8/24 검증 게이트 — `external_data/collectors/collect_krx_etf_constituents.py`, `tests/test_collect_krx.py` |
| 8 | BUYABLE_QUANTITY 무효 · 만기 도래(리스팅 종료) 제외 전부 구매가능 [공지] | 지킴 | 전처리 R8이 만기 상태로만 매수가능 판정 + 답변에 판정 기준 문구 |

## 2. 평가 방식 관련 — 전부 지킴

| # | 요구사항 (출처) | 우리 상태 | 근거 |
|---|---|---|---|
| 9 | 35문항: 상10·중10·하10 + 답변불가5 [공지] | 대비 완료 | 같은 분포의 "실전 미러 세트" 38문항(본 35+부록 3) 상시 100% 유지 |
| 10 | 교차질의 출제 가능 (예: 삼성전자 보유 국내/해외 ETF·펀드 연 수익률 TOP10) [공지] | 지킴 | 전용 규칙+조회 틀(`constituent_holders_top_return` 등) — 공식 예시 원문이 시험지(MR-A-01)에 있고 통과 |
| 11 | 해외 ETF 1년 수익률 질의 없음 · 종목 보유 ETF 수익률 질의에서 해외 제외 무방 [문답] | 지킴 | 교차질의 답변에 "해외 제외(주최 문답 확정)" 문구 명시 |
| 12 | 응답시간 기준 비공개 · 300초 넘으면 미응답 처리 [공지·API] | 지킴 | 내부 목표 15초(전 시험지 최대 13.2초, p95 9~10초) — 300초의 1/20 수준 여유 |
| 13 | 모호한 질의는 최대한 안 낼 예정 [문답] | 지킴 | 해석이 갈리는 표현은 채택 해석을 답변에 명시(PLAN §5 원칙) — 모호성 대응력은 덤 |
| 14 | 크롤링 가능하나 신뢰성·무결성 관리도 과제의 일부 [문답] | 지킴 | 수집원은 KRX 공식 공시만 · 수집 게이트(행 수·날짜·형식 검증) · 기준일 답변 명시 |
| 15 | 추가 예시 질문 없음 (8개가 전부) [문답] | 지킴 | 8개 예시의 '유형'만으로 실전 미러 세트 구성(출제자 정렬 — PLAN §5 8/26) |
| 16 | 결측 보충은 정량평가 무영향 · **보고서에 기재 시 정성평가 인정** [문답] | **예정** | 결측 파악·보충 내역은 확보됨(전처리 보고서·PLAN 기록) — 기술제안서(9/6 전)에 정리해 넣어야 함 |
| 17 | 수집 방법·적재 흐름·활용 로직을 보고서에 기재해야 정성평가 인정 [문답] | **예정** | 재료 확보됨(`external_data/COLLECTION_SUMMARY.md` 등) — 기술제안서에 기재 |

## 3. LLM 사용 관련 — 전부 지킴

| # | 요구사항 (출처) | 우리 상태 | 근거 |
|---|---|---|---|
| 18 | **HCX 필수 구간 = 질의 Intent분석 + 답변 생성** [공지 굵은 글씨] | 지킴 | 전 질의에 HCX 의도 확인 호출(`make_hcx_intent_checker`) + 답변 생성·최종화 호출(`make_hcx_finalizer`) — 서버·채점기 양쪽 배선, 규칙 경로도 예외 없음 |
| 19 | 그 외 구간 제약 없음 · 유료 LLM 지양 권장 [공지] | 지킴 | 전 구간 HCX-005 단일 모델 + CLOVA 임베딩 v2 — 타사 LLM 코드 0건(코드 검색으로 확인, 'GPT' 언급은 함정 질문 감지용뿐) |

## 4. API 규격 관련 — 전부 지킴 (시험 9개로 잠금)

| # | 요구사항 (출처) | 우리 상태 | 근거 |
|---|---|---|---|
| 20 | `GET {주소}/answer?question_id=&question=` 경로 고정 [API] | 지킴 | `server/app.py` — `tests/test_server.py` 9개 통과 |
| 21 | 응답 JSON 5필드, 전부 문자열 [API] | 지킴 | 직렬화에서 전 필드 문자열 강제 + 시험 |
| 22 | `question`은 요청값 그대로 반환 [API] | 지킴 | `server/app.py:157` |
| 23 | 요청에 별도 헤더 없음(인증 헤더 미사용) [API] | 지킴 | 무헤더 GET 그대로 처리 |
| 24 | `application/json` 반환 [API] | 지킴 | `application/json; charset=utf-8` 명시 |
| 25 | 순차 1건씩 호출 · 타임아웃/5xx 시 재시도 2회 [API] | 지킴 | 단일 경로 처리 + 어떤 경로든 유효한 5필드 응답 보장(생성 실패 시 규칙 요약 폴백) — 5xx가 나올 구조적 지점 없음, 같은 질문 재호출은 캐시로 동일 답 |
| 26 | Public 망 접근 가능한 서버 · 표준 포트(80/443) · README에 End-point 명시 [API·평가] | **예정** | M2 NCP 배포(팀원 담당) — 서버 코드는 준비 완료, 운영 기간 9/7~9/20 전 완료 필요 |

## 5. 제출물 관련

| # | 요구사항 (출처) | 우리 상태 | 근거 |
|---|---|---|---|
| 27 | 소스코드 + requirements.txt + README(실행 명령) [평가] | 지킴 | 전부 저장소에 있음 — 새 컴퓨터 재현 절차는 SETUP.md |
| 28 | 도메인별 Ontology.ttl [평가] | 지킴 | `ontology/` 5개 도메인 TTL + 검사 규칙(shapes.ttl) |
| 29 | API 명세서(요청/응답 JSON 스키마 필수 명시) [평가] | 지킴 | `API_SPEC.md` — 규격 대조표 포함 |
| 30 | 기술 제안서(PDF 권장, 권장 목차 6절) [평가] | **예정** | 9/6 마감 — 목차 재료(수집 스토리·결측 처리·온톨로지·검색 설계) 확보됨 |

## 결론

- **현재 코드가 지켜야 할 조건: 어긋남 0건.**
- **마감 전 해야 할 일 4건(전부 일정 인지됨)**: ⑯⑰ 결측 보충·수집 스토리를 기술제안서에 기재 ·
  ㉖ NCP 공개 서버 배포(9/7 전) · ㉚ 기술제안서 작성(9/6).
- 대조 일자: 2026-08-28. 이후 주최 공지가 새로 오면 이 표를 갱신한다.
