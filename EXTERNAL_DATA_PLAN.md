# 추가 데이터 수집 계획

> 과제 규칙: 외부 데이터 수집 가능하나, 평가 시 기준 데이터는 주최 측 제공 데이터. 상충 시 주최 측 데이터 우선.
> 아래는 "있으면 채워지는 결측"을 기준으로 우선순위를 매긴 것 — 착수 전 조인 성공률부터 확인할 것.

## 핵심 발견: 제공 데이터와 외부 데이터 불일치

국내ETF 총보수(`cu_charge_rt`)를 KRX 데이터와 대조한 결과, 기존에 제공 데이터가 있던 215건 중
**201건(93.5%)이 외부 값과 다름**. 특히 제공값이 `0.00%`인데 KRX는 0이 아닌 실제 값인 경우가
많아, **제공 데이터의 0%가 진짜 무보수가 아니라 미수집 대체값일 가능성이 있음**
(해외ETF `cu_charge_rt` 0.00% 363건 의심 건과 같은 패턴). 값은 덮어쓰지 않고
`fee_conflict` 플래그만 남겨둠 — 상세는 아래 "1순위 추가" 항목 참고.

## 우선순위

### 1순위 — 국내ETF·공모펀드 총보수

- 현재 커버리지: 국내ETF 12.5%(217/1,734), 공모펀드 0%
- 왜: 과제 예시 질문에도 나온 핵심 비교 지표라 공백이 제일 크게 티남
- 출처 후보: KOFIA(금융투자협회) 전자공시
- 조인키: 종목코드/펀드코드
- **[완료] 공모펀드**: KOFIA "펀드별 보수비용비교" 파일(`external_data/`) 확보, `std_itm_no`=`표준코드`로 조인. 7,293/11,138건(65.5%) 채움. `scripts/enrich_fund_fees.py`. `expense_ratio_source`로 출처(`external_kofia_20260805`) 항상 구분 표시.
- **[보류] 국내ETF**: 위 파일의 ETF 항목은 KRX 상장코드가 아니라 펀드등록코드라 우리 `pd_itm_no`와 형식이 달라 이름 매칭 시도했으나 성공률 2.8%로 실패. 다른 조인키(펀드등록코드) 확보 전까지 보류.

### 1순위 추가 — 국내ETF 총보수 (별도 KRX 소스)

- **[완료]** KRX ETF 종목정보(`external_data/KRX_ETF_종목정보_20260806.xlsx`) 확보, `표준코드`=`pd_itm_no`로 정확히 조인(펀드와 달리 코드 체계 일치). 1,132/1,734건(65.3%) 매칭.
- 기존 제공 데이터가 있던 215건 중 **201건(93.5%)이 KRX 값과 상충**. 특히 제공값=0.00%인데 KRX는 0이 아닌 경우가 많음 → 해외ETF에서 의심했던 "0%=미수집 대체값" 가설을 뒷받침하는 강한 증거.
- 처리 원칙: 과제 규칙(상충 시 주최 측 데이터 우선)에 따라 **제공값은 절대 덮어쓰지 않음**. 결측(1,517건)만 KRX로 채우고, 상충 201건은 `fee_conflict=True`로 플래그만 남김 (`scripts/enrich_domestic_etf_fees.py`).
- 결과: 국내ETF 총보수 커버리지 12.5% → **65.4%**
- **[완료] 기초지수명**: 같은 파일로 결측만 보강. 제공 커버리지 3.3%(58/1,734) → **68.5%**. 겹치는 2건은 상충 빈도가 무의미해 별도 플래그 없이 결측만 채움
- `기초자산분류`는 아직 반영 안 함(다음 후보, taxonomy 대조와 함께 고려)

### 2순위 — 국내ETF ETF/ETN 구분

- **[완료]** KRX ETN 종목정보(`external_data/KRX_ETN_종목정보_20260806.xlsx`, `상품구분`='ETN' 명시) 추가 확보.
- ETF 파일 매칭(1,132건)과 ETN 파일 매칭(360건)이 **교차 0건**으로 완전히 분리됨을 확인하는 과정에서,
  `pd_itm_no` **접두사 자체가 완벽한 구분자**임을 발견 (`KR7`=ETF, `KRG`=ETN). 외부 파일 없이도
  100% 적용 가능한 내부 규칙이라 `scripts/normalize_data.py`에 직접 반영.
- 결과: ETF 1,201건 / ETN 532건 / 미분류 1건(코드가 `KR`뿐인 깨진 행, 이름도 `.`)
- 조인키: 종목코드(검증용으로만 사용, 최종 규칙은 코드 접두사)

### 3순위 — taxonomy 공식 분류체계 대조

- 현재 상태: 지역·자산군 매핑 104개 중 11개 ambiguous, 1개 unmapped (수작업 매핑)
- 출처 후보: KOFIA 표준 펀드분류 등 공식 체계
- 급하지 않음, 여유 있을 때 대조해서 애매한 것 줄이기

### 보류 (필요성 불명확 / 비용 큼)

- 벤치마크·기초지수 시계열: 있으면 "추종지수 대비 수익률" 같은 답변 가능하지만 외부 시계열 정합 비용 큼
- 신용등급 세부화, 환율 데이터: 현재 결측·필요성 낮음

## 대량 수집 아님 — 지금 바로 해도 되는 표본 검증

해외ETF `cu_charge_rt`가 0.00%인 363건(ARKK 등 액티브 펀드 포함) 중 1~2개만 운용사 공시로 대조해서
"미수집 대체값"인지 "진짜 0%"인지 확정. 대량 수집 전에 먼저 하면 좋음.
