# 용어집 (GLOSSARY)

> **이 문서는**: 이 프로젝트의 모든 문서·코드 주석에 나오는 용어를 처음 보는 사람도 이해할 수 있게 풀어쓴 사전입니다.
> 다른 문서를 읽다가 모르는 말이 나오면 여기서 찾으세요. 새 용어를 문서에 쓸 때는 반드시 여기에 등재합니다.

---

## 1. 대회·평가 용어

| 용어 | 뜻 |
|---|---|
| **평가 API** | 대회 주최가 우리 서버 주소로 질문을 보내고, 우리 서버가 답을 돌려주는 방식의 시험. 사람이 아니라 프로그램이 `GET /answer?question_id=...&question=...` 형태로 호출한다. |
| **5필드 응답** | 답변 JSON에 반드시 들어가야 하는 5개 항목 — ① question_id(문항 번호) ② question(질문 원문 되돌려주기) ③ retrieved_context(답의 근거가 된 데이터 조각들) ④ think_trace(어떤 과정으로 답했는지 기록) ⑤ answer(최종 답변). **5개 전부 문자열(string) 타입**이어야 한다는 게 공식 규격. |
| **트랩(함정) 문항** | 일부러 답할 수 없게 만든 문제. 예: "신용등급 AAAA인 채권"(그런 등급은 세상에 없음), "kimi 관련 상품"(데이터에 없음). **그럴싸하게 답을 지어내면 감점**이고, "확인할 수 없다"고 말하거나 "어떤 조건인지 알려 달라"고 되묻는 것이 정답(공식 자료: 답변 불가 질의 5문항 포함 예정). |
| **API 명세서** | 우리 서버를 부르는 방법(주소·파라미터)과 돌려주는 형식(JSON 필드)을 적은 문서. 대회 제출 필수물이며 우리 것은 [API_SPEC.md](API_SPEC.md). |
| **End-point(엔드포인트) URL** | 채점 프로그램이 접속할 우리 서버 주소(예: `http://공인IP/answer`). NCP 배포 후 확정해 README 와 API 명세서에 적는다. |
| **제출물 3종** | ① 소스코드(+ requirements.txt + README + 온톨로지 TTL 5개 파일) ② 기술제안서(PDF) ③ 서버 주소 + API 명세서. 주최가 준 GitHub 비공개 저장소에 올린다. |
| **behavior(답변 태도) 3분법** | 모든 문항의 정답 태도를 3가지로 나눈 것 — **answer**(정상적으로 답한다) / **refuse**(답할 수 없다고 안내하는 게 정답) / **partial**(답하되 "데이터가 일부만 있다" 같은 한계를 반드시 함께 말해야 함). |
| **기준일 / 스냅샷** | 모든 데이터는 **2026-07-11 시점에 찍은 사진(스냅샷)**이다. 그 이후의 정보(예: 8월 상장 ETF, 실시간 주가)는 우리가 모르는 게 정상이고, 모른다고 답해야 한다. ETF 구성종목만 7/11이 토요일이라 직전 영업일인 **7/10** 기준. |
| **근거 표시** | 답변에 "이 사실은 어느 데이터의 몇 번 상품에서 나왔고 기준일이 언제인지"를 붙이는 것. 대회 필수 규칙이며, 우리 시스템에서는 Evidence(근거) 규격으로 통일했다. |
| **평가 러너(채점기)** | 우리가 만든 모의고사 105문항을 우리 시스템에 자동으로 던지고 채점하는 프로그램(`evalset/eval_runner.py`, 8/19 완성). 답변 태도·근거 표시·내용(문항별 검사표)·응답 시간 4축으로 채점해 성적표를 만든다. 코드를 고칠 때마다 다시 돌려 "좋아졌나 나빠졌나"를 숫자로 본다(회귀 확인). |
| **NCP** | 네이버 클라우드 플랫폼. 대회 규정상 서버를 여기에 띄워야 하고, 크레딧(무료 사용권)을 지급받는다. |
| **HCX-005 (HyperCLOVA X)** | 네이버의 AI 언어모델. 대회 규정상 **답변 생성과 질문 해석에는 이 모델만** 쓸 수 있다(다른 회사 AI 쓰면 실격). |
| **파인튜닝** | AI 모델을 우리 데이터로 추가 학습시키는 것. 우리 팀 방침: 필수가 아니며, 정확성은 검색+검증 구조에서 나오므로 후반에 여유 있으면 검토. |
| **기술제안서** | 시스템 구조와 설계 이유를 설명하는 제출 문서. 배점 40점으로 실전 답변(40점)과 동급이라, 개발 중 실측 수치·설계 근거를 계속 모아둔다. |
| **Ontology.ttl (온톨로지)** | "상품·회사·지수 같은 개념들이 서로 어떤 관계인지"를 정의한 파일. 제출 필수물. 지식그래프의 설계도에 해당한다. 주최 표현으로는 온톨로지 = "무엇이 가능해야 하는가"(개념·규칙), 지식그래프 = "실제로 무엇이 있는가"(실데이터). 공식 자료는 **`ontology/` 아래 파일 5개**(common.ttl 공통 상위 · bond_kr.ttl 국내채권 · etf_kr.ttl 국내ETF · etf_gl.ttl 해외ETF · fund_pub.ttl 공모펀드)로 내라고 정했다. |
| **Turtle (.ttl)** | 온톨로지를 적는 파일 형식 중 사람이 읽기 가장 쉬운 것. "주어–관계–목적어" 한 줄씩 적는 방식(예: `TIGER_200 운용사는 미래에셋 .`). 같은 내용을 RDF/XML·JSON-LD 형식으로도 쓸 수 있지만 대회는 Turtle 기준. 우리 제출물은 `ontology/` 의 5파일(common·bond_kr·etf_kr·etf_gl·fund_pub) — 이름 앞에 붙는 `fp:` 는 "이 이름은 우리 금융상품 온톨로지의 것"이라는 표시(공식 예시 접두어). |
| **SHACL** | 온톨로지에 적은 규칙을 데이터가 지키는지 자동 검사하는 언어(예: "위험등급은 1~6만 허용", "해외 ETF 는 위험등급이 없어야 한다"). 주최 기술 세션이 강조한 것. **8/19 도입** — 규칙을 `ontology/shapes.ttl` 에 적어 두고 `python kg/validate_shacl.py` 로 그래프 파일을 검사한다(위반 0건 확인). 같은 규칙을 그래프 생성 코드도 검사하므로 두 겹 검사가 된다. 서버 실행에는 필요 없다. |
| **연합 검색 (Federated Query)** | 질문 하나를 조각내어 표(SQL)·관계(그래프)·의미(벡터)·이름(키워드) 저장소에 각각 물어보고 결과를 합치는 방식. 주최 세션 결론: "한 가지 검색법의 정답률 천장은 약 66%, 표+그래프 결합이 가장 안정적". 우리 4채널 구조가 이것이다. |
| **필수 구성요소 4** | 주최가 정한 에이전트의 4부품 — ① 데이터 정제 + 도메인 온톨로지 ② 지식 저장소(RDB+벡터+그래프) ③ 의도 분석·검색 엔진 ④ 근거 기반 답변 생성기. 우리 구현과의 대응은 ROADMAP §2 표. |

## 2. 우리 시스템의 구조 (질문이 들어오면 지나가는 길 순서)

| 용어 | 뜻 |
|---|---|
| **에이전트** | 질문을 받아 스스로 필요한 조회를 하고 답을 만드는 프로그램 전체를 부르는 말. |
| **라우터(Router)** | 질문을 읽고 "어떤 저장소에 어떤 조회를 할지 계획을 세우는 두뇌". 우리는 2단계로 만들었다 — **1단(규칙)**: 미리 짜둔 규칙으로 처리(AI 호출 0번, 빠르고 오답 없음). **2단(AI)**: 규칙으로 안 되는 복잡한 질문만 HCX-005에게 계획을 세우게 함. |
| **채널** | 데이터를 조회하는 통로 4가지 — **SQL 채널**(표 데이터 필터·정렬·집계), **그래프 채널**(관계 추적: 누가 뭘 담았나), **키워드 채널**(이름 사전에서 찾기), **벡터 채널**(의미로 찾기). 질문 유형에 따라 라우터가 조합해 쓴다. |
| **SQL 템플릿** | 미리 사람이 작성·검증해 둔 데이터베이스 조회문 26종. **AI는 조회문을 직접 쓰지 못하고 템플릿 번호와 빈칸 값만 고른다** — AI가 엉터리 조회문을 지어내는 사고를 원천 차단하는 장치. |
| **엔티티 / grounding(그라운딩)** | 엔티티 = 질문에 나오는 개체(상품명·종목명·운용사·지수). grounding = 그 이름을 실제 데이터의 정확한 코드로 연결하는 일(예: "삼성전자" → 종목코드 005930). |
| **정확 일치 vs 부분 일치** | 정확 일치 = 이름이 완전히 같음(띄어쓰기·대소문자만 무시) → 존재의 근거로 인정. 부분 일치 = 이름의 일부만 겹침 → "혹시 이걸 찾으셨나요" 안내까지만 하고 **존재의 근거로는 절대 쓰지 않음**(함정 방어의 핵심 규칙). |
| **답변 검증(Validation) 게이트** | 답이 나가기 직전 통과해야 하는 **자동 검문소 5곳**. ① 값이 실제 존재하는 범위인가(AAAA 등급? 0등급? → 차단) ② 질문 속 상품·종목이 데이터에 진짜 있나 ③ 시점이 데이터 범위(7/11) 안인가 ④ 요청한 정보 항목이 데이터에 있기는 한가(예: 해외 ETF엔 위험등급 항목 자체가 없음) ⑤ 데이터가 충분한가(예: 보수 정보는 12.5%만 있음 → 한계 명시 강제). 라우터가 놓쳐도 여기서 잡는 **이중 방어**. |
| **거절문 템플릿** | "확인할 수 없습니다"류의 답은 AI가 자유롭게 쓰지 않고 **미리 정해둔 문장 틀**로만 나간다. 함정 문항에서 AI가 말을 지어낼 통로를 아예 없애는 장치. |
| **생성기** | 조회된 근거들을 바탕으로 HCX-005가 최종 답변 문장을 쓰는 단계. 생성 후에는 **사후 대조(post-check)** — 답변 속 상품명·숫자가 근거에 없는 것이면 그 문장을 삭제한다(AI가 아는 척 끼워 넣는 것 방지). |
| **데드라인 강등** | 응답 목표 15초를 지키기 위해, 단계별 제한시간을 넘기면 더 간단한 방법으로 자동 전환하는 것(예: AI 계획이 늦으면 규칙 계획으로). |
| **폴백(fallback)** | 원래 방법이 실패했을 때 대신 쓰는 예비 방법. 우리 시스템은 어떤 단계가 실패해도 폴백이 있어 "응답 없음"이 나오지 않게 설계돼 있다. |

## 3. 데이터·저장소 용어

| 용어 | 뜻 |
|---|---|
| **제공 데이터 4종** | 주최가 준 원본(엑셀) — 국내채권 42,394건 · 국내 ETF/ETN 1,733건 · 해외 ETF 5,646건 · 공모펀드 95,618행(실제 상품 11,138개). |
| **구성종목** | ETF 하나가 담고 있는 개별 주식·채권들. 제공 데이터에 없어서 **KRX(한국거래소)에서 직접 수집**했다(1,139개 ETF, 75,081행). |
| **RDB / DuckDB** | RDB = 표 형태로 데이터를 저장하고 SQL로 조회하는 표준 데이터베이스. DuckDB = 우리가 쓰는 RDB 제품 — 서버 프로그램 없이 **파일 1개(31MB)**로 동작해서 작은 서버에 알맞다. |
| **지식그래프(KG) / 트리플** | 데이터를 "A—관계—B" 화살표들의 그물로 저장한 것(예: TIGER 200 —운용사는→ 미래에셋). 트리플 = 그 화살표 1개. "삼성전자를 담은 ETF 전부?" 같은 관계 질문에 강하다. |
| **임베딩 / 벡터 검색** | 임베딩 = 문장을 의미를 담은 숫자 배열로 바꾸는 것(CLOVA 임베딩 API 사용). 벡터 검색 = 그 숫자로 "뜻이 비슷한 문서"를 찾는 것 — 단어가 달라도 찾는다(한국어 질문 ↔ 영어 설명문). |
| **RRF (순위 결합)** | 벡터 검색 순위와 키워드 검색 순위를 합쳐 최종 순위를 내는 계산법. 벡터 단독의 오답(실측: "반도체" 질문에 로보틱스 ETF 1위)을 키워드 근거로 눌러 바로잡는다. |
| **센티널 값** | 진짜 값이 아니라 "특수 상황 표시용"으로 넣어둔 값. 예: 만기일 99991231 = 영구채(만기 없음), 종목코드 CASH… = 현금 표시. 이걸 진짜 값으로 착각하면 오답이 된다 — 전처리에서 전부 표시 처리했다. |
| **커버리지** | 어떤 항목에 값이 실제로 채워진 비율. 예: 국내 ETF 총보수는 12.5%만 값이 있음 → "총보수 최저 상품" 질문에는 이 한계를 반드시 밝혀야 함(partial). |
| **정책 파일 (config/policy.json)** | 해석이 갈리는 규칙("AA 이상에 AA-가 포함되나" 등)을 코드가 아니라 설정 파일에 둔 것. 주최 답변·팀 결정이 나오면 **코드 수정 없이 파일만 바꿔** 반영한다. |

## 4. 개발·품질 용어

| 용어 | 뜻 |
|---|---|
| **단위 테스트** | 프로그램의 작은 부품 하나가 정해진 입력에 정해진 출력을 내는지 자동으로 확인하는 코드. 현재 333개가 있고(8/19 기준) 30초 안에 전부 돈다. |
| **회귀 테스트** | "예전에 되던 것이 코드 수정 때문에 다시 망가지지 않았는지" 확인하는 테스트. 예: 함정 15문항 방어 테스트를 만들어 두면, 이후 어떤 수정을 해도 테스트가 자동으로 다시 돌아서 함정 방어가 깨지는 순간 바로 알게 된다. **"트랩 회귀 상시" = 함정 방어 테스트가 매번 자동으로 도는 상태**를 말한다. |
| **E2E (엔드 투 엔드)** | 입구부터 출구까지 전 구간. "E2E 관통" = 질문을 넣으면 실제 답변 JSON이 나오는 전체 흐름이 끊김 없이 동작한다는 뜻. |
| **M1 / M2 (마일스톤)** | 중간 목표 지점. **M1 = 내 컴퓨터에서 E2E 관통**(8/14 달성) · **M2 = NCP 서버에 올려 외부에서 관통**. |
| **멱등** | 같은 작업을 몇 번 다시 실행해도 결과가 같아서 안전하다는 뜻(전처리·적재 스크립트가 이렇게 만들어져 있다). |
| **Function Calling (FC)** | AI에게 "이 양식으로만 대답하라"고 선택지를 강제하는 호출 방식. 우리 2단 라우터에서 HCX가 템플릿 번호+값만 고르게 하는 데 쓴다. |
| **스텁** | 나중에 제대로 만들 부품 자리에 임시로 끼워둔 간단한 버전. |
