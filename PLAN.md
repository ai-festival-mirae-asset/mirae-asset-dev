# PLAN — 프로젝트 계획서 (과거 · 현재 · 미래)

> **이 문서는**: 이 프로젝트의 **과거에 완료한 계획, 현재 진행 중인 작업, 앞으로의 계획**을 한곳에 모은 문서입니다.
> 2026-08-14에 세 문서를 통합했습니다 — 구 S2_PLAN.md(구현 계획) + 구 PROGRESS_20260813.md(8/13 진행 보고) + 구 memory.md(날짜별 작업 일지 → 부록 A).
> 모르는 용어는 [용어집(GLOSSARY.md)](GLOSSARY.md), 대회 규칙·상세 수치는 [ROADMAP.md](ROADMAP.md), 처음 오신 분은 [README.md](README.md)부터.

**문서 갱신 규칙**: ① 단계가 끝나면 §2 진행표와 부록 A 일지에 기록 ② 쉬운 말로 쓴다(전문용어는 용어집 등재 후 사용) ③ 추정은 `가정`, 미확정 선택은 `잠정 결정` 표시 ④ 실패·막힌 것도 지우지 말고 원인과 함께 남긴다 ⑤ 비밀값(API 키·쿠키)은 기록하지 않는다.

---

## §1. 목표와 마일스톤

- **최종 목표**: 주최가 우리 서버에 질문을 보내면 15초 안에(무감점 경계 — 설명회 발화, 8/22 확인. 문서 권장 60초) 근거 있는 답변 JSON이 돌아오는 상태. 함정 문항에는 오답변 0건. 서버는 **9/7~9/20 두 주 동안 사람 손 없이 계속 켜져 있어야** 한다(공식 자료: "운영 09.07~09.20 상시 활성").
- **제출물 3종**(9/6 23:59까지, 공식 자료 8/18 확인): ① 소스코드 + `requirements.txt` + README(환경 구성·실행 명령어) + **온톨로지 TTL 5개 파일** ② 기술제안서(PDF, 배점 40) ③ 서버 주소 + **API 명세서**([API_SPEC.md](API_SPEC.md) 초안 완료).
- **중간 목표 M1** — 내 컴퓨터에서 질문→답변 전 구간 동작 (✅ 8/14 달성)
- **중간 목표 M2** — NCP 클라우드 서버에 올려 외부에서도 동작
- **고정 날짜는 9/6 제출 마감뿐** — 진행은 날짜가 아니라 순서 기준(앞 단계가 뒤 단계의 재료). 참고 페이스: 8/23까지 원격 관통이면 여유.
- 저장소 선택(승인): 표 데이터는 **DuckDB**(서버 프로그램 없이 파일 1개로 도는 정식 RDB), 관계는 **지식그래프**(메모리 사용, 실측 620MB로 4GB 예산 안), 의미 검색은 **벡터 인덱스 파일**. 셋 다 파일이라 상주 DB 프로그램 0개 — 작은 서버(2vCPU/4GB)에서 고장 지점 최소화.

## §2. 과거 — 완료한 것

### 2.1 준비 단계 (8/3 ~ 8/13, 설명회 이후 첫 주)

과제의 핵심 판단 2가지가 이 기간에 확정됐습니다: **① 중·상 난이도 문제는 제공 데이터만으로 못 푼다**(ETF가 어떤 종목을 담는지 등은 직접 수집 필요) **② 함정 문항(답하면 감점)이 섞여 나온다** — 그래서 "잘 답하기"만큼 "모르는 걸 모른다고 하기"가 점수를 가릅니다.

| 완료물 | 내용 (쉬운 설명) |
|---|---|
| 데이터 정제 (8/7) | 제공 엑셀 4종을 그대로 쓰면 틀리는 지점들을 코드로 교정 — 펀드 9.5만 행의 실제 상품 수는 1.1만(판매 클래스 중복), 국내 "ETF" 데이터의 30%는 사실 ETN, 채권 38%는 이미 만기 지남, 가짜 -100% 수익률 제거 등 규칙 79건 |
| 온톨로지 + 지식그래프 (8/11) | 상품·회사·지수를 관계로 연결한 그래프 + 제출 필수물 Ontology.ttl 초안. "TIGER 200의 운용사는?" → "미래에셋"이 그래프에서 답해짐 |
| ETF 구성종목 직접 수집 (8/12~13) | KRX가 로그인제로 바뀌어 있었지만 세션 방식으로 해결, **활성 ETF 1,139개 전량의 구성종목 75,081행 수집(실패 0)**. 덤: 국내 ETF가 담은 해외 주식 3.4만 행이 포함돼 "캠브리콘 편입 중국 반도체 ETF"(공식 중 난이도 예시)가 국내 데이터만으로 16종목 답변됨을 실증 |
| CLOVA 실전 연결 (8/13) | HCX-005 채팅·Function Calling 실 호출 검증 + 임베딩 v2 승인받아 해외 ETF 전략 설명문 5,567건 인덱싱. 한계도 실측: "반도체 집중" 질의에 로보틱스 ETF가 1위 → 의미 검색은 키워드와 결합 필요(이후 ③에서 해결) |
| 자체 모의고사 105문항 (8/13) | 실전(30+함정5)의 3배수. 전 문항을 실측 확인된 사실로만 작성, 함정 15문항 포함. 개발 방향 점검과 자동 채점의 잣대 |
| 데이터 결함 발견·수정 | 운용사 이름 오염 64행 복구 · 현금 표시를 종목으로 착각하던 685개 가짜 관계 정리 · 한글 별칭 사전 66개(캠브리콘→중국 ISIN 등) |

### 2.2 구현 8단계 진행표

| 순서 | 하는 일 | 상태 |
|---|---|---|
| ① | **저장소 적재** — 정제 데이터를 DuckDB 6개 표(31MB)로, 그래프 메모리 실측(620MB), 근거(Evidence) 규격 확정, 운용사 오염 복구, 한글 별칭 사전 | ✅ 완료 (8/13) |
| ② | **검색 기반** — 모든 이름(상품·종목·운용사·지수)의 통합 사전(정확 일치/부분 일치 엄격 분리), 미리 검증한 SQL 조회문 26종(AI는 번호+값만 선택), 키워드 채널 | ✅ 완료 (8/13) |
| ③ | **라우터 + 채널 실행기** — 질문을 읽고 조회 계획을 세우는 두뇌(규칙 우선, 복잡하면 HCX 1번) + 4채널 실행 + 답변 JSON 조립 → **M1 달성**. 의미 검색 순위결합으로 "반도체→로보틱스" 오답 정정(실 호출 검증) | ✅ 완료 (8/14) |
| ④ | **답변 검증** — 답이 나가기 직전 5중 검문소(값·존재·시점·항목·충분성) + 거절이 정답인 16문항 자동 방어 테스트(이후 코드 수정 때마다 자동 재검증). 테스트가 실제 구멍 2개를 잡아 수정('kimi'가 해외 지수명 공백 제거 문자열에 우연 포함 / '챗GPT'의 GPT가 실제 티커와 오인) | ✅ 완료 (8/14) |
| ⑤ | **답변 생성기** — 검증 통과 근거만 HCX-005에 주고 최종 문장 생성, **사후 대조**로 근거에 없는 상품명·숫자가 든 줄 자동 삭제, 실패 시 규칙 요약 폴백. 실 호출 6.2초 검증 | ✅ 완료 (8/14) |
| ⑥ | **API 서버** — `/answer` 주소로 서비스(공식 규격). 시작 시 저장소 미리 적재, 시간 예산 초과 시 자동 강등, 같은 질문 캐시(실패·강등 응답은 캐시 금지), 어떤 오류에도 HTTP 200 + 유효 JSON, 브라우저 질문 시험대(`/`) 포함. 실 가동 검증: 그래프+HCX 구성으로 6.3~9.7초 응답 | ✅ 완료 (8/14) |
| ⑦ | **자동 채점기** — 모의고사 105문항을 서버와 같은 부품으로 자동 실행·4축 채점(태도→근거→내용 검사표→시간). 함정 16문항·가짜 답변으로 채점기 자체를 먼저 검증(테스트 8개) → **첫 정량 성적표(8/19, 실전 구성): 종합 통과 80/105(76%) — 하 26/30 · 중 25/30 · 상 14/30 · 함정 15/15, 함정 오답 0/16, 과잉 거절 3, 응답 중앙값 5.4초·95% 지점 11.0초·15초 초과 1건**(`evalset/reports/eval_20260819_1045_full.md`). 채점기가 실제 결함 4건을 찾아 즉시 수정(펀드 '판매중' 값 오판, 기준일 표기 누락 2종, 거절문 안내 우연 겹침) | ✅ 완료 (8/18~19) |
| ⑦-2 | **온톨로지 5파일 분할(공식 형식)** — `finance.ttl` → `ontology/common.ttl`(공통 상위 `fp:Product`·기관·지수·공통 속성)·`bond_kr.ttl`·`etf_kr.ttl`·`etf_gl.ttl`·`fund_pub.ttl`, 접두어 `fp:`·공식 예시 선언(`fp:ForeignETF ⊑ fp:ETF`) 그대로 채택, 국내/해외 ETF·ETN 4클래스 신설. **덤: 데이터 규칙 검사 파일 `shapes.ttl`(SHACL)** — 위험등급 1~6·신용등급 AAA~D·ETF/ETN 배타·해외ETF 위험등급 없음 같은 규칙을 선언문으로 적고 `kg/validate_shacl.py`로 그래프를 독립 재검사(위반 0). 그래프 생성 코드 동기 + 전량 재생성(펀드 '판매중' 여부 11,138건 신규 적재 — 값 정정 효과) + 테스트 15개(5파일 파싱·공식 선언·**코드가 쓰는 어휘가 전부 온톨로지에 있는지**·SHACL 이 위반을 실제로 잡는지). 채점기 회귀 없음(HCX 없이 87/105 유지) | ✅ 완료 (8/19) |
| ⑧ | **다듬기 + 배포** — **다듬기(8/19) 완료**: 첫 성적표의 실패 유형을 규칙·템플릿·생성기 세 층에서 보정 — 규칙 라우터 11종 신설(별칭 복수 상장, 그룹·계열사, 상품명 조각 우선·조각 교집합, 운용사×테마, 펀드 상세, 코스닥 비중, 리츠 구분, 수익률 상위 공통 종목, 만기형 채권 ETF 만기 창, 지역 제외 해외 ETF, 분포·건수·결측·파생 노트) + SQL 템플릿 7종 신설·6종 보정(총보수 0 표기 제외, 순자산 정렬, 점유율 사전 계산, 잔존만기 SQL 계산, 채권 약칭, 원화 금액 환산 표기 자동 첨부) + 사후 대조 완화(조·억 환산, 순위 표기, 같은 상품 표기 변형, 이름 한 글자 오기 정정) + HCX 라우터 파라미터 의미 검증(이름→키, 숫자·날짜·범위) + 답변 3단 형식·낮은 온도·고정 seed + 타임아웃 조정(계획 6초·생성 8초·생성 진입 7초) + **네트워크 벽시계 상한**(DNS 정지 368초 사고 실측 → timeout+2초에 강제 종료). **HCX 없이 105/105**, **실전 구성 105/105(100%)** — 하 30/30 · 중 30/30 · 상 30/30 · 함정 15/15 · 함정 오답 0/16 · 과잉 거절 0 · 한계 미명시 0 · 중앙값 2.84초 · 95% 지점 8.3초 · 최대 9.24초(15초 초과 0건 · 60초 초과 0건 — 생성 시간 초과로 규칙 요약 폴백 12건) · 실행 3회(12:33 96 → 12:55 103 → 13:22 105) 동안 새로 실패 누적 0(`evalset/reports/eval_20260819_1322_full.md`). **배포 묶음** `infra/deploy/`(설치 스크립트·systemd·상태 점검 cron·예열·운영 안내서) 완성 — 실제 NCP 서버 생성·원격 리허설(M2)은 사용자 계정 작업 대기. **[8/22 개선 완료]** 시간 규정 확정(설명회 발화: 15초 이하 무감점 → 내부 목표 15초 유지) · HCX 강건성 3종(상품명 띄어쓰기 복원·라우터 파라미터 의미 검증·근거 0개 방지망) · **별칭 통합**(그래프 이름표 218개, 병합 없음) · **벡터 국내 확장**(합성 문장 1,733건 + 시장 필터) · 채권·펀드 임베딩은 실측 근거로 안 만들기로 결정 → **최종 105/105 · 15초 초과 0건 · 최대 10.71초**(`evalset/reports/eval_20260822_0228_full.md`, 부록 A 8/22) | 🔜 다듬기 완료 · M2 대기 |
| ⑨ | **제출물 3종 마무리** (8/18 신규) — 기술제안서 완성(골격은 ⑦부터 상시 축적) · API 명세서에 서버 주소 기입 · README 서버 주소·실행 명령어 최종 · 온톨로지 5파일 최종 동기 · 9/6 23:59 전 마지막 푸시 | ⬜ |
| 상시 | 기술제안서 재료 모으기(배점 40) — 구조도·실측 수치·설계 이유를 단계마다 기록. 8/18부터 주최 기술 세션 자료의 용어·수치(ROADMAP §4.5)를 인용 틀로 사용 | 진행 중 |

자동 테스트 **395개 전부 통과** 상태 (함정 16문항 상시 방어 + 과잉 거절 방지 표본 + 공식 API 규격 잠금 + 채점기 자체 검증 + 온톨로지 5파일·SHACL 검증 + ⑧ 규칙·사후 대조·배포 파일 검사 + 별칭 통합·벡터 국내 확장(8/22) + ⑧ 3차 규칙·표현 변형 일반화 시험(8/26) 포함, 8/26 기준).

## §3. 현재 — 진행 중

**⑨(8/27, 사용자 승인) 데이터 전면 교체 — 주최 8/26 공지·문답·재배포 데이터 반영.** 07/11 구본 폐기 → 재배포본(국내 8/22 · 해외 8/23 · 배포 8/24) 기준으로 전처리 3차 개편(채권 소문자 컬럼·시장별 중복 행 대표화 R33·위험등급 코드 11~16 정정 R11·매수가능 공지 확정 규칙 R8·펀드 fss 코드 R32) → DuckDB·지식그래프·벡터(신규 495건만 임베딩)·개체 인덱스 재구축 → 엔진 반영(분배(배당) 채널 신설 + 거절 세분화 · 0값 제외 일반화 · 기준일 3원화 · 신설 컬럼 노출 · Refinitiv 기초지수 폴백 · 교차질의 채널 5.87 · HCX 필수 구간 준수 훅 2종) → 5차 3건(등급 값 단독 min=max · 구조·전략 요약 노트 · 계열사×규모) 흡수 → 시험지 4종 재생성(미러 35→38: 부록 A 3문항 — 교차질의 공식 예시·분배 2종). **HCX 포함 최종 재채점 — 4종 전부 100%: v1 105/105 · v2 80/80 · v3 80/80 · 미러 38/38(총 303문항, 함정 오답 0, 15초 초과 0, 최대 13.2초)**, 시험 395개 통과. 재채점 절차가 후속 3건을 적발·당일 수정(부록 A 8/27 — 과잉 거절 안전망 정밀화·규칙 거절 게이트 신설·비교 대상 이름 노트). 남은 것: KRX 구성종목 재수집(쿠키 재발급 필요 — 사용자), 펀드 보유 종목 수집기 설계(사용자 지시: 성능 최대 방향), M2 배포(팀원).

이전 상태(8/26): ⑧ 1~4차 완료 — v1 105/105 · v2 80/80 · v3 80/80(구본 기준), 실전 미러 32/35. 성적표는 `evalset/reports/` 에 있고, 코드를 고칠 때마다 `python evalset/eval_runner.py --baseline evalset/reports/<직전>.jsonl` 로 "새로 통과/새로 실패"를 확인한다.

**팀원 병렬 개선 도입(8/27 사용자 지시, 당일 v2 개정)**: 리더가 돌던 개선 한 바퀴(출제→채점→분석→수정→재채점)를 팀원 전원이 AI 와 함께 돌 수 있게 안내서 `evalset/TEAM_IMPROVEMENT_GUIDE.md` 작성 — 역할 요약표 + 복붙용 프롬프트 3개(① 블라인드 출제 ② 채점표 변환+측정 ③ 개선+회귀 4종) 포함. 블라인드 순서(출제 전 코드 열람 금지)·과적합 금지(일반 정책만+표현 변형 시험 동반)를 프롬프트에 내장. **운영 방식(사용자 확정)**: ⓐ 채점은 항상 HCX 포함(실전 구성이 기준 — 주최 평가가 HCX 포함이므로, --no-hcx 는 반복 수정 중간 확인용만) ⓑ 팀원은 `main` 브랜치에 직접 푸시(검토 대기 없이 지속 개선) — 대신 푸시 관문 필수(pull → 시험 395 전부 통과 → 회귀 4종 HCX 포함 점수 하락 없음 → force push 금지·비밀값 검사) ⓒ 백업: git 이력 + 리더가 "4종 100%" 확인 커밋마다 `stable-날짜` 태그를 남기고, 성능 하락 발견 시 마지막 stable 태그 기준으로 원인 커밋을 찾아 리더가 revert. main 은 8/27 papuagigi 최신 상태로 동기화(빨리감기), 이후 리더 작업도 main 기준.

서버 실행법: `python server/app.py` (기본 8000 포트, 그래프·벡터·HCX 전부 적재 — 기동 약 1분). 개발용 가벼운 시작은 `--light`. 브라우저에서 `http://localhost:8000/` 을 열면 질문을 직접 입력해 보는 시험대가 뜬다.

## §4. 미래 — 남은 계획

**8/18 제안 — 순서 조정(승인 요청)**: 공식 자료로 필수 제출물이 늘어나(온톨로지 5파일 · API 명세서 · 기술제안서) 기존 ⑦→⑧ 사이와 뒤에 두 단계를 끼운다. 이유: 온톨로지 분할은 채점 결과와 무관하게 반드시 해야 하는 일이고, 그래프 생성 코드를 건드리므로 **⑧ 다듬기 전에 끝내야 이후 성적표가 흔들리지 않는다.**

1. ~~**⑦ 자동 채점기**~~ **8/18~19 완료** — `evalset/eval_runner.py`(4축 채점: 태도·근거·내용 검사표·시간) + `checks_v1.jsonl`(105문항 검사표, 기대값은 SQL 로 계산) + 자체 검증 테스트 8개. 성적표는 부록 A 8/19 항목과 `evalset/reports/`.
2. ~~**⑦-2 온톨로지 5파일 분할**~~ **8/19 완료** — 5파일 + `shapes.ttl`(SHACL 도입 확정: 코드 검사가 1차, 선언문 재검사가 2차) + 그래프 재생성 + 테스트 15개. 상세는 §2.2 ⑦-2 행과 `kg/KG_METHOD.md` 1~4장. 팀원(그래프를 쓰는 사람)은 **`python kg/build_kg.py` 로 그래프 파일을 다시 만들어야 한다** — 옛 파일을 적재하면 서버 기동 시 "재생성 필요" 오류로 멈추게 해 두었다(조용한 오답 방지).
3. **⑧ 다듬기 + 배포**: ~~성적표 기준 보정~~ **1차 완료(8/19 — 아래 작업 목록 1~8 전부 반영)** → 남은 것: **NCP 서버에 올려 공인 IP로 원격 리허설(M2)** — 배포 묶음 `infra/deploy/`(설치 스크립트 한 방·systemd 자동 재기동·5분 상태 점검·예열·운영 안내서 `README_DEPLOY.md`) 준비 완료, 서버 생성·키 기입은 사용자 계정 작업 → 원격 리허설 성적표(`--mode http`)로 시간 확인 → 2차 다듬기(실전 성적표 잔여 실패).

   **⑧ 작업 목록 — 첫 성적표(8/18~19)가 드러낸 것, 효과 큰 순서** (문항 번호는 `evalset/evalset_v1.jsonl`):
   1. ~~**사후 대조가 맞는 줄까지 지운다**~~ **[8/19 반영]** — 조·억·만 단위 환산("약 28.4조")·순위 표기(1위)·같은 상품의 표기 변형(약칭↔정식명 — 데이터 키로 대조) 허용, 점유율은 `mgmt_top_share` 가 미리 계산(`share_pct`). 테스트로 잠금.
   2. ~~**HCX 라우터 계획의 파라미터 검증 강화**~~ **[8/19 반영]** — `router_llm.coerce_llm_params`: 키 자리의 이름→grounded 키 변환, 숫자 자리의 글자 거부, 범위(위험등급 1~6·서열 1~20·limit 1~100) 밖 거부, 날짜 형식 검사 → 위반 시 수리 콜 1회 → 규칙 폴백. HCX 계획이 0건이면 "이 조건 해석으로는 못 찾음(없다고 단정하지 않음)" 노트 강제.
   3. ~~**상품명 우선 grounding + 별칭 정규화**~~ **[8/19 반영]** — `resolve_product_candidates`(질문 어절 조합을 상품명 조각으로 검색, 종목·회사 이름은 가로채지 않음) → 규칙 6.1(상품 구성 by 이름), 6.0(그룹·계열사 — 'X그룹주' 상품 우선 + 회사명 접두 후보 집계), 8(펀드 부분 명칭 → 마스터 상세). 별칭의 복수 상장(구글=알파벳 A/C, 알리바바=홍콩/ADR)은 한 개체로 합쳐 조회.
   4. ~~**새 조회 경로**~~ **[8/19 반영]** — 팀원(8/18)의 H-12/H-13/H-09/L-29/H-15 + 8/19 신설: 운용사×테마 구성(H-08), 두 종목 교집합+보수(H-03 — 총보수 0 표기 제외), 코스닥 비중(H-22), 리츠 ETF vs 개별 리츠(H-07), 잔존만기 복합(H-26 — 채권 약칭 표시). SQL 템플릿 5종 신설(`etp_pattern_top_constituents`·`constituent_group_holders`·`fund_detail`·`constituent_ksq_share`·`reit_constituents`).
   5. ~~**이름이 지목된 상품의 부가 정보 자동 결합**~~ **[8/19 반영]** — 위험등급 병기(편입 ETF 행에 포함), 구성 공시가 비어 있으면 '구성 공시 없음' 명시(M-19 실측: ACE 애플밸류체인액티브), 기초지수 결측 명시(L-28), 국내/해외 비교 시 위험등급 비대칭 명시(H-04), 개별 리츠(H-07). 남음: 구성이 선물이면 파생 위험 설명(H-21 — 현재도 통과).
   6. ~~**HCX 답변의 흔들림**~~ **[8/19 반영]** — 생성 온도 0.2 + 고정 seed, 라우터 온도 0.1 + 고정 seed, 3단 형식 지시. 흔들림 폭은 다음 실전 성적표 2회 비교로 확인.
   7. ~~**시간**~~ **[8/19 반영]** — 라우터 타임아웃 8→6초, 생성 10→8초, 생성 진입 한계 8→7초 → 최악에도 14초 안(15.83초 사례 방지). 추가로 **벽시계 상한**(`agent/net_guard.py`): 12:55 성적표 실행 중 HCX 호출 1건이 DNS 조회 정지로 368초 멈춘 사고 실측(httpx timeout 밖) → 모든 HCX·임베딩 호출을 timeout+2초에 강제 종료하고 규칙 폴백. 실전 성적표: 중앙값 2.84초 · 95% 지점 8.3초 · 최대 9.24초(15초 초과 0건 · 60초 초과 0건 — 생성 시간 초과로 규칙 요약 폴백 12건).
   8. ~~**주최 기대 답변 형식(결론 → 비교 → 근거)**~~ **[8/19 반영]** — 생성 지시문 3단(결론 → 상품별 비교 → 근거·기준일) + 분포 결론 문장 자동 생성("거래통화: 전부 USD(5,646건) — 다른 거래통화 없음", 규칙 답변·생성 답변 모두에 강제).
4. **⑨ 제출물 3종 마무리**: 기술제안서(권장 목차 6항 — 특히 5번 기대효과·확장성) 완성 · [API_SPEC.md](API_SPEC.md) 와 README 에 서버 주소 기입 · 온톨로지 5파일 최종 동기 · 9/6 23:59 전 마지막 푸시(이후 변경 = 실격).
5. **상시**: 기술제안서(배점 40) 재료 축적 — 각 단계의 실측 수치·설계 이유·구조도가 그대로 제안서 §2~⑤의 소재. 주최 기술 세션 자료의 주장(단일 검색기 천장 ~66%, SQL 실행 성공 ≠ 정답 45%, 온톨로지는 런타임 통제 +20pp 등 — ROADMAP §4.5)을 인용 틀로 쓴다.
6. **대기 사안**: 주최 문의(함정 채점 기준) — 답변 수신 시 정책 파일(config/policy.json) 교체만으로 반영. ~~구성종목 수집 전제 문의~~ → 8/18 공식 자료("기준데이터만 물어보지 않음", 외부 수집 가능)로 사실상 해소. 페스티벌 크레딧(20만) 신청 경로 확인. 주최 발신 IP 대역·LLM 활용 기준·난이도별 배점 공지 수신 시 반영.
7. **선택(여유 시, 우선순위 순)**: ① **자회사 관계 수집(DART)** — 8/18 상향: 공식 예시(상)와 주최 기술 세션이 같은 문항("에코프로의 자회사를 편입한 ETF")을 쓴다 ② 비정형 문서 파싱(투자설명서 등 → 필수 구성요소 ①의 [Parsing] 항목, 중 예시 "국민성장펀드 동향" 유형) — ⑦ 성적표에서 해당 유형 실패가 확인되면 착수 ③ 해외 상장 ETF 자체의 구성종목 수집(SEC N-PORT — 국내 수집분이 커버함을 실증했으므로 긴급도 낮음) ④ 파인튜닝 검토(정확성은 검색+검증 소관이므로 후반 선택 최적화).

   **⑧ 2차 작업 목록 — 블라인드 v2(8/22, 46/80)가 드러낸 것, 효과 큰 순서**:
   **[8/22 밤 전부 반영 — 1~6 완료, v2 46 → 73/80(검사표 정정 포함) · 함정 오답 8 → 0. 상세는 부록 A 8/22(3)]**
   1. **이름 경계 검사** — 정확 일치는 이름 뒤에 글자·숫자가 이어지면 무효("TIGER 코스피300"≠"TIGER 코스피", "애플파이"≠"애플", 별칭 "KB스타"가 펀드명 속에서 잡히지 않게). 함정 오답 2 + 엉뚱한 상품 1 해소.
   2. **거절 문장 통일** — 폴백·HCX 경로에서도 "확인할 수 없음" 고정 문장으로만 거절(자유 문장 거절 금지) + 행위 요청(매수·주문)·실시간 변형 탐지 → 함정 5건.
   3. **상품 속성 규칙** — 이름이 잡힌 채권(만기일·신용등급·표면금리)·ETF(상장일·기초지수)·펀드(위험등급·순자산) 속성 질문 템플릿 → 7건.
   4. **운용사 결합 규칙** — 정식명 별칭을 라우터에도 적용 + 운용사×(순자산 순위·건수·테마) SQL 템플릿 + "상위 N개 ETF 의 운용사"는 상품 순위 우선 → 6건.
   5. **띄어쓰기 없는 질문** — 존재 검문이 문장 전체를 상품명으로 보지 않게(규칙 결과가 있으면 거절 대신 노트) → 4건.
   6. 판매 중 펀드 수 필터 · 회사채 분류(은행채 제외)·목록 개수 · 해외 주식형 펀드 순자산 정렬 · ETF/ETN 건수 비교 → 4건.
   **⑧ 3차 작업 목록 — 블라인드 v2 보정 후 남은 7건**:
   **[8/26 전부 반영 — 구현 전 방식 점검 조사(§5 8/26 결정)로 7건을 '일반 원칙/버그 수정'과 '해석 명시 조건부'로 분류한 뒤 진행. 상세는 부록 A 8/26]**
   1. ~~종목×운용사 교집합(H-08)~~ — `constituent_holders` 에 운용사(mgmt) 파라미터 + 0건이면 "확인되지 않음" 명시.
   2. ~~"AA급"(이상/이하 없이)을 등급대(AA+·AA·AA-)로 해석(O-07)~~ — `rating_condition` 의 '급' 단독 처리 + `bond_filter` 에 서열 하한 파라미터 추가(원래 상한만 받아 하한이 목록에 안 걸리던 잠복 결함도 함께 해소).
   3. ~~그룹 낱말 '자회사'(O-05)~~ — 후보 4종 나열 대신 그룹 규칙(6.0)과 같은 회사명 접두 집계(새 템플릿 `constituent_prefix_holders_by_aum`, 순자산 큰 순)로 교체. 검사표도 질문("순자산이 큰")에 맞게 순자산순 상위 10으로 정정.
   4. ~~종목 편입 규칙 우선(O-06)~~ — "X처럼/같은 …을 담은"(빗댐 표현)일 때만 상품명 조각 규칙(6.1)이 종목 역질의(규칙 6)에 양보. 처음엔 '종목+편입 동사'면 전부 양보하게 했다가 재채점에서 O-03("에코프로비엠이 편입된 2차전지 ETF" — 종목+테마 결합은 조각 경로가 정답)이 깨져 빗댐 표지로 좁힘 — 수정 직후 재채점이 잡아준 두 번째 사례.
   5. ~~순자산 금액 조건 건수(O-09)~~ — `etp_count` 에 순자산 구간 파라미터(초과/이상·미만/이하 구분) + "1조 넘는"·"5000억 이상" 해석기.
   6. ~~펀드 목록 기본 정렬(O-08)~~ — 정렬 미지정 시 판매중 상품 먼저(순자산 정렬 요청·판매중 필터가 있으면 기존 그대로).
   7. ~~띄어쓰기 없는 조건어(P-02)~~ — "판매가능한"·"매수가능"·"살수있는" 붙여 쓴 조건 인식(정규식 공백 선택 허용).

   ~~**다음 측정 제안(8/26 조사)**: 새 씨앗 블라인드 v3~~ **[8/26 오후 승인·제작·첫 측정 완료 — 부록 A 8/26(2)]** `evalset/make_evalset_v3.py`(씨앗 20260826) 80문항: 새 씨앗 표본 26 + 표현 변형 25(불변 시험) + 3단 순회형 14 + 새 함정 15. **첫 점수 64/80(80%)** — 새 구멍 16건 발견(함정 5·과잉 거절 1·한계 미명시 2·내용 8). 병행 승인분도 반영: HCX 예비 경로 모범 예시 3개(표적 회귀 4/4 통과) · 계열사 수집기 준비(공정위 API — 사용자 키 발급 대기).

   **⑧ 4차 작업 목록 — 블라인드 v3(64/80)가 드러낸 것**:
   **[8/26 오후 전부 반영 — v3 64 → 80/80(100%) · v2 80/80 · v1 105/105 전부 유지. 상세는 부록 A 8/26(3)]**
   1. ~~함정 방어 4갈래 확장(T-04/06/09/10/12)~~ — 배당수익률·배당금·분배금·공매도 속성 거절 · "다음 주/내일/모레 상장" 미래 거절 · "지금 ○○ 주가/종가" 실시간 거절(종목까지) · 행위 낱말(계좌 개설·해지·이체·송금·리밸런싱).
   2. ~~과잉 거절 수리(M-04)~~ — 브랜드 표기 감지에 영문·숫자 경계 검사(`find_brand_token` — 별칭 'HK'가 '삼익THK' 안에 걸리던 결함).
   3. ~~"AA 등급대" 띄어쓰기(P-09)~~ — 등급 토큰 뒤 공백 허용(lstrip).
   4. ~~자회사 접두 집계에 운용사 열(C-01)~~.
   5. ~~총보수 최저 결합 3종(C-03·C-13·H-04)~~ — 편입×보수·운용사×보수·위험등급×보수(0 표기 제외 정책 노트 포함). 운용사 밋밋한 목록도 SQL 우선으로 바꿔 그래프 별칭이 해외 계열 운용사(Global X) 상품을 앞세우던 오답 차단.
   6. ~~목록 1위 상품의 속성 명시(C-05·C-10)~~ — `top_rank_attribute_notes`(순위 낱말+속성 낱말 → 최상위 행 값 노트) + 운용사 목록에 상장일·위험등급·총보수 열.
   7. ~~채권 표면금리 정렬(C-06)~~ — bond_filter 에 coupon/coupon_asc 정렬.
   8. ~~테마×순위×구성(C-08)~~ — etp_pattern_top_constituents(top_etfs=1) 재사용 + etp_top_aum 테마 필터.
   9. ~~교집합×순자산(C-09)~~ — 새 템플릿 constituent_intersection_top_aum + 규칙 5.85.
   10. ~~전체 재검증~~ — 재채점이 잡은 후속 2건도 당일 정정: v2 T-11 재발("X 주식을 담은"에서 X 가 미등록 한글 토큰이면 규칙이 거절 확정 — HCX 변동 차단) · v3 P-21("X 산업/섹터/분야에 투자하는"도 이름+의미 검색 규칙으로).

   ~~**⑧ 5차 후보 — 실전 미러 세트 첫 측정(31/35, 검사표 정정 후 32/35)이 드러낸 것**~~ **[8/27 ⑨ 데이터 전면 교체에 흡수·전부 반영 — 부록 A 8/27]**:
   1. ~~등급 값 단독 표기(MR-L-10)~~ — 이상/이하/급 없이 등급만 쓰면 정확히 그 등급(min=max)으로 해석 + 해석 노트.
   2. ~~ETF 비정형 한계 노트(MR-M-03)~~ — 구조·전략·운용 동향 질문에 **구조화 필드 기반 '구조·전략 요약'**(유형·복제 방식·기초지수·위험등급·보수·분배·순자산)을 조립하고 "서술형 공시 자료는 제공 데이터에 없음" 한계를 함께 명시(`structure_summary_notes` — ETF·펀드 공통, 8/26 비정형 조사 권고 ⑴ 구현).
   3. ~~계열사×규모(MR-H-06)~~ — 그룹 질의에 순자산·규모+순위 낱말이 있으면 접두 편입 ETF 순자산 정렬(`constituent_prefix_holders_by_aum`)을 첫 조회로.

### 일정이 밀릴 때의 비상 축소 순서 (실행 전제 아님 — 배점 영향 작은 것부터)

1. 벡터(의미) 검색 끄기 — 해당 문항 105개 중 9개뿐, 키워드가 부분 대체
2. 그래프 조회를 단순 형태로 제한
3. 채점기의 자동 대조 축소(태도·근거만 자동)
4. **절대 보존**: 함정 방어 · 유효 JSON 보장 · 쉬운 문항 SQL — 배점 직결

## §5. 결정 기록 (확정된 해석·방침)

- 8/13 승인: 순서 기반 진행 · DuckDB 채택 · 1인 개발 체제(개인 브랜치 → 이상 없으면 main 병합) · 각 단계 완료 시 보고.
- 8/14 해석 확정(사용자): **"AA 이상" = 문자 그대로**(AA- 미포함 — 'AA급/등급대'라고 물을 때만 AA- 포함) · **"위험등급 낮음" = 위험이 낮은 것**(5~6등급, 1=매우 위험·6=매우 안전) · **펀드 개수 = 상품 수**(11,138 — 판매 클래스 95,618과 구분 병기). 전부 정책 파일로 반영, 답변에 채택 해석 명시.
- 거절 원칙(비대칭): "없다는 적극적 증거"가 있을 때만 거절, 애매하면 한계를 밝히고 답한다. 부분 일치는 "혹시 이것?" 안내까지만 — 존재의 근거로 쓰지 않는다.
- 거절문은 AI가 쓰지 않고 정해진 템플릿만(함정 경로에서 AI 발화 0).
- HCX-005 단일 모델(007 안 씀), 임베딩은 CLOVA Embedding v2. 다른 회사 AI는 규정상 실격이라 코드 차원에서 차단.
- 8/18 (주최 공식 PDF 2종 대조 후): **발언과 문서가 다르면 문서를 따른다** — 온톨로지는 8/6 발언("TTL일 필요 없다")이 아니라 문서 기준 **TTL 5개 파일**로 제출하고, 예시 접두어 `fp:`·상위 클래스 `fp:Product` 를 채택(⑦-2). 공식 자료의 예시 저장소 구조 `src/` 는 요구사항이 아니라 예시로 보고 코드 이동은 하지 않는다(README 폴더 안내로 대체). 주최 PDF 원본은 "목적 외 전달·게시 금지" 문구가 있어 저장소에 넣지 않고 내용만 ROADMAP 에 옮긴다. 응답 헤더는 문서대로 `application/json; charset=utf-8`, `question` 은 요청값 그대로 반환.
- 8/18: 답변 형식은 주최 예시의 3단 구성(결론 → 상품별 비교 → 근거·기준일)을 표준으로 삼는다(⑧에서 생성기 지시문 반영).
- 8/19: **SHACL 도입** — 온톨로지 규칙 검사를 코드(`build_kg.py`)와 선언문(`ontology/shapes.ttl` + `kg/validate_shacl.py`) 두 겹으로 둔다. 서버 실행에는 선언문 검사가 필요 없다(오프라인·테스트 전용, rdflib·pyshacl). 인스턴스는 가장 구체적인 클래스 하나로만 타이핑하고 상위 클래스는 온톨로지가 추론한다.
- 8/19: **총보수 0 표기는 '무보수'가 아니라 값 미확정(미수집 추정)으로 다룬다** — KODEX 200(실제 0.15%)도 0 으로 표기돼 있어, 보수 순위·필터에서 0 은 제외하고 그 사실을 답변에 명시한다. 그래프 상세에서도 "0(의미 미확정)"으로 표시.
- 8/19: **국내 채권은 위험등급 질문에 포함**한다(공식 자료 p.5 가 채권 주요정보에 '신용·위험등급'을 명시, 데이터도 1~6 축) — 신용등급(AAA~D)과 위험등급(1~6)은 다른 축이라 질문 단어대로 답한다. 해외 ETF 만 위험등급이 없다. 거절 답변은 규정상 "확인할 수 없음" 명시만으로 정답 처리되지만, 근거 표시가 모든 답변의 필수 규칙이므로 **데이터에서 확인한 사유 한 줄 + 검문 근거 + 기준일**을 붙이는 현재 방식을 유지한다(사유는 데이터 사실만 — AI 사전지식 금지). 팀 운영: 팀원은 자기 브랜치에서 작업하고 papuagigi 로는 검증 후 반영한다(8/19 사용자 결정).
- 8/22 (사용자 결정 — 당일 2단계): **시간 기준 최종 = 내부 목표 15초 유지.** 경위: ① 오전 "정확도가 15초보다 중요할 것"(사용자 추측)에 따라 예산을 문서 권장선 60초로 올림(생성 강등 12건 해소 목적). ② 이후 사용자가 설명회 참석 기억을 확인 — **"15초 이하 응답은 감점 없음, 초과하면 감점(방식 미공개)"** → 같은 날 15초 사다리(계획 6초·생성 8초·임베딩 5초·생성 진입 7초)로 복원. 강등된 규칙 요약도 같은 근거의 정답이라(8/19 실측 105/105·최대 9.24초) 15초 준수가 우선. **60초 실험이 남긴 소득(유지)**: 짧은 타임아웃이 가려 주던 HCX 약점 2종을 실측·수정 — ① 생성기가 상품명에 공백을 끼움('코스닥 150', M-18) → 사후 대조에 띄어쓰기 복원 추가 + 생성 토큰 상한 1024→2048(상한일 뿐, 시간 예산과 무관), ② HCX 라우터가 `constituent_holders` 에 ETF 키·지수명을 넣어 0건(H-17) → 파라미터 의미 검증(수리 콜 유도) + **근거 0개 방지망**(어떤 경로든 근거가 비면 실행한 조회 기록을 validation 근거로 첨부).
- 8/22 (사용자 승인 후 구현 — KG_NEXT 1·2순위): **별칭 통합** — 노드 병합 없이 `skos:altLabel` 이름표(운용사 정식명·지수 통칭·해외 종목 한글명, 218개)를 그래프에 선언하고 회사·종목 검색이 정식명+별칭을 한 색인으로 봐 합집합으로 답한다("미래에셋자산운용" 질의가 "미래에셋" 노드를 못 찾던 결함 해소). **국내 벡터 확장** — 구현 중 실측으로 전제 정정: 국내 `cu_strtegy` 는 서술문이 아니라 분류값 4종(실물복제·합성복제·액티브·C — 해외와 컬럼명만 같음) → 분류값은 코퍼스에서 제외하고 **정식명·약칭·기초지수 합성 문장 1,733건**을 임베딩(인덱스 파일명 `index_corpus.*` 로 개명, 구 저널 이어받아 재임베딩 0). **채권·공모펀드 임베딩은 만들지 않는다** — 채권은 서술 컬럼 0개, 펀드 `*_desc` 7개는 전부 최대 12자 분류값(실측). 임베딩이 이기는 대상은 자유 서술문뿐이고 분류값은 SQL 필터가 결정적으로 처리. 재검토 조건: 주최가 비정형 문서(투자설명서 등)를 추가 제공할 때.
- 8/26 (사용자 지시 조사 → 승인): **"규칙 우선 + LLM 예비 + 검증 관문" 구조 유지 확정.** 사용자 질문 "다른 AI(ChatGPT·Claude)는 이런 문장을 어떻게 인식하나, 입력을 세세하게 거르는 우리 방식이 옳은가"에 대한 조사 결론 — ① ChatGPT·Claude '모델'은 규칙 없이 문장을 통째로 이해하지만, 그 모델로 만든 '서비스'는 전부 규칙·검증을 두른다(Anthropic 공식 설계 가이드가 '라우팅'을 표준 패턴으로 권고, "가장 단순한 구성 먼저"). ② 그 회사들도 세부 규칙 논의를 한다 — 위치가 문서·평가셋·학습일 뿐(OpenAI Model Spec = 해석·거절 세부 규칙 공개 문서). ③ 우리 과제 유형(자연어→상품 DB 조회)은 국제 시험 BIRD 에서 GPT-4 순수 프롬프트 정답률 ~35~55%·사람 93% — 상위권은 전부 사전·검증 단계를 붙인 파이프라인. ④ 15초 규정·HyperCLOVA X 한정(실측 약점 있음)·채점 재현성·상품명 수천 개가 규칙 우선을 정당화. **단 과적합(시험문제 맞춤 규칙) 방지 장치 채택**: 새 규칙은 '처음 보는 비슷한 질문에도 도움 되는 일반 정책'만 넣고, 규칙마다 같은 뜻 다른 표현 2~3개 시험을 함께 넣는다. 애매한 해석(자회사=이름 접두 근사 등)은 답변 문장에 해석을 명시한다.
- 8/26 (출제자 정렬 — 사용자 질문 "자체 문제가 실전에 도움이 되나, 출제자 의도에 맞추려면?"의 조사 승인): ① **대표 지표는 '실전 미러 세트'(35문항, `evalset/make_evalset_mirror.py`)** — 실전과 같은 구성(하10·중10·상10·답변불가5), 문항 전부 공식 예시 8개의 '유형'에서만, 관계 질문엔 주최 채점 문구("기대 개체·**관계** 포함")를 미러한 관계 낱말 검사 추가. 자체 무작위 세트(v2·v3)는 결함 탐지 도구로 계속 쓰되 대표 지표는 미러 점수. ② **비정형 서술(펀드 구조·전략·위험요인) 유형은 현 정책 유지** — 상품 식별 + 마스터 사실 + "서술 자료 미수집" 한계 명시(규정상 확인 불가 명시는 정답 처리). 수집(b안)은 4차·미러 안정 후 시간이 남으면 순자산 상위 상품 한정으로 재검토. ③ **팀원 블라인드 출제 도입** — 안내문 `evalset/BLIND_AUTHORING_GUIDE.md`, S3 교차평가(8/27~29)와 겸함. ④ '훈련'의 의미 확정: 모델 학습이 아니라(HCX 고정) 규칙·평가셋·채점 기준을 출제자 신호(공식 예시·채점 문구·분포·기술 세션 철학)에 정렬하는 것.
- 8/26 (⑧ 3차에서 확정): **"AA급"(이상/이하 없이 단독) = 그 등급대 묶음(AA+·AA·AA-, 서열 2~4)** — 상한만 걸면 AAA 까지 섞여 나오므로 하한을 함께 건다. 8/14 결정("AA 이상"=문자 그대로)은 그대로 유지 — '이상'이 붙은 경우와 '급' 단독은 다른 표현이라 충돌 없음. 채택 해석은 항상 답변에 명시. / **'자회사' 질의는 '계열사·그룹'과 같은 원칙(회사명 접두 근사)** — 법적 관계 데이터가 없으므로 이름이 같게 시작하는 종목을 후보로 집계하고 그 한계를 답변에 명시.
- 8/27 (주최 8/26 공지·문답 반영 — 사용자 승인 "데이터 전면 교체, 제안 순서대로"): ① **데이터 기준 = 8/26 재배포본**(국내 8/22 · 해외 8/23 · 배포 8/24) — 07/11 구본과 그 파생물 전부 폐기. ② **채권 매수가능 = 공지 확정 규칙**(만기 도래 제외 전부 구매가능, BUYABLE_QUANTITY 무효 — §8.4 잠정 규칙 폐기). ③ **"값이 0인 행은 순위·목록에서 제외"를 일반 원칙으로**(주최 문답 원문 — 총보수뿐 아니라 수익률·분배 등 조회 컬럼 전반). ④ **분배(배당) 질의는 조회로 전환**(재배포본에서 분배수익률·연간 분배금·지급횟수·지급월 신설) — 여전히 없는 배당락·분배락 일자와 공매도만 거절, '정확한 지급일자'는 지급월 수준 한계 명시. ⑤ **응답시간 기준 비공개**(주최 공지 — 300초 물리 타임아웃만 확정): 내부 15초 목표는 안전 여유로 유지하되 '무감점 경계'라는 서술은 문서에서 정정. ⑥ **HCX 필수 구간 = 질의 의도 분석 + 답변 생성**(사용자 확인 — 공지 굵은 글씨): 규칙 라우팅+생성 생략 경로도 HCX 를 거치도록 의도 확인 호출(전 질의, 판정은 규칙·검증 우선)과 최종화 호출(확정 답안 그대로 출력 — 내용 불일치 시 확정 답안 유지) 상시화. ⑦ **펀드 상품 단위 = 금감원 펀드코드(fss_itm_no) 그룹**(23,622 상품 / 23,676 클래스 — 8/14 '펀드 개수=상품 수' 원칙의 새 데이터 적용, 코드 없는 행은 행 자체가 상품). ⑧ **채권 1종목 1행 대표화** — 재배포본의 장내 시장별 중복 행(1,385)은 별도 표(kr_bond_alt)에 보존. ⑨ **펀드 보유 종목**: 주최 명시 없음 + "시간 충분, 성능 최대"(사용자) → 수집 방향 채택, ⑨ 본 작업 뒤 별도 설계. ⑩ 교차질의(상품군 합산) 공식 예시 대응 채널 신설 — 해외 1년 수익률 제외는 주최 문답으로 공식 확정.

---

## 부록 A — 날짜별 작업 일지 (구 memory.md에서 이관)

> AI 작업 세션의 연속성을 위한 기록 겸 상세 이력. **8/14 이전 항목은 당시 표기 그대로 보존**했으므로 어려운 용어가 섞여 있다 — [GLOSSARY.md](GLOSSARY.md) 참조. 새 항목은 쉬운 말 원칙으로 쓴다.
>
> 새 항목 형식: `### YYYY-MM-DD — 제목` + 목표/변경/검증/결정/blocker/다음.

### 2026-08-03 — 저장소 초기 조사 및 문서화

- `README.md`, Excel schema/datarows 8개, `manifest/2.manifest`를 조사했다.
- 네 원본 데이터셋의 행/컬럼 수, 선언 타입, 후보 키 유일성, 주요 결측과 공백 문제를 확인했다.
- 공모펀드의 안전한 행 키가 `(itm_no, prfd_attr_cd)`임을 전체 95,619행에서 검증했다.
- Lance manifest의 스키마와 누락 fragment를 확인했다.
- `PROJECT_GUIDE.md`와 `memory.md`를 생성했다.

### 2026-08-03 — 정규화·답변 불가 중심으로 설계 개편

- 사용자가 제공한 상세 데이터 분석을 원본 Excel 전체 행과 대조했다.
- 공모펀드 그룹 내 변동 컬럼, 위험등급, 결측 coverage, 만기채권, ETF/ETN 혼재, 해외 ETF 전략 고유값을 재검증했다.
- 공모펀드 Excel 84,563행의 컬럼 밀림과 도메인 위반을 확인했다.
- PostgreSQL + Text-to-SQL을 주 경로로, 해외 ETF 전략 pgvector를 보조 경로로 정했다. (→ 8/7 아키텍처 리빌딩으로 대체됨)
- 답변 가능성 상태, coverage, 구조화 근거, 5초/LLM 2회 API 목표를 문서화했다.
- 2026-09-06 마감에 맞춘 5주 로드맵으로 `PROJECT_GUIDE.md`를 개편했다.

### 2026-08-04 — Tier1 해석 메타데이터 수집·검증 및 전처리 1차 파이프라인

- 목표: 컬럼/값 사전 완성과 원본 함정을 제거한 스테이징 CSV 확보
- 변경: `external_data/dictionaries/` 사전 9종(웹 리서치 428건, 적대 검증 정정 13건 반영), `preprocessing/preprocess.py`와 `preprocessing/processed/`(CSV 4종·quarantine·규칙 리포트 56건), 방법 문서·프로파일링 추가
- 검증: 행수·키 assertion 통과(42,394 / 1,734 / 5,646 / 95,618+격리 1). 별칭 110건·신용등급 45건·채권약어 40건은 독립 에이전트 반박 검증 수행
- 결정: 해외 ETF 기초지수 센티널 2종 → null 정규화, `or_attr_desc='06'` 보존, 상품군 공통 위험등급(1~6) 파생, 신용등급 서열화(AAA=1~D=20)
- blocker: `NDY_*` 공식 정의, `CRD_GRD` 대표값 규칙, 금액 단위 미확인 — 8/6 설명회 질문 리스트로 정리

### 2026-08-05 — 브랜치 분리 정리 (main 상속 파일 제거)

- 목표: `papuagigi`를 main·타 브랜치와 분리해 개인 산출물만 담는 브랜치로 만든다
- 변경: `PROJECT_GUIDE.md`·`manifest/` 삭제, `datasets/` 추적 해제, `.gitignore` 신규, `MIRAE_DATASETS` 환경변수 지원, `README.md` 개편
- 검증: 스크립트 재실행으로 동일 산출물 확인, `git ls-files` 확인
- 결정: 원본 xlsx는 참가자 전원 보유 → 저장소에 담지 않는다. 삭제 파일은 `git show main:<경로>`로 조회 가능

### 2026-08-05 — dev-kyung 브랜치 교차검증 및 반영

- 목표: 팀원 브랜치 산출물을 정독해 우위 항목을 가려내고 반영
- 변경: `ROADMAP.md` v2.2(교차검증 장 신설), `PREPROCESSING_METHOD.md` 6장(R8 정정 + 신규 규칙 R24~R30)
- 검증: 주요 정정 — 잔존만기 저장값은 행별 갱신일 기준(중앙값 137일 지연), 국내ETF 총보수 양수 67건뿐, 유효 상품 11,138, `(itm_no, prfd_attr_cd)` 유일
- 결정: 답변 가능성 상태 8종·time_policy 이식·구조화 근거·HCX 단일 강제·결정성 검증 등 채택
- 다음: 전처리 정정 구현, S0 공통 계약 초안

### 2026-08-05 — 3인 역할 분담 개편 (수평 분할 폐기)

- 각자 브랜치에서 완결 시스템을 만들고(수직 분할) 교차평가로 통합하는 방식으로 ROADMAP 5장 전면 개편. 통합은 노드 단위 채택표 기반, 통합본이 개별 브랜치 최고 정답률 이상일 때만 확정.

### 2026-08-06~07 — Sprint 0/1 잔여 구현 (부분 완료)

- HCX 클라이언트(단일 provider 강제·감사 로그)와 FC Hello World(--dry-run), time_policy 이식, 전처리 정정 일부. API 키 미발급이 blocker.

### 2026-08-07 — 8/6 설명회 녹음본 반영: ROADMAP v3.0 + 아키텍처 리빌딩 확정

- 녹음 전사본(36,809자) 전문 정독 → 규칙 확정 13건 반영. **아키텍처를 Ontology + 지식그래프 + 4채널 연합 검색으로 확정**(제출 요건 + 팀 지시). 위험등급은 발언(1~5)보다 데이터 실측(1~6) 우선.

### 2026-08-07 — Sprint 0/1 잔여 구현 완결 + 2단 검증

- 전처리 정정(R8+R24~R31) 완결 — 독립 재실행 검증 + 적대적 리뷰 + 수정 라운드. pytest 88개 통과. 채권 만기 4상태(16,180/7/25,888/319), ETF 1,201/ETN 532 확정. 결정성 간헐 이슈 1회 관측(이후 재현 실패) — 증거 보존형 검증기로 재작성, 단정 보류.

### 2026-08-07 — 자료 배포 계획 확인: 기술 세션 PDF 없음

- 기술 세션(Ontology/KG)은 음성 녹음이 유일 기록 — 녹음본 기반 기록이 최종, 전사 오류 의심 항목은 데이터 실측·Q&A 우선.

### 2026-08-11 — 설명회 녹음 전사본 교정 + ROADMAP 핵심 위주 재구성

- STT 오전사 교정(HCX·타이거·감점·질의·30문항·OWL 등 40여 패턴, 원본 보존) · ROADMAP 731→190줄 재구성(전문은 ROADMAP_ARCHIVE.md 보존).

### 2026-08-11 — 온톨로지 TTL 초안 + KG 추출 파이프라인 골격

- `ontology/finance.ttl`(클래스·관계 6종·제약·CQ 주석) + `kg/`(추출→N-Triples, 경량 스토어, 질의 CLI). 상품 노드 60,911·트리플 1,026,100·제약 위반 0, "TIGER 200 운용사" 그래프 질의 통과. 국내ETF 운용사 컬럼 55행 오염 발견(→8/13 복구).

### 2026-08-12 — 서버 구축 가이드 + ETF 구성종목 소스 실사·수집기 골격

- `infra/NCP_SERVER_SETUP.md`(따라 하기 절차) · 소스 실사(KRX가 유일하나 로그인제, SEIBro·공공API 부적합 실증) · 쿠키 기반 수집기(기준일 게이트·재개·자동 프로브).

### 2026-08-13 — 공식 세부 규정 3종 반영 + 평가셋 시드 확보

- 참가자 필기 3종 입수 → 타임아웃 300초·HTTP 확정·재기동 허용·제안서 목차 6항·공식 예시 7+트랩 3 반영. `evalset/SEED_QUESTIONS.md` 신설.

### 2026-08-13 — 외부 blocker 2건 동시 해소: KRX 수집 실행 + CLOVA 실 호출 검증

- KRX 프로브 성공(TIGER 200: 삼성전자 33.03%·SK하이닉스 30.02%) · HCX-005 FC 왕복 실 호출 성공 · HCX-007은 서비스 앱 미대상 실측(안 쓰기로). "공식 예시는 유형 예시"(사용자 정정) 전 문서 반영.

### 2026-08-13 — KG 구성종목 연결 완료 + 세션 만료 사건 해소

- **전량 1,139/1,139 ETF 수집 완료(실패 0)** → 그래프 연결: "삼성전자 편입" 229종·"캠브리콘 편입" 16종·"에코프로비엠" 66종 실증. 사건: HTTP 400 = 세션 만료(쿠키 재발급으로 즉시 해소, IP 차단 아님).

### 2026-08-13 — 벡터 채널 골격 + 임베딩 서비스 앱 blocker 발견 → 해소·전체 인덱싱 + 평가셋 105문항

- Embedding v2 클라이언트+인덱스 빌더+검색 스토어 구현 → 서비스 앱 승인(사용자) 후 **5,567건 전체 인덱싱**(지연 p50 0.15초). 한↔영 교차 검색 성공, "반도체 집중"→로보틱스 1위 한계 실측(키워드 결합 필요 근거). **평가셋 105문항 초안**(실측 grounding·행동 3분법·함정 15).

### 2026-08-13 — 사람용 진행 보고서 + S2 계획(3안 설계·2인 심사)

- `PROGRESS_20260813.md`(→ 본 문서 §2.1로 통합) · `S2_PLAN.md` 제안(→ 본 문서로 개칭). AI 설계안 3개를 별도 심사 AI 2개가 채점(86/77/89) — 최고안 기반으로 나머지 강점 이식, 공통 누락 8건 보완.

### 2026-08-13 — S2 승인·착수: DuckDB 적재 + KG 메모리 실측 (순서 ① 전반)

- 사용자 질문 5건 답변 후 착수. DuckDB 6테이블 전량 일치 적재(31MB), **AUM 상위 5 = KODEX 200 1위**(주최 채점 예시 검증), KG 인메모리 실측 620MB/20.6초(4GB 통과). 의존성 추가: duckdb.

### 2026-08-13 — S2 순서 ① 완결: 오염 복구·한글 별칭·Evidence 계약 + 현금 센티널 버그 정정

- 운용사 오염 64행 100% 복구(접두 매핑 15조합) · 한글 별칭 66행(실측 ISIN만) · 근거(Evidence) 규격 확정 · **현금 센티널 버그 발견·정정**(CASH·USDZZ·JPYZZ가 종목으로 취급돼 가짜 관계 685개 — 이중 차단 + 회귀 테스트). 156개 테스트 통과.

### 2026-08-13 — S2 순서 ② 완결: 엔티티 인덱스 + SQL 템플릿 25종 + 키워드 채널

- 통합 이름 사전(정확/부분 일치 2의미론 분리 = 함정 방어 정책의 코드화) · SQL 템플릿 25종(모의고사에서 역산, AI는 번호+값만) · 키워드 채널. 169개 테스트 통과. 교훈: duckdb 결과는 반드시 .fetchall().

### 2026-08-14 — S2 순서 ③ 완결: Router 2단 + 4채널 실행기 + M1 로컬 E2E 관통

- 규칙 라우터(함정 12유형 전부 AI 0콜 확정) + HCX-005 FC 라우터(템플릿 enum 강제·수리 1콜·폴백) + 4채널 실행기 + 의미·키워드 순위결합(RRF). 라이브 3건: 규칙 2.3초 / **반도체 1위 정정** / FC grounding 정확(7.5초). 199개 테스트 통과. 발견·정정: 매수가능 문맥에 만기 경과 채권이 나오던 문제(활성 필터 자동 결합), 티커 우연 매칭('KODEX AI'의 xa).

### 2026-08-14 — 미확정 해석 3건 사용자 확정 (정책 룩업 첫 실전 교체)

- 'AA 이상'=문자 그대로(서열≤3) · '위험 낮음'=5~6등급 · 펀드 개수=상품 수. 코드 로직 변경 없이 정책 파일+문구 교체로 반영 — "미확정 정책 룩업화" 설계의 첫 실전 검증. 평가셋 L-01·H-26 답안도 정정.

### 2026-08-14 (2) — 문서 체계화 + 순서 ④(답변 검증)·⑤(답변 생성기) 완료

- 문서: GLOSSARY.md 신설 · README 전면 재작성 · S2_PLAN 쉬운 말 재작성 · 쉬운 말 원칙 명문화(사용자 지시).
- ④ 5중 검문소(값·존재·시점·항목·충분성) — 라우터와 독립 재검사(이중 방어), 거절이 정답인 16문항 상시 자동 방어. **테스트가 실제 구멍 2개 발견**: 'kimi' ⊂ 'Denmark IMI' 공백 제거 우연 겹침 → 원문 표기 재대조로 교정 / '챗GPT'의 GPT가 실제 티커와 일치 → 한글 붙은 영문은 한 단어 취급.
- ⑤ 생성기 — 검증 통과 근거만 HCX-005 주입, 사후 대조로 근거 밖 상품명·숫자 줄 삭제(반올림만 허용), 실패 시 규칙 요약 폴백, 거절 경로는 생성기 미호출. `answer_service.py`로 개편(5필드 직렬화 분리).
- 검증: 테스트 240개 통과 · 실 호출 L-01 생성 6.2초.
- 다음: ⑥ API 서버 → ⑦ 자동 채점기.

### 2026-08-14 (3) — 문서 통합(PLAN.md 일원화) + 순서 ⑥ API 서버 완료

- **문서 통합(사용자 지시)**: S2_PLAN.md → **PLAN.md** 로 개칭·재편(과거·현재·미래 계획 문서). PROGRESS_20260813.md 는 §2.1(과거)로, memory.md 작업 일지는 부록 A로 통합 후 두 파일 폐지. memory.md 의 참조 정보(실측 사실·설계 결정·열린 결정)는 ROADMAP.md 말미 부록으로 이관. 문서는 5개로 정리: README(입구)·GLOSSARY(용어집)·PLAN(계획·일지)·ROADMAP(원장)·ROADMAP_ARCHIVE(보관).
- **⑥ API 서버**: `server/app.py` — 공식 규격 `GET /answer`(5필드 전부 문자열), `/health`(상태 확인), `/`(브라우저 질문 시험대 — 답변·근거·처리 과정 표시). 시작 시 저장소 일괄 적재, 요청별 시간 예산(`engine/deadline.py` — 의미 검색 6초·문장 생성 8초 초과 시 자동 강등), 같은 질문 캐시(메모리+디스크, **실패·강등 응답은 캐시 금지**), 전역 예외 처리(어떤 오류에도 HTTP 200 + 유효 JSON). 의존성 추가: fastapi·uvicorn.
- 검증: **테스트 248개 통과(+8)** — 5필드 규격·함정 경유·빈 질문·내부 오류 방어·캐시 규칙(강등 미캐시)·상태 확인. **실 가동**: 그래프(11.8만 트리플)+벡터+HCX 구성으로 기동 후 HTTP 실측 — "삼성전자 포함 ETF" 6.28초, 브라우저 시험대에서 "위험등급 낮은 ETF" 9.7초(해석 기준 명시 포함).
- 다음: ⑦ 자동 채점기(모의고사 105문항 자동 실행·채점 — 함정 15로 채점기 선검증).

### 2026-08-17 — 작업 정리본 작성 + GitHub papuagigi 푸시

- `WORK_SUMMARY_20260806-0814.md` 신설 — 직전 커밋(8/5) 이후 이번 푸시까지의 전 작업을 한 문서로 정리(푸시 동반 기록). `requirements.txt` 신설.
- 푸시 전 점검: 스테이징 대상 79개 파일에서 비밀값(쿠키·API 키) 0건, 대용량 산출물(DB·그래프·벡터)은 .gitignore 로 제외 확인. 수집 구성종목 CSV(11.4MB)는 데이터 자산으로 포함.
- 커밋 후 origin/papuagigi 로 푸시 (원격: github.com/ai-festival-mirae-asset/mirae-asset-dev).

### 2026-08-18 — 주최 공식 PDF 2종 반영: 문서 갱신 + API 명세서 + 서버 규격 보강

- **입수**: `AI페스티벌_과제설명_금융상품Agent_참여자공유용.pdf`(14쪽 — 일정·과제·데이터·평가·제출·API 규격) · `AI페스티벌_기술세션_금융상품Agent_참여자공유용.pdf`(25쪽 — 온톨로지·지식그래프·연합 검색 강의). PyMuPDF 로 본문을 뽑아 기존 기록(8/6 녹음·8/13 필기)과 전수 대조. 두 PDF 는 "목적 외 전달·게시 금지" 문구 때문에 저장소에 넣지 않고 상위 폴더에 둔다.
- **새로 확정된 것**(ROADMAP §0 헤더 요약): 온톨로지 = TTL 5개 파일(common/bond_kr/etf_kr/etf_gl/fund_pub) · 응답 60초 이내 권장 · 서버 09.07~09.20 상시 활성 · 미정의 파라미터에도 500 금지 · 응답 헤더 charset=utf-8 · 제출물 3종(소스+README+온톨로지 / 제안서 / End-point+API 명세서) · 외부 수집 공식 허용 + "기준데이터만 물어보지 않음" · 필수 구성요소 4종 · 답변 형식 3단 · 본선 10/1~10/16, 결선 10월(6팀) · 기술 세션 실험 수치(SQL 실행 성공 ~100% vs 정답 45%, 단일 검색기 천장 ~66%, 온톨로지 런타임 사용 +20pp 등).
- **문서**: ROADMAP(§0·§1·§2·§3·§4.1·§4.5 신설·§5·§6·§7·§8·§10) · PLAN(§1 제출물 3종, §2.2 ⑦-2·⑨ 신설, §4 순서 조정 제안, §5 결정) · README(현황·규격·문서 지도) · GLOSSARY(신규 용어) · **`API_SPEC.md` 신설**(제출 필수물 — 요청/응답 스키마·실제 출력 예시·규격 대조표).
- **코드**: `server/app.py` — 응답 헤더 `application/json; charset=utf-8` 명시, `question` 요청값 그대로 반환. `tests/test_server.py` — 미정의 파라미터·`question_id` 누락에도 200+5필드 테스트. **실측 중 발견·수정 2건**: ① 거절문의 "혹시 다음 상품…" 안내에 우연 겹침(kimi ⊂ MSCI Denmark IMI)이 되살아나던 문제 → 질의어가 원문 표기 그대로 보이는 '상품'만 안내(`engine/answer_service.py`) ② 값 도메인 함정(AAAA)의 사유가 두 줄로 중복 → 라우터·검문소 문구 통일(`engine/router.py`). 회귀 테스트 추가(`tests/test_validation.py`). **테스트 267개 통과**(+2, 다른 세션의 `.env` 로더 작업분 포함).
- **작업 트리 참고**: 이 세션 시작 시 다른 세션이 만든 미커밋 변경(`config/env_loader.py`·`.env.example`·README `.env` 절·테스트 등)이 있었고 그대로 보존한 채 위 편집을 얹었다. 커밋·푸시는 사용자 지시 시.
- 다음: ⑦ 자동 채점기(순서 조정안 §4 승인 후 ⑦-2 온톨로지 분할).

### 2026-08-18~19 — 순서 ⑦ 자동 채점기 완료: 첫 정량 성적표

- **만든 것**: `evalset/eval_runner.py`(채점기 — 서버와 같은 부품을 직접 부르는 in-process 모드 + 켜진 서버에 HTTP 로 부르는 모드, 4축 채점, 성적표 md·상세 jsonl, `--baseline` 회귀 비교) · `evalset/checks_v1.jsonl`(105문항 전부의 내용 검사표 — 기대값은 DuckDB SQL 로 그때그때 계산, 검사 종류 9가지) · `tests/test_eval_runner.py`(8개: 가짜 답변으로 판정 규칙 잠금 — 거절문 뒤에 목록을 숨긴 답변도 함정 오답으로 잡음 / 실제 거절 정답 16문항이 전부 '거절 정답'으로 읽힘 / 검사표 SQL 전부 실행 가능).
- **채점기가 찾아낸 우리 결함(즉시 수정)**: ① 공모펀드 `sale_yn` 실제 값이 'Y'가 아니라 '판매중'이라 판매중 필터가 항상 0건(L-22) → 템플릿 2곳 수정 ② 신용등급 비교 답변에 기준일 줄 누락(L-08) ③ 생성기가 "데이터 기준일: 현재"(날짜 없음) 또는 날짜만(라벨 없음) 쓰는 경우 정식 기준일 줄이 안 붙던 문제(M-08·M-30·L-21) → 라벨+날짜 둘 다 확인 ④ 거절문의 "혹시 다음 상품…" 안내에 우연 겹침(kimi ⊂ Denmark IMI)이 되살아나던 문제(8/18 항목).
- **성적표(실전 구성: 그래프 전체 + 벡터 + HCX-005, 8/19 10:45)**: 종합 통과 **80/105(76%)** — 하 26/30(87%) · 중 25/30(83%) · 상 14/30(47%) · 함정 15/15(100%). **함정 오답 0/16**(거절이 정답인 16문항 전부 거절), 과잉 거절 3(L-20·M-30·H-12), 한계 미명시 5. 응답 시간 중앙값 5.35초 · 95% 지점 10.99초 · 최대 15.83초(15초 초과 1건 H-27, 60초 초과 0). HCX 없이 규칙·그래프·벡터만으로는 79/105(응답 0.04초). 실패 25문항의 원인 분류는 §4 "⑧ 작업 목록" — 가장 큰 것은 **사후 대조가 맞는 줄까지 지우는 문제**(단위 환산·띄어쓰기·계산값)와 **HCX 라우터 파라미터 오류로 "없다"고 답하는 위험**.
- 주의: 8/18 밤 실행은 컴퓨터가 절전에 들어가 시간 측정이 오염돼(4만 초) 폐기하고 8/19 오전에 다시 돌렸다. 같은 구성이라도 HCX 문장이 매번 달라 3문항 안팎이 흔들린다(⑧-6).
- 다음: 승인된 순서대로 **⑦-2 온톨로지 5파일 분할**(→ 8/19 완료, 아래 항목) → ⑧ 다듬기(위 목록) + 배포.

### 2026-08-19 — ⑦-2 온톨로지 5파일(공식 형식) + SHACL 데이터 규칙

- 목표: 공식 자료가 지정한 `ontology/` 5파일 형식으로 온톨로지를 나누고(접두어 `fp:`, 공통 상위 `fp:Product`), 그래프 생성 코드와 어긋남 없이 동기.
- 변경: `ontology/common.ttl·bond_kr.ttl·etf_kr.ttl·etf_gl.ttl·fund_pub.ttl` 신설(finance.ttl v0.2 어휘 계승 — 클래스 15·관계 8·속성 37; 국내/해외 ETF·ETN 4클래스 신설; 해외ETP 위험등급 최대 0 제한) + `ontology/shapes.ttl`(SHACL 규칙 8종) + `kg/validate_shacl.py`(pyshacl 재검사) · `kg/build_kg.py`·`kg/kg_store.py`·`kg/query_kg.py`·`engine/channels.py` 어휘 상수 `FP/FPR`(옛 `MF/MFR` 별칭 유지), 잎 클래스만 타이핑, 옛 어휘 파일 적재 시 즉시 오류 · 펀드 `sale_yn` '판매중'/'판매완료' → 판매중 여부 적재(그전엔 조용히 0건) · `finance.ttl` 삭제(이력은 git) · `requirements.txt` rdflib·pyshacl 추가(서버 실행엔 불필요) · `tests/test_ontology.py` 12개 + `tests/test_kg.py` +3.
- 검증: 테스트 310개 통과(+15) · 그래프 전량 재생성 트리플 1,128,224(제약 위반 0) · SHACL 위반 0(국내ETF 전량 3.5초 + 나머지 표본 12.7초) · 채점기(HCX 없이) 87/105, 새로 실패 0.
- 결정: SHACL 도입(두 겹 검사) · 국내 채권은 위험등급 질문에 포함 · 거절문에 데이터 사유+근거 유지 · 팀원 브랜치 분리 운영(§5).
- 팀원 안내: 그래프 파일 재생성 필요(`python kg/build_kg.py`), `pip install -r requirements.txt` 재실행(rdflib·pyshacl).
- 다음: ⑧ 다듬기(§4 작업 목록 1~8) + NCP 배포(M2). (→ 아래 8/19 ⑧ 항목)

### 2026-08-19 — ⑧ 다듬기 1차 + 배포 묶음

- 목표: 첫 성적표(80/105)가 드러낸 실패 유형(PLAN §4 작업 목록 1~8)을 전부 손보고, 서버 배포를 스크립트 한 방으로 만든다.
- 변경(규칙 라우터 `engine/router.py`): 별칭의 복수 상장을 한 개체로(구글·알리바바) · 순자산 정렬은 SQL 에서(M-02) · 그룹/계열사(6.0) · 상품명 조각 우선(6.1, `resolve_product_candidates`) · 펀드 부분 명칭→마스터 상세(8) · 운용사×테마×구성(8.6) · 코스닥 비중(10.5) · 리츠 구분(10.6) · 국내/해외 비교의 위험등급 비대칭 노트 · 총보수 0 제외 노트.
- 변경(SQL `engine/sql_templates.py`): 신설 7(`etp_pattern_top_constituents`·`constituent_group_holders`·`fund_detail`·`constituent_ksq_share`·`reit_constituents`·`etp_top_return_common_holdings`(H-09, 현금성 제외)·`etp_target_maturity_within`(H-12, 상품명 'YY-MM' 판독)) · 보정 6(`constituent_holders` order·약칭, `constituent_intersection_low_fee`·`etp_low_fee` 0 제외, `mgmt_top_share` 점유율, `bond_top_maturity` 잔존만기 SQL 계산, 채권 3종 약칭 열, `global_etf_filter` 지역 제외·이름 패턴) · `like_prefix_param`(회사명 접두) · **원화 금액 열에 환산 표기(`pd_net_tamt_krw`='28.4조원') 자동 첨부**(생성기가 346,687,108,988원을 '346조'로 잘못 환산한 M-03 실측 → 근거의 환산 표기를 그대로 옮기게). DuckDB 1.5 의 `row_number()<=N` 최적화가 정렬 키 전부 NULL 이면 행을 버리는 현상 발견 → coalesce 로 회피.
- 변경(답변 `engine/answer_service.py`): 분포 결론·건수 문장(L-05: 'n=306' 을 생성기가 정보 없음으로 오독)·기초지수 결측·구성의 선물/옵션(파생) 명시·HCX 계획 0건 비단정 노트를 데이터에서 만들어 강제(`data_notes`) · `*_pct` 열에 % · 구성 0건은 '구성 공시 없음' 표현 · 펀드 비정형 답변에 마스터 정보 · 지운 줄·이름 정정을 think_trace 에 기록.
- 변경(생성 `engine/generator.py`): 사후 대조 — 조·억·만(십억·백만 등) 환산·순위 표기·같은 상품 표기 변형(데이터 키 대조) 허용 · **이름 한 글자 오기 정정**('퀀타매트릭스'→'퀸타매트릭스' L-06 실측 — 한글·영문 글자 치환만, 숫자·띄어쓰기는 제외) · 3단 형식 지시 · 금액은 근거의 환산 표기 그대로 · 온도 0.2·seed 고정. (`agent/clova_client.py` 에 temperature/topP/seed 옵션.)
- 변경(HCX 라우터 `engine/router_llm.py`): `coerce_llm_params`/`coerce_graph_query`(이름→키, 숫자·날짜·범위 검사) · 온도 0.1·seed 고정. 타임아웃: 계획 6초·생성 8초·생성 진입 7초(`server/app.py`·`engine/deadline.py`).
- 변경(네트워크 `agent/net_guard.py` 신설): HCX·임베딩 호출을 작업 스레드에서 돌리고 timeout+2초 벽시계 상한 — 12:55 실전 성적표 실행 중 HCX 호출 1건이 **368초** 멈춘 뒤 `getaddrinfo failed`(DNS 조회 정지 — httpx timeout 밖)로 끝난 사고 실측(주최 하드 타임아웃 300초 초과). 이제 어떤 멈춤도 예산 안에서 끊고 규칙 폴백.
- 변경(그래프 `kg/kg_store.py`): 상품 검색 정확 일치 우선(tiger200 → TIGER 200) + 이름 캐시. `engine/channels.py`: 총보수 0 → "0(의미 미확정)".
- 배포 묶음 `infra/deploy/`: `install.sh`(설치·갱신 한 방) · `mirae-api.service`(자동 재기동·부팅 시작·메모리 상한) · `healthcheck.sh`(5분 cron, 실패 시 재기동) · `warmup.sh` · `README_DEPLOY.md`(운영 안내서). `.gitattributes`(*.sh LF 고정). `infra/NCP_SERVER_SETUP.md` §7 갱신.
- 평가셋: H-13 gold 정정(국내채권 위험등급 포함 — 8/19 판단) · EVALSET_README 성적표 이력·흔들림 안내.
- 검증: 테스트 333개 통과 · 채점기 HCX 없이 **105/105**(87 → 97 → 105, 새로 실패 0) · 실전 구성 80/105 → 96/105(12:33) → 103/105(12:55) → **105/105(100%)** — 하 30/30 · 중 30/30 · 상 30/30 · 함정 15/15 · 함정 오답 0/16 · 과잉 거절 0 · 한계 미명시 0 · 중앙값 2.84초 · 95% 지점 8.3초 · 최대 9.24초(15초 초과 0건 · 60초 초과 0건 — 생성 시간 초과로 규칙 요약 폴백 12건) · 실행 3회(12:33 96 → 12:55 103 → 13:22 105) 동안 새로 실패 누적 0.
- 결정: 총보수 0 표기 = 값 미확정(순위 제외·명시) · 생성/라우터 온도·seed 고정 · 타임아웃 6/8/7초 + 벽시계 상한 · 금액은 SQL 이 환산 표기를 붙이고 생성기는 옮겨 쓰기만.
- 다음: 사용자가 NCP 서버를 만들면 `install.sh` 로 배포 → 원격 리허설(`--mode http`) → 남은 실패·흔들림 점검 → ⑨ 제출물 3종.

### 2026-08-22 — ⑧ 개선: 시간 규정 확정 · HCX 강건성 · 별칭 통합 · 벡터 국내 확장 · 임베딩 범위 결정

- 목표: 사용자 질문·지시 4건 처리 — ① 시간 기준 ② 임베딩 모델 허용 여부 ③ 지식그래프·온톨로지 발전 연구 → 1·2순위 구현 ④ 채권·펀드 임베딩 필요성 결정.
- 시간(경위): 오전 "정확도가 15초보다 중요할 것"(사용자 추측)에 따라 예산 60초로 상향 → 재채점 103/105 → 사용자가 설명회 기억 확인(**15초 이하 무감점, 초과 시 감점**) → 당일 15초 사다리(계획 6초·생성 8초·임베딩 5초·생성 진입 7초) 복원. 60초 실험이 드러낸 HCX 약점은 수정 유지 — `engine/generator.py` 상품명 띄어쓰기 복원(M-18 '코스닥 150')·토큰 상한 2048, `engine/router_llm.py` `constituent_holders` 파라미터 의미 검증(H-17 — ETF 키·지수명이면 수리 콜), `engine/answer_service.py` 근거 0개 방지망(조회 기록을 validation 근거로).
- 임베딩 규정: 공식 자료 3종 재확인 — LLM 만 HyperCLOVA X 고정(과제소개 p.4 원문), 임베딩 모델 지정 조항 없음 → CLOVA Embedding v2 사용은 가장 엄격한 해석으로도 안전(ROADMAP 규정 표 기록).
- 연구(`kg/KG_NEXT.md` 신설): 내부 빈틈 + 외부 문헌(GraphRAG·개체 연결) 조사, 후보 7개 비교 → 1순위 별칭 통합, 2순위 국내 벡터 확장.
- 변경(1순위 별칭 통합): `ontology/common.ttl` §0 `skos:altLabel` 선언 · `kg/build_kg.py` 별칭 사전 2종 → 이름표 218개(운용사 정식명은 가장 긴 접두 원시 표기에만 — 삼성≠삼성액티브 · 지수 통칭은 정규화 동등만 · 해외 종목 한글명) · `kg/kg_store.py all_names` · `kg/query_kg.py` 회사·종목 대조를 정식명+별칭 합집합으로. 그래프 재생성 트리플 1,128,442 · SHACL 위반 0. 검증: "미래에셋자산운용" 질의 → "미래에셋" 노드 상품 ✅(종전 0건) · "캠브리콘" 그래프 선언만으로 16 ETF ✅.
- 변경(2순위 벡터 국내 확장): **전제 정정** — 국내 `cu_strtegy` 는 서술문이 아니라 분류값 4종(실물복제 745·C 409·액티브 343·합성복제 82)임을 실측 → 분류값 제외, `vector/build_index.py kr_synth_text` 로 정식명·약칭·기초지수 합성 문장 1,733건 임베딩(36분, 64,975 토큰). 인덱스 파일명 `index_corpus.*` 로 개명(구 저널 이어받기 — 해외 5,567건 재임베딩 0) · `.gitignore` 예외를 새 이름으로 교체(누락 발견·정정). `engine/channels.py` anchor 검색 국내 확장 + **시장 필터**(질문이 해외/국내를 명시하면 벡터·anchor 후보 제한 — 재채점에서 M-12 '배당 해외 ETF'에 국내 근거 혼입 발견 → 정정) · `engine/router.py` 테마 규칙이 시장 필터를 싣고 국내 명시 질문에도 벡터 호출 추가.
- 결정(채권·공모펀드 임베딩 안 만듦): 채권 52컬럼에 서술문 0개, 펀드 `*_desc` 7개는 최대 12자 분류값 — 임베딩이 이기는 대상(자유 서술문)이 없고 분류값은 SQL 이 결정적 처리. 재검토 조건: 주최가 비정형 문서 추가 제공 시(`kg/KG_NEXT.md` §4-2).
- 검증: 테스트 341개 통과(+8) · 실전 채점 0121 105 → 0217 104(M-12 발견) → **0228 105/105(100%)** · 함정 오답 0/16 · 15초 초과 0건 · 최대 10.71초 · 중앙값 2.63초.
- 커밋: `d17250e`(시간 규정·강건성·연구) · `25ceba9`(별칭 통합·벡터 확장) · `b5cd366`(인덱스 커밋 대상 교체) — origin/papuagigi.
- 다음: M2(사용자가 NCP 서버 생성) → 원격 리허설 → ⑨ 제출물 3종.

### 2026-08-22 (2) — 블라인드 평가셋 v2 제작 + 첫 측정 (44/80 → 검사표 정정 후 46/80)

- 목표: v1(105문항)이 105/105 로 포화돼 실력 차이를 못 재므로, **우리 규칙을 보지 않고 데이터에서 무작위로 뽑은** 새 시험지로 일반화 성능을 잰다(사용자 지시 "테스트 추가 제작 → 계획 → 실행").
- 변경: `evalset/make_evalset_v2.py`(seed 20260822 고정 생성기 — 문항과 검사표를 같은 SQL 로 만들고 스스로 검증) → `evalset_v2.jsonl`·`checks_v2.jsonl` 80문항(하 24·중 26·상 15·함정 15 — A 표본형 하 15 · B 중 15 · C 상 10 · D 표현 변형 15(v1 5문항×존댓말·띄어쓰기 제거·구어체) · E 함정 신종 15 · F 주최 예시 스타일 10). `tests/test_evalset_v2.py`(구조·검사 SQL 실행 가능) · EVALSET_README §6.
- 결과(첫 실행, 실전 구성): **44/80(55%)** — 검사표 오류 2건(비교 표현) 정정 후 **46/80(57.5%)** · 함정 오답 8/15 · 과잉 거절 7 · 내용 실패 19 · 15초 초과 0건(최대 12.06초). 원인 5갈래: ① 이름 경계 무시(TIGER 코스피300→TIGER 코스피, 애플파이→애플, KB스타→RISE) ② 상품은 잡고 속성 규칙 없음(채권 만기·등급, ETF 상장일, 펀드 위험등급) ③ 운용사 결합 규칙 부재(순자산 1위·건수·테마·상위 ETF 의 운용사) ④ 폴백 거절이 자유 문장(함정 4)+행위 요청 미탐지 ⑤ 띄어쓰기 없는 질문 과잉 거절(4). 상세는 EVALSET_README §6.
- 해석: v1 105/105 와 v2 46/80 의 격차 = "시험지에 맞춘 정도". 함정 8/15 는 실전 감점 직결이라 최우선.
- 다음: ⑧ 2차 다듬기 — §4 "2차 작업 목록" 순서로 수정 → v2 재채점(보정 후 점수는 따로 기록) → 포화되면 v3.

### 2026-08-22 (3) — ⑧ 2차 다듬기: 블라인드 v2 46/80 → 73/80, 함정 오답 8 → 0

- 목표(사용자 승인 순서 1→6): v2 실패 5갈래 수정. 1·4(함정 방어)를 먼저 끝내고 중간 보고 후 2·3·5·6.
- 변경: `pipeline/entity_index.py`(scan 이름 경계 — 원문 위치 맵 + 뒤 글자 검사, 운용사 정식명·해외 한글명 별칭 색인) · `engine/router.py`(행위 요청·배당락일·표면금리>100%·미존재 운용사·해외 상품 위험등급·"N개월 전" 이력 거절, 'BBB++' 등급 기호 무효, 원문이 상품명을 품으면 별칭 치환 우회, 채권·펀드 속성 규칙 7.5, 운용사 건수/순위/테마 규칙 8.5, 상위 N개 ETF 의 운용사, ETF/ETN 건수 비교, 띄어쓰기 없는 라틴 토큰, 채권 목록 기본 만기 미경과, 펀드 지역·순자산 정렬) · `engine/validation.py`(질문 어미 확장, 별칭 우회) · `engine/answer_service.py`(자유 문장 거절 → 정해진 거절문/규칙 요약, 상세 행 요청 항목 노트 `attribute_notes`, 판매 중 건수 라벨) · `engine/sql_templates.py`(`bond_detail`·`etp_by_mgmt`·`mgmt_product_count` 신설, `fund_counts` 판매 중 열, `fund_filter` 지역·정렬) · `tests/test_v2_fixes.py`(+25) · 검사표 정정(비교 표현, 순자산 열 2종 any_of).
- 검증: 테스트 368개 통과 · v1 HCX 없이 105/105 · v1 실전 구성 105/105(새로 실패 0, 최대 10.67초) · v2 실전 구성 46 → **71/80**(검사표 정정 후 73/80) · 함정 오답 8/15 → **0/15** · 과잉 거절 7 → 0 · 15초 초과 0건(최대 13.82초).
- 발견: 경계 규칙 도입 직후 'BBB++'의 'BBB'가 해외 티커로, 펀드명 속 'KB스타'가 별칭 치환으로 깨지는 2차 부작용을 재채점에서 잡아 같은 날 정정 — 수정 직후 블라인드 재채점을 돌리는 절차가 효과적. 중간에 v1 M-09(운용사 건수 답변에 운용사명 누락) 회귀도 잡아 정정.
- 다음: 남은 7건(§4 3차 후보) 승인 여부 → M2 배포 → ⑨.

### 2026-08-26 — 방식 점검 조사 + ⑧ 3차 다듬기: 블라인드 v2 73/80 → **80/80(100%)**

- 목표: ① 사용자 질문("다른 AI 는 이런 문장을 어떻게 인식하나, 입력을 세세하게 거르는 우리 방식이 옳은가")의 조사 → ② 승인 후 3차 후보 7건 수정 → ③ "추가 문제 제작 vs 다른 성능 향상 방법" 조사.
- 조사 결론(§5 8/26 결정 참조): 규칙 우선 + LLM 예비 + 검증 관문 구조는 업계 표준과 일치(Anthropic 라우팅 패턴 권고 · OpenAI Model Spec · BIRD 순수 LLM ~35~55% vs 사람 93%). 위험은 방식이 아니라 과적합 — 방지 장치(일반 정책만 + 표현 변형 시험 동반 + 애매한 해석 답변 명시) 채택.
- 변경(7건): 붙여 쓴 매수 조건 인식(P-02) · "AA급" 단독=등급대 묶음 + `bond_filter` 하한 파라미터(O-07, 잠복 결함 동시 해소) · 자회사=접두 집계 신템플릿 `constituent_prefix_holders_by_aum`(O-05, 검사표도 순자산순 정정) · 빗댐 표현은 종목 역질의 우선(O-06) · 편입 목록 운용사 필터(H-08, 0건이면 "확인되지 않음" 명시) · 건수 규칙 순자산 금액 조건(O-09, 초과/이상 구분) · 펀드 판매중 우선 정렬(O-08).
- **수정 직후 재채점이 잡은 부작용 2건(당일 정정 — 이 절차의 세 번째 적중)**: 6.1 양보 조건이 넓어 O-03 회귀 → 빗댐 표지로 좁힘 + 종목 역질의 테마 상품명 필터(생성기가 무필터 그래프 나열을 골라 쓰던 것도 근거 단일화로 차단). M-12 가 HCX 라우팅 변동으로 흔들림 → 'X 관련/테마' 이름+의미 검색 규칙 신설(사전에 없는 테마어도 동작, HCX 의존 제거).
- 검증: 테스트 377개 통과(표현 변형 일반화 시험 포함) · v1 HCX 없이 105/105(새로 실패 0) · v1 실전 구성 105/105(새로 실패 0, 중앙값 2.76초 · 최대 10.15초) · v2 실전 구성 **80/80(100%)** — 함정 오답 0/15 · 과잉 거절 0 · 15초 초과 0건(중앙값 3.48초 · 95% 8.22초 · 최대 8.29초).
- 결정: v2 도 포화(80/80) — 다음 측정은 새 씨앗 v3 필요(→ "추가 문제 제작 vs 다른 방법" 조사 보고로 이어짐).
- 다음: M2 NCP 배포(서버 생성은 사용자) → ⑨ 제출물.

### 2026-08-26 (2) — 블라인드 v3 제작·첫 측정(64/80) + HCX 모범 예시 + 계열사 수집기 준비

- 목표(사용자 승인 순서): ① v3 구성안 4갈래 제작·측정 ② HCX 예비 경로 모범 예시 ③ 자회사 실데이터(DART/공정위) 수집 준비 ④ md 문서 상시 갱신 점검. 서버 배포(M2)는 팀원 담당으로 후순위 조정(사용자 결정).
- 변경: `evalset/make_evalset_v3.py`(씨앗 20260826 고정·자체 검증) → `evalset_v3.jsonl`·`checks_v3.jsonl` 80문항(새 씨앗 표본 26 · 표현 변형 25 — 유의어 '굴리는/톱3/가입 가능한'·단위 '1조=10000억'·붙여쓰기·어순, CheckList 불변 시험 방식 · 3단 순회형 14 — 주최 기술 세션 예시 유형 첫 문항화 · 새 함정 15) + `tests/test_evalset_v3.py`. `engine/router_llm.py` 시스템 지시문에 모범 예시 3개(순위=정렬+limit 인코딩 · 미래 시점 조회 불가 · 편입 역질의 형식 — 기존 예시 0개). `external_data/collectors/collect_group_affiliates.py`(공정위 소속회사 API 수집기 — 엔드포인트·인증키는 환경변수, XML 해석기는 오프라인 시험으로 잠금) + `tests/test_group_affiliates_collector.py`.
- 검증: v3 첫 실행(실전 구성) **64/80(80%)** — 함정 오답 5/15 · 과잉 거절 1 · 한계 미명시 2 · 15초 초과 0건(최대 8.95초). 실패 16건의 원인 분류 = §4 "⑧ 4차 작업 목록"(승인 대기). HCX 예시는 Stage B 사용 문항 4건(v1 H-17·T-09, v2 T-02·T-11) 표적 회귀 4/4 통과 — 전체 재채점은 4차와 함께 1회로 묶음. 시험 382개 통과.
- 발견: 3단 순회형이 예상대로 최다 실패(14문항 중 8) — 관계→순위→속성을 잇는 마지막 고리(1위 상품의 속성 명시)가 없다. 함정 5건은 전부 '방어 규칙의 낱말 목록 밖'(배당수익률·공매도·다음 주·종목 주가·계좌 개설) — 규칙은 맞고 어휘가 좁았다.
- 결정: '자회사' 실데이터는 DART(기업별 지분 관계) 대신 **공정위 대규모기업집단 소속회사(계열사) 명단**을 1차 소스로 — 한 번의 수집으로 전 그룹 커버, 무료 API. 단 '자회사≠계열사'이므로 답변에는 "공정위 기업집단 소속회사 기준" 명시 예정. **사용자 작업 필요: data.go.kr 가입 → 데이터셋 15091891 활용신청 → 인증키 발급 → 환경변수 설정(수집기 상단 안내 참조).**
- 다음: ⑧ 4차(승인 대기) → 수집기 실행(키 수령 후) → M2(팀원) → ⑨.

### 2026-08-26 (3) — ⑧ 4차 다듬기(v3 64→80/80) + 실전 미러 세트 신설(첫 31/35) + 블라인드 출제 안내

- 목표(사용자 승인): ① 4차 — v3 실패 16건 수정 + 전체 재채점 ② 조사 후속 — 실전 미러 세트·팀원 블라인드 출제·비정형 정책 결정(§5 8/26 출제자 정렬).
- 변경(4차): 함정 방어 어휘 4갈래(배당·공매도/미래 상장/종목 실시간/행위) · 브랜드 경계 검사 `find_brand_token`(M-04 'HK'⊂'삼익THK') · 등급대 띄어쓰기 · 접두 집계 운용사 열 · 총보수 결합 3종(+운용사 목록 SQL 우선 — 그래프 별칭의 Global X 오답 차단) · 목록 1위 속성 노트 `top_rank_attribute_notes` · 채권 금리 정렬 · 테마×순위×구성 · 교집합×순자산(템플릿 신설). **재채점이 후속 2건을 잡아 당일 정정(절차 4번째 적중)**: "X 주식을 담은"의 미등록 한글 토큰 거절 확정(T-11 재발 — HCX 변동 차단) · "X 산업/섹터/분야" 표현 확장(P-21).
- 변경(조사 후속): `make_evalset_mirror.py`(35문항 — 실전 분포 하10·중10·상10·답변불가5, 전 문항 공식 예시 유형, 관계 낱말 검사 포함) + `BLIND_AUTHORING_GUIDE.md`(팀원 블라인드 출제 안내 1쪽) + 시험 3종.
- 검증: 시험 395개 통과 · v1 무-HCX 105/105 · **실전 구성 v1 105/105 · v2 80/80 · v3 80/80(전부 100%, 15초 초과 0 — p95 8.1~8.3초)** · **실전 미러 첫 실행 31/35(89%)** — 함정 오답 0/5 · 최대 8.23초. 미러 실패 4건 중 1건은 검사표 결함(알리바바 복수 상장 — 이름 기준 합산으로 정정, 재채점 통과 → 실질 32/35), 3건은 시스템 공백(§4 5차 후보: 등급 값 단독 해석·ETF 비정형 한계 노트·계열사×규모).
- 발견: 실전 미러가 자체 무작위 세트가 못 찾던 유형(등급 '값' 표기·ETF 서술 질문)을 즉시 찾아냄 — 출제자 정렬 전략의 효과 실증. 해외 종목 복수 상장(원주+ADR)은 검사도 이름 기준으로 묶어야 한다는 교훈.
- 다음: ⑧ 5차(미러 3건, 승인 대기) → 팀원 블라인드 문항 수령 시 측정 → 계열사 수집(사용자 키) → M2(팀원) → ⑨.

### 2026-08-26 (4) — 조사: '비정형 서술' 공백은 수집으로 풀 문제인가 (사용자 질문)

- 질문: 공식 예시 중·상 4개가 건드리는 '구조·전략 동향·위험요인' 서술 — ① 원본 데이터에 있나 ② 출제 자료의 과장인가 ③ 추가 수집해야 하나.
- 실측(전 테이블 문자열 컬럼 길이 스캔): 진짜 서술문은 **해외 ETF `cu_strtegy`(최대 535자·5,566건) 단 하나**(이미 임베딩·검색 중). 국내 채권·국내 ETP·펀드의 40자 이상 컬럼은 전부 상품명·벤치마크 명칭 — **국내 상품 서술문 0 확정**. 주최 과제설명 p.5도 제공물은 "4종 상품 마스터 + 스키마 엑셀"뿐(비정형 문서 미제공).
- 출제자 발언·자료 재검(녹음본 원문 + 기술 세션 PDF): ⓐ (58:00) "금융 상품 관련 데이터는 모든 참가팀이 수집해 활용 가능. 다만 **주최 제공 데이터가 평가 기준 데이터** — 거기 없는 종목은 애초에 검색(출제)하지 않는다. 외부 수집과 상충 시 주최 데이터 우선" ⓑ 기술 세션 p.17이 상-2와 같은 문항을 자회사→편입→운용사 **관계 3단 순회** 예제로 사용(채점 축 = 개체·관계) ⓒ 세션의 온톨로지 예시는 전략을 서술이 아니라 **패시브/액티브 열거값**으로 모델링 ⓓ p.19 "RAG 의 함정 — 문서 조각 검색은 놓친다, 조각이 아니라 의미·관계·검증 중심으로"(문서 수집 RAG 를 오히려 경고) ⓔ 트랩 정의 "주어진 데이터로 확인할 수 없는 질의에 답변을 생성하면 감점"(외부 서술로 단정하는 답변은 감점 경로) ⓕ 단, 기술제안서 권장 목차 §2 첫 질문이 "**어떤 특화 데이터를 어떻게 수집·정제했는가**"(배점 40) — 수집 스토리 자체는 제안서 점수 재료.
- 결론: ② '과장'이 아니라 **의도된 유형이되, 기대 답은 서술 원문 재현이 아니라 데이터로 검증 가능한 개체·관계·값**일 가능성이 높다. 대규모 투자설명서 수집은 (감점 경로·출제 철학 역행·기준 데이터 우선 원칙·마감 대비 비용) 비추천. 권고 3단: ⑴ 구조화 필드 기반 '구조·전략 요약' 답변 조립(이름·필드에서 도출: 액티브/패시브·레버리지/커버드콜·환헤지·모자형/클래스·벤치마크·지역·위험등급) + 한계 명시 유지 — 5차의 'ETF 비정형 노트'와 통합 ⑵ 수집 스토리는 기존 확보분(KRX 구성종목 75,081행·계열사 수집기)으로 제안서 §2 답변 충분 ⑶ (선택·후순위) 순자산 상위 국내 ETF ~100종의 '투자목적' 1~2문장 보조 수집은 5차·배포 뒤 시간이 남을 때만.
- 상태: 사용자 결정 대기(⑴을 5차에 포함할지, ⑶ 착수 여부).

### 2026-08-27 — ⑨ 데이터 전면 교체: 주최 8/26 공지·문답·재배포 데이터 반영 (사용자 승인)

- 계기: 주최가 8/26 카페에 ① 데이터 재배포(07/11 구본은 "정합성·코드 이슈"로 폐기, 새 기준 "26.08.24 기준·영업일 08.22·해외 08.23") ② LLM 공지(의도 분석·답변 생성 = HCX 필수, 이외 자유·유료 LLM 지양) ③ 문답(0값 행 미포함 확정 · 해외 1년 수익률 질의 없음 · 결측 보충은 정량평가 무영향+보고서 기재 · 크롤링 가능 · 추가 예시 없음 · 모호 질의 없음)을 게시. 대조 조사로 어긋남 5곳(배당 거절 규칙 정면 충돌 · 매수가능 기준 뒤집힘 · 데이터 전면 교체 · 교차질의 유형 신설 · 시간 기준 서술) 확인 후 사용자 승인으로 착수.
- 새 데이터 실측: 채권 42,394→21,882행(전 컬럼 소문자화·평가사 등급 삭제·위험등급명/퇴직연금/장내종가 신설 · pd_no 중복 1,078종목=장내 시장별 시세 행 · 위험등급 코드 11~16 체계) / 국내 ETP 1,734→1,780(신규 46종 · 분배 9종/변동성/추적오차·괴리율 채워짐 · ISIN/티커/Refinitiv 참조 신설 — 배당수익률·추적오차는 구본 전량 0이라 거절하던 항목) / 해외 5,646→6,037(컬럼 동일 · 1일 수익률 정상화 · 전략문 6,026) / 펀드 95,619→23,676(1행=1클래스 정리 · fss_itm_no 상품 코드 · 자산구성/클래스/보수 분해/최근 분배율 신설).
- 반영(제안 순서 그대로): ① 전처리 3차 — 새 파일·시트 형식, 채권 대문자 통일, R8 매수가능 재정의(공지), R10 폐지, R11 코드 정정(등급명 교차검증 불일치 0), R15/R19 날짜 형식, R32(fss 센티널)·R33(채권 대표화 20,497+alt 1,385) 신설 ② DuckDB(+kr_bond_alt · fund_master=fss 그룹 23,622)·KG(노드 수 = DB 일치)·벡터(고유 7,723 중 신규 495만 임베딩)·개체 인덱스 재구축 ③ 엔진 — 분배 채널(`etp_by_dividend`+상세 필드+거절은 배당락·분배락·공매도만) · 0값 제외 일반화(수익률 정렬 등) · 기준일 3원화(국내 8/22·해외 8/23·구성종목 7/10) · bond/etp/fund 상세 신설 컬럼 · Refinitiv 기초지수 폴백(1,208행 — 구 기초지수 56행) ④ 교차질의 채널(규칙 5.87 + `constituent_holders_top_return`, 해외 제외=주최 확정·펀드 한계 명시) ⑤ 5차 3건 흡수(등급 값 단독 min=max · 구조·전략 요약 노트 `structure_summary_notes` · 계열사×규모 접두 순자산 정렬) ⑥ HCX 필수 준수 훅 2종(`make_hcx_intent_checker` 전 질의 의도 분석 기록 · `make_hcx_finalizer` 비생성 경로 확정 답안 그대로 출력, 내용 불일치 시 확정 답안 유지 — 서버·채점기 배선).
- 새 데이터가 드러낸 회귀 5건 즉시 수정: 채권 위험등급 전량 NULL(코드 체계 변화) · 운용사×테마가 펀드명 조각에 가로채임(어림 추정 가드) · 'SK' 동명이의(해외 신상품 약칭 — 편입 문맥에서 상품 해석 폐기) · 등급 필터+순자산 1위에서 등급 조건 소실 · 분배 정렬 metric 우선순위.
- 시험지: v2·v3·미러 재생성(기대값 새 데이터 재계산 — 결정적 설계라 스크립트 재실행으로 완료). v3 함정 2건 교체(배당수익률·분배금 지급일 → 정상 질의 전환, 배당락·분배락로 대체). 미러 35→38(부록 A: 교차질의 공식 예시 원문 · 분배 정렬 · 분배 단건). v1(정적)은 낡은 기대값 8건 정정(영구채·외화채가 재배포본에서 소멸 → '없음'이 정답으로 전환된 것 포함). KRX 수집기 게이트 7/11→8/24(공지 상한), 기본 수집일 8/21.
- 검증: 시험 395개 전부 통과 · HCX 포함 최종 재채점 **4종 전부 100%**(v1 105/105 · v2 80/80 · v3 80/80 · 미러 38/38 — 함정 오답 0, 15초 초과 0, p95 8.9~10.0초). 재채점 절차가 후속 3건 적발→당일 수정(절차 5·6·7번째 적중): ① v1 L-06 과잉 거절 — 영구채 소멸로 '없음'이 정답이 됐는데 자유 거절 안전망이 거절문으로 바꿈 → 규칙 라우팅+미등록 이름 없음+SQL 0건이면 '결과 0건' 사실 답변 유지(원래 지키려던 함정 4종은 표적 재채점으로 유지 확인) ② v3 T-12 함정 오답 — 행위 요청 거절이 검문 5종에 짝이 없어 생성 경로로 새던 잠복 결함(그동안 표현 운으로 통과) → 규칙 단계의 결정적 거절 힌트를 검문으로 승격(gate_router_rule_refusal) ③ v1 H-30 내용 실패 — 생성기가 상품명 대신 '두 ETF'라고 쓰는 표현 흔들림 → 비교 질문에서 두 상세 조회 상품명을 '비교 대상: A vs B' 노트로 강제.
- 남은 것: KRX 구성종목 재수집(사용자 쿠키 재발급 필요 — 그전까지 7/10 수집분 유지·기준일 명시) · 펀드 보유 종목 수집기 설계(사용자: 성능 최대 방향) · M2 배포(팀원) · 블라인드 출제 안내문은 새 데이터 기준으로 갱신 완료.
