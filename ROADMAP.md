# 제10회 미래에셋증권 AI Festival — 금융상품 Agent 로드맵 (papuagigi 브랜치)

> **이 문서는**: 대회 규칙·확정 사항·상세 진행 상황을 정확한 수치와 함께 기록하는 **참조용 원장**입니다. 분량이 많고 밀도가 높으므로, **처음 오신 분은 [README.md](README.md)부터**, 모르는 용어는 [GLOSSARY.md](GLOSSARY.md)에서 찾으세요.
> 최종 갱신: 2026-08-14 · **예선 제출 마감: 2026-09-06(일) 23:59** · **기능 동결: 2026-08-30(일)** · 평가: 09-07~09-30
> 이 문서는 **현재 유효한 확정 사항·계획·진행 상황만** 담는다. 과거 의사결정 이력과 상세 근거 전문은 [ROADMAP_ARCHIVE.md](ROADMAP_ARCHIVE.md)(v3.0, 2026-08-07), 과거·현재·미래 계획과 일자별 작업 일지는 [PLAN.md](PLAN.md) 참조(구 memory.md 는 8/14 통합·폐지 — 참조 정보는 이 문서 말미 부록으로 이관).

## 0. 문서 맵 — 무엇이 어디에 있나

| 알고 싶은 것 | 위치 |
|---|---|
| 현재 전략·아키텍처·일정·진행 상황 | **이 문서** |
| 일자별 작업 일지 · 과거/현재/미래 계획 | [PLAN.md](PLAN.md) (확인된 사실·열린 결정은 이 문서 말미 부록) |
| 전처리 규칙(R1~R31)과 산출물 CSV | `preprocessing/PREPROCESSING_METHOD.md` · `preprocessing/processed/` |
| 해석 사전 9종(컬럼·값·별칭·등급·코드·지수) | `external_data/dictionaries/` · `COLLECTION_METHOD.md` |
| 8/6 설명회 발언 전문(전사 교정본, 2026-08-11 교정) | `../미래에셋 ai페스티벌 오프라인 설명회_녹음본.txt` |
| **설명회 공식 세부 규정(참가자 필기 3종, 8/13 입수)** — API 규격·질의 예시·제출 방법 | `../API 호출 관련 참고.txt` · `../난이도 별 질문 예시 및 답변 불가능 질문 예시.txt` · `../평가 및 제출 방법 정리본.txt` |
| 평가셋 시드(공식 질의 예시 분석) | `evalset/SEED_QUESTIONS.md` |
| 과거 의사결정 이력 전문(8/4 리서치 · 8/5 교차검증 · 8/6 확정 기록 원문 · 참고 출처) | [ROADMAP_ARCHIVE.md](ROADMAP_ARCHIVE.md) |
| 공식 규정 원문 | `../금융상품Agent_과제소개.pdf` |
| 팀 공동 실행 가이드 | main 브랜치 `PROJECT_GUIDE.md` (`git show main:PROJECT_GUIDE.md`) |

## 1. 핵심 목표

**금융상품 4종 마스터 데이터(국내채권 · 국내ETF · 해외ETF · 공모펀드, 총 145,393건, 기준일 2026-07-11)를 기반으로 자연어 질의에 근거를 붙여 답하는 금융상품 AI Agent(API 서버)를 만들어 예선을 통과하고 수상한다.**

- 팀: 3인(개발·기획 겸임) · 원칙: **성능 최우선** · 이 문서는 개인 브랜치(papuagigi) 관점
- 승부처 3가지:
  1. **API 답변 정확도(배점 40)** — 기대 엔티티·릴레이션 포함 여부 + 근거 유의성으로 채점. 트랩(존재하지 않는 상품) 방어와 응답 속도(목표 15초)가 당락을 좌우
  2. **기술제안서(배점 40)** — 답변과 동급 배점. 온톨로지 구축 방법·활용 데이터 서술 필수. 마지막 주 몰아 쓰지 말고 매주 누적 작성
  3. **온톨로지 제출(소스코드 20점에 직결)** — 도메인별 온톨로지가 소스코드 필수 제출물(TTL 권장, 형식 자유)

## 2. 평가 구조 — 확정 (8/6 설명회)

| 항목 | 확정 내용 |
|---|---|
| 배점 | 총 100 = **소스코드 20 / 기술제안서 40 / 평가 API 질의 답변 40** |
| 정량 평가 | **30문항(상·중·하 각 10) + 답변 불가 트랩 5문항**(점수 미포함, 답변하면 감점). 전 팀 동일 문항, **순차 호출**(동시 아님) |
| 응답 시간 | **[8/13 공식 확정] 문항당 타임아웃 300초 — 타임아웃·5xx 시 최대 2회 재시도**(설명회 필기 "API 호출 관련 참고"). 응답 소요 시간 자체는 여전히 평가 항목(발화 기준: 60초면 사실상 감점 경계, 주최 실서비스 기준 15초, 전체 평균 대비 상대 가감점 가능) → **내부 목표 15초 유지** |
| 대화 방식 | **멀티턴 없음** — 단일 질문·단일 답변. "조건을 주시면 답변드리겠다"는 역질문형 답변은 인정되나 후속 질문은 오지 않음 |
| 채점 방식 | 문항별 **기대 엔티티·릴레이션이 답변에 전부 포함됐는지 + 근거의 유의성**(기준 가이드 비공개). 표현 차이는 무관 |
| 트랩 예시(공개) | 신용등급 AAAA 채권 / **kimi** 관련 투자상품(표기 확정 — 8/13 필기) / KODEX AI 로봇 ETF — 7/11 기준 미존재 → **"없다/확인 불가"가 정답**, LLM 사전지식으로 답하면 감점 |
| 공식 질의 예시 | **[8/13 확보]** 하: "현재 판매 가능한 원화채권 중 AA이상 종목" · 중: "국민성장펀드의 구조와 투자전략 동향"(비정형 서술) / "**캠브리콘이 편입된 중국 반도체 ETF**"(해외ETF 구성종목!) · 상: "최근 6개월 우주항공 테마 연결 이력 ETF" / "에코프로의 자회사를 편입한 ETF 중 순자산 큰 상품의 위험요인" → `evalset/SEED_QUESTIONS.md`. **주의(8/13 재확인): 예시는 실제 출제 문항이 아니라 "유형·난이도" 예시** — 특정 종목명에 과적합하지 말고 유형 전반(구성종목 전 종목·기업관계·비정형)을 커버할 것 |
| API 계약 | **[8/13 공식 확정]** `GET {end-point}/answer?question_id=&question=` — 경로 `/answer` 고정 · 인증 헤더 없음(접근 제어는 주최 측 발신 IP 허용 방식, IP 대역 추후 공지) · 응답 JSON 5필드 **모두 string 타입**(복수 문서는 문자열 안에서 구분 태그 자유 — 태그는 평가 대상 아님) · 길이 제한 없음(과도하면 초과분 미평가) · **HTTP 기본, HTTPS 선택(self-signed 무방)** · 표준 포트 80/443 · **도메인 불필요 — 공인 IP 제출 가능** · End-point 주소는 README.md에 필수 명시 |
| 기술제안서 권장 목차(확정) | ① 전체 아키텍처·특화기능 ② 제안 방법(어떤 특화 데이터를 어떻게 수집·정제 / 어떤 온톨로지 엔지니어링 / 어떤 검색 방법론을 왜 선택) ③ 시스템 구성도(적재→색인→검색→생성) ④ 주요 기능 흐름도(데이터 수집→온톨로지 설계→그래프 구축→검색) ⑤ **기대효과·확장성 — "5번 항목" = 수상 단계 핵심**(현업 적용 지점·상품군 확장) ⑥ 부록(온톨로지 스키마·평가 재현 절차). 자유 양식·PDF 권장 |
| UI | 예선 평가 대상 아님(본선 라이브 시연에서만 유리) |
| 결선 | 본선 팀 선발(10/1 발표) 후 멘토링 → PT + 라이브 시연(10월 중) |

## 3. 절대 규칙 — 위반 시 실격·감점

| 영역 | 규칙 |
|---|---|
| LLM | **최종 답변 생성 + 메인 에이전트 구성(인텐트 분석 포함)은 HyperCLOVA X 고정** — 타 LLM 사용 시 평가 제외. 전처리·비정형 추출·KG 구축 등 오프라인 경로는 도구 자유(8/6 완화 확정). OpenAI **SDK**를 CLOVA 호환 엔드포인트에 연결하는 것은 가능(모델은 HCX). 임베딩은 CLOVA Embedding v2 유지(보수적 선택) |
| 데이터 기준일 | **2026-07-11 스냅샷이 평가 기준.** 외부 데이터(비정형 포함) 수집 가능하되, 상충 시 주최 제공 데이터 우선 · **7/11 이후 생성 데이터 사용 금지**(9월 실적 보고서 등) · 출처·기준일을 근거에 명시 |
| 답변 원칙 | 근거(참조 데이터) 표시 **필수** · 확인 불가 질의는 "확인할 수 없음" 명시 또는 조건 역질문 **필수** · 데이터에 근거 없는 수익률 전망·단정적 투자 추천 **금지** |
| 제출·운영 | **9/6 23:59 이후 커밋·push·배포 등 '결과물 변경' = 실격.** 단 **[8/13 확정] 불가피한 사유(서버 장애 등)의 재기동은 실격 아님.** 서버 운영 기간은 **9/7~9/20 중 별도 공지 기간(주제별 최대 1주)** — 공지 전까지는 9/7~9/20 전체 가동 가정 · 소스코드 제출물에 **도메인별 Ontology.ttl** 포함(필기 정리본 명시 — TTL로 확정) · 제안서 PDF 권장(녹음본은 Word 동시 제출 언급 — 규정 PDF로 재확인) · **9/30 NCP 크레딧 만료** — 평가 종료 확인 후 리소스 정리 |

## 4. 확정 아키텍처 (8/7) — Ontology + Knowledge Graph + Federated Retrieval

### 4.1 스택

| 계층 | 구성 | 역할 |
|---|---|---|
| **Ontology** (TTL 권장) | 클래스(Product/ETF/ETN/Bond/Fund/ManagementCompany/RiskGrade/Region/AssetClass/Index …) + 관계(운용하다↔운용되다) + 제약(ETF·ETN disjoint, 등급 범위, 단일 운용사) | 용어 표준화 · 저장 전 무결성 검증 · 역관계/상위클래스 추론 · **런타임 통제 계층**(프롬프트 주입) · **소스코드 제출물** |
| **Knowledge Graph** | 온톨로지를 적용해 정형(마스터 4종) + 비정형(외부 수집 문서)에서 추출한 트리플 | 멀티홉 질의 · RDB에 없는 값(ETF 구성 종목 등) · 관계 검색 · 근거 추적 |
| **RDB** | 전처리 정정판 CSV 적재 (DuckDB vs PostgreSQL — 미정, §8) | 정량 필터·정렬·집계 — "가장 빠르고 강력한 필터" 채널. SQL은 사전 작성·검증하고 LLM은 파라미터만 채움(환각 SQL 구조적 차단) |
| **Vector 검색** | CLOVA Embedding v2(1,024차원) + FAISS flat — VectorDB 서버 불필요 | 의미 검색(해외ETF `cu_strtegy` 영문 서술, 비정형 문서). 필요 시 CLOVA Reranker 2단계 |
| **키워드 매칭** | 별칭·정규화 사전("타이거"=TIGER=미래에셋) + LIKE/FTS | 상품명 검색 기본 채널 |
| **Federated Query** | Query Understanding → Ontology Grounding → **Query Router**(채널 배분 + 실행 순서 플랜) → 결과 통합 | 질의별 최적 채널 조합 |
| **Answer Validation** | 비즈니스 규칙(YTD=2026-01-01~07-11, 등급 범위) + **존재 검증**(답하기 전 우리 데이터에 실재하는지) + 7/11 이후 데이터 차단 | **트랩 5문항 방어 — 배점 직결.** 답변 가능성 상태 8종(answerable/partial_coverage/… — ARCHIVE C절) |
| **LLM (HCX)** | 최종 답변 생성 + 인텐트 분석 = HCX 고정 구간. HCX-005 vs HCX-007 역할 분담 미정(§8) | 근거 기반 생성. 질의당 호출 2~3회 제한 + 동일 질의 캐싱 |

### 4.2 질의 처리 플로우

```
질의 → Query Understanding(인텐트) → Ontology Grounding(정확한 엔티티·관계명 전환)
  → Query Router(그래프/벡터/SQL/키워드 채널 배분 + 순서 플랜)
  → 채널 실행 → 통합 + 실데이터 존재·조건 충족 재확인(Answer Validation)
  → 통과한 근거로만 HCX 답변 생성
모든 도구 호출을 구조화 로그로 기록 → 그대로 think_trace.
retrieved_context는 근거 행 배열(출처 테이블·행 키·컬럼·값·필드별 기준일)로 내부 유지, 공식 스키마가 문자열이면 어댑터 직렬화.
```

### 4.3 HyperCLOVA X 제약·기능 요약

| 항목 | 내용 |
|---|---|
| FC 지원 모델 | HCX-007(128K, 하이브리드 추론) · HCX-005(멀티모달, **설명회 권장 — 프롬프트·파인튜닝 유리**) · HCX-DASH-002(32K 경량) — HCX-003, DASH-001은 FC 미지원 구버전 |
| FC 제약 | `POST /v3/chat-completions/{modelName}` · `maxTokens ≥ 1024` 필수 · **튜닝 모델은 FC 미지원**(튜닝 채택 시 도구 호출/답변 생성 모델 분리 필요) |
| Embedding v2 | `POST /v1/api-tools/embedding/v2` · 최대 8,192 토큰 · 1,024차원 |
| 활용 검토 API | Reranker · RAG Reasoning · Segmentation · Router · Summarization |
| 성능 병목 | DB가 아니라 **LLM 호출**(14.5만 건은 어떤 DB든 ms). 호출 2~3회 제한 · 규칙 우선 조건추출 · 캐싱 · 레이트리밋 대응이 진짜 성능 작업 |

### 4.4 기존 자산 재배치 (리빌딩 후에도 전부 유효)

사전 9종·taxonomy 계약 → 온톨로지 어휘의 시드 / 전처리 파이프라인 → RDB·KG 공통 입력 / time_policy → Answer Validation 시간 규칙 / HCX provider 강제 코드 → 통신 계층 / 자체 평가셋 설계 → competency questions.

## 5. 데이터 개요와 핵심 함정

| 테이블 | 상품군 | 건수 | 특징 | 대표 함정 |
|---|---|---|---|---|
| PRBD01N001 | 국내채권 | 42,394 | 한글 컬럼명 전무 — 사전 9종으로 해석 완료 | 날짜가 `double`(캐스팅 필수) · **38%가 이미 만기**(기본 검색 제외) · 수익률 있는 종목 2.1%(881건) · 영구채 센티널 99991231 |
| PREF01N001 | 국내ETF | 1,734 | 한글명 있음, 값이 지저분 | **30.7%가 ETN**(분리 필수) · 거래종료 212건 혼재 · 기초지수 실효 커버리지 0.18% · 총보수 신뢰 가능 양수 67건뿐 · `-100` 수익률은 종가 0에서 나온 가짜 손실 · **운용사 컬럼 55행이 상품명으로 오염**(8/11 KG 구축 중 발견 — §8.4) |
| PREF02N001 | 해외ETF | 5,646 | 총보수·AUM 충실 — 정량 질의 최적 | **위험등급 컬럼 없음**("해외ETF 저위험" = 확인 불가) · 기초지수 센티널 문자열 48% · `cu_strtegy` 영문 서술이 유일한 벡터 검색 대상 |
| PRFD01N001 | 공모펀드 | 95,619 | 컬럼 정보 완비, 최다 건수 | 실상품 11,138개 × 클래스(평균 8.6행) — `(itm_no, prfd_attr_cd)` 2컬럼 키, master/attribute 분리 · **보수 정보 미포함**(= 확인 불가 케이스) |

- **추출일 ≠ 필드 기준일**: 국내ETP 일간 ~06-15, 해외 06-14~16, 채권 표준정보 중앙값 137일 지연 → 근거는 "스냅샷 추출일 + 필드 실제 기준일" 2단 표기, 잔존만기는 요청 시점 재계산(time_policy)
- schema 샘플에만 있는 `axis_*` 컬럼 = 주최 측의 교차 질의용 공통 분류 축 힌트 → **온톨로지 공통 클래스/속성 설계의 입력**
- **외부 비정형 수집이 과제의 핵심**(8/6 확정): ETF **구성 종목** 질의 출제 예고(제공 데이터에 없음) — 7/11 이전 스냅샷 확보가 관건. 시세·수익률 대체 데이터와 7/11 이후 데이터는 금지
- 전체 함정 목록·수치 근거: `preprocessing/PREPROCESSING_METHOD.md` · 이 문서 말미 부록 A

## 6. 일정

### 스프린트 (일요일 종료)

| 스프린트 | 기간 | 중심 작업 | 게이트 |
|---|---|---|---|
| ~~S0~~ | ~8/9 | 킥오프 · 8/6 설명회 · 프로파일링 · FC Hello World | **완료(8/13 HCX-005 실 호출 검증까지 종결)** |
| **S1 (현재)** | 8/10~8/16 | **온톨로지 TTL 초안 + 사전 9종→어휘 승격 · RDB 적재 · ETF 구성 종목 수집 착수 · KG 추출 골격** | SQL 채널·그래프 골격으로 기본 질의 응답 |
| S2 | 8/17~8/23 | Federated Retrieval(채널+Router) · Ontology Grounding · Answer Validation · `/answer` API E2E | **8/23 E2E 관통 — 하드 게이트**(미달 시 기능 축소) |
| S3 | 8/24~8/30 | 평가셋 회귀 · 15초 최적화 · **브랜치 교차평가·통합(8/27~29)** | **8/30 기능 동결** |
| S4 | 8/31~9/6 | **기술제안서(40점) 완성** · 안정성(부하·복구·immutable release) · 제출 3종 | 9/6 제출 |

### 주요 날짜

| 날짜 | 이벤트 |
|---|---|
| 8/8(금) | 3인 총검토 — 저장소·모델 역할·수집 분담 확정 예정(§8 참조, 결과 반영 대기) |
| **8/23(일)** | **E2E 관통 하드 게이트** |
| **8/30(일)** | **기능 동결** — 이후 프롬프트·문서만 |
| **9/6(일) 23:59** | **제출 마감** (이후 변경 = 실격) |
| 9/7~9/30 | 예선 평가(주최 측 API 순차 호출 · 응답 시간 측정 · 서버 상시 가동) |
| **9/30** | NCP 크레딧 만료 — 리소스 정리 |
| 10/1 | 본선 진출 발표(6팀) → 10월 멘토링·결선(PT+라이브 시연) |

## 7. 현재 진행 상황 (기준: 2026-08-11, 일자별 상세는 PLAN.md 부록 A)

한 줄 요약: **데이터 레이어(프로파일링·사전·전처리) 완료 + 온톨로지 초안·KG 추출 골격 완료(8/11, S1 DoD 충족).** 검색 채널(Federated)·에이전트·API 레이어는 미착수. 외부 blocker는 NCP 크레딧/API 키 하나다.

### 7.1 완료 — 검증까지 끝난 자산

| 산출물 | 위치 | 내용과 검증 상태 |
|---|---|---|
| 데이터 프로파일링 (8/4) | `preprocessing/profiling/` | 4종 원본 엑셀의 컬럼별 결측률·고유값·타입 실측(재실행 가능 스크립트 + 컬럼별 CSV). 답변 전략을 좌우하는 수치가 여기서 나옴 — 국내ETF 기초지수 실질결측 96.7% · 총보수 실질결측 87.5% · 수익률 보유 채권 2.1%(881건) 등 |
| 해석 사전 9종 (8/4) | `external_data/dictionaries/` | 207개 전 컬럼의 한글 해석 + 값 사전(위험등급·통화·시장 코드 매핑) + 별칭 사전 110건("타이거"=TIGER=미래에셋) + 신용등급 서열 45건(AAA=1~D=20) + 채권 약어 40건(`DUR`·`COV`·`DIRTY`·`NDY_*`) + 기초지수 명칭 정규화 + KOFIA 펀드클래스 코드. 웹 리서치 428건 기반, 적대적 독립 검증으로 정정 13건 반영. **리빌딩 후 온톨로지 어휘(schema vocabulary)의 시드로 승격** — "의미 계층 유무 = 정확도 +17~23%p"의 그 의미 계층을 이미 보유 |
| 전처리 파이프라인 정정판 (8/7, 2단 검증) | `preprocessing/preprocess.py`(605줄, 규칙 79건) → `processed/` CSV 4종 | 타입 캐스팅(문자열 숫자·`double` 날짜) · 공백→NULL 정규화 · **ETF/ETN 분리**(국내 1,201+532 / 해외 5,587+59) · **채권 만기 4상태**(만기도래 16,180 / 당일 7 / 활성 25,888 / 불명 319 — 영구채 4건은 활성+플래그) · **거래종료 212건 분리**(기본 검색 대상 1,520건) · 가짜 -100% 수익률 무효화(98행 252값 — 종가 0에서 파생된 산출물) · 펀드 불가능 수익률 20값 NULL · 손상 행 격리 2건(공모펀드 1·국내ETP 1) · 비-KR 국제채권 1건 예외 태깅. 검증: **pytest 88개 통과 + 원본 xlsx 전수 대조 오포착 0 + 결정성 3회 해시 동일** |
| time_policy 모듈 (8/7 이식) | `pipeline/time_policy.py` | as_of_date 이원화 — 재현용 스냅샷 통계는 2026-07-11 고정, 잔존만기·만기상태는 **요청 시점 서울 날짜로 재계산**(사용자 명시 날짜 우선). 채권 `REMAINING_DAYS` 저장값의 기준일이 행별로 다르고(중앙값 137일 지연) 그대로 쓰면 틀리는 문제의 해법. dev-kyung 브랜치에서 이식, 단위 테스트 포함 |
| HCX FC Hello World (8/6~7 코드 · **8/13 실 호출 검증 완료**) | `agent/clova_client.py` + `agent/fc_hello.py` | **HCX 단일 provider 코드 강제** — allowlist, 설정 누락 시 시작 실패(타 LLM fallback 금지), 모델 식별자 감사 로그. "타 LLM 사용 0건"을 로그로 증명해 규정 준수를 어필하기 위한 장치. 더미 도구 FC 왕복(`--dry-run`) + mock 테스트 통과. **8/13 HCX-005 실 API 호출 FC 왕복 성공**(`--model` 선택 지원. HCX-007은 서비스 앱 별도 신청 필요 — §7.3) → **S0 DoD 완전 종결** |
| 설명회 기록 (8/7 반영 · 8/11 교정) | `../미래에셋 ai페스티벌 오프라인 설명회_녹음본.txt` | 8/6 설명회 전사본 전문 반영(과제 규칙 13건 + 테크 세션 12항목 확정 — ARCHIVE "8/6 설명회 확정 기록" 장). 기술 세션은 자료 PDF가 없어 이 녹음이 유일 기록. 8/11 오전사 교정 완료(원본은 `_원본(교정전).txt`) |
| **온톨로지 TTL 초안 (8/11)** | `ontology/finance.ttl` | 클래스 계층(FinancialProduct→Bond/ETF/ETN/PublicFund + Organization·Index) · 관계 6종(managedBy↔manages, issuedBy↔issues, tracksIndex, hasBenchmark) · 데이터 프로퍼티 30여 종(공통 축=axis_* 힌트 대응 + 채권·ETP·펀드 전용) · 제약(ETF·ETN disjoint, 상품군 disjoint, 단일 운용사 Functional, 등급 1~6) · **competency question 7종(CQ1~CQ7)+트랩을 주석으로 명세**. 어휘는 사전 9종에서 추출. **소스코드 필수 제출물의 초안 확보** — 8/8 총검토 자료 |
| **KG 추출 파이프라인 골격 (8/11)** | `kg/` — `build_kg.py`·`kg_store.py`·`query_kg.py`·`KG_METHOD.md` | 전처리 CSV 4종 → **저장소 중립 N-Triples**(rdflib/Neo4j/AGE 어디든 적재 가능 — 제품 선정 8/8과 독립). 실측: 상품 노드 60,911(펀드는 itm_no 마스터 11,138) · 트리플 1,026,100 · 제약 위반 0 · 결측은 트리플 미생성("확인할 수 없음"의 근거). **S1 DoD 충족** — CQ1 "TIGER 200의 운용사는?" → ETF·미래에셋·근거(테이블·상품번호·기준일) 포함 응답, CQ2 역관계 질의 성공. 테스트 25개 신규, 전체 113개 통과 |
| **서버 구축 가이드 (8/12)** | `infra/NCP_SERVER_SETUP.md` | VPC→Subnet→Server(2vCPU/4GB·시간제)→공인IP→ACG(22/80/443)→관리자 비밀번호→SSH→systemd 상시 가동까지 따라 하기 절차 + 비용·정리 수칙. NCP 공식 가이드 대조. **실제 구축은 S2(8/17~) 권장, 8/23 E2E 게이트 전 필수** |
| **구성종목 소스 실사 + 수집기 골격 (8/12)** | `external_data/CONSTITUENTS_PLAN.md` + `collectors/collect_krx_etf_constituents.py` | 소스 실사(직접 접속 검증): **KRX가 유일한 전 종목×과거 일자 소스이나 로그인제 전환**(비로그인 API는 LOGOUT) · SEIBro는 FnGuide 현재 시점만이라 부적합 · 공공데이터포털에 구성종목 API 없음. 수집기: 기준일 7/11 게이트(코드 강제)·LOGOUT 감지·재개·bld 자동 프로브·연속 실패 조기 중단(8/13)·통합 CSV+출처 메타 |
| **벡터 채널 완성 (8/13)** | `agent/clova_embedding.py` + `vector/` | Embedding v2 클라이언트(경로 후보 자동 순회·도메인 검증·감사 로그) + 재개형 인덱스 빌더 + numpy flat 코사인 스토어(FAISS 불필요·의존성 0) + 질의 데모(근거 표시). 테스트 9개(전체 137개 통과). **인덱스 완성: 해외ETF 전략 서술 5,567×1024(미임베딩 0 · 총 38.3만 토큰 · 지연 p50 0.15초). 한↔영 교차 의미검색 실증**("미국 소형주"→Small Cap ETF 0.63, "배당 수익 중심"→Dividend Income 0.65). **실측 한계: "반도체 집중" 1위가 로보틱스 ETF(0.56)** — 벡터 단독은 애매 케이스 존재, keyword 채널 결합 필요(federated 설계 근거 확인) |
| **자체 평가셋 105문항 초안 (8/13)** | `evalset/evalset_v1.jsonl` + `EVALSET_README.md` | 하 30·중 30·상 30·트랩 15(실전 3배수 미러). **전 문항 실측 grounding**(확인된 상품·종목·수치만 사용) · gold=질의 스펙(값 아님 — 데이터 갱신에 견딤) · behavior 3분법(answer 79/partial 10/refuse 16 — 트랩 오답변이 최우선 채점축) · 데이터 함정 전부 문항화(보수 결측·해외 등급 부재·펀드 마스터/클래스·ETN 혼재·잔존만기 재계산·오염 55행·우선주·빈 구성공시). 스키마 검증 통과(105행·ID 유일) |
| **KRX 구성종목 수집(전량) + KG `holdsConstituent` 연결 (8/13)** | `external_data/constituents/` + `kg/` + `ontology/finance.ttl` v0.2 | **active ETF 1,139종목 전량 수집 완료**(쿠키 세션, bld=`MDCSTAT05001`, 기준일 2026-07-10, 실패 0 — 구성 75,081행). **KG 확장: 국내 상장 증권(ST·DR·리츠·인프라·상장펀드) + 해외 주식(ISIN 키 — 미국·중국·일본) membership 적재 → ETF 923 · 종목 6,819 · 관계 67,205 · 트리플 90,986**(현금 센티널 CASH·USDZZ·JPYZZ 685관계 버그 발견·정정 후 — KG_METHOD 5장). **CQ6 가동**: "삼성전자 편입" 229종목(우선주 자동 구분) · **"캠브리콘 편입" 16종목(한글 별칭 질의 포함) — 중-2 유형이 국내 데이터로 답변됨을 실증** · "에코프로비엠 편입" 66종목(상-2 부품). 비중 등 수치는 CSV(SQL 채널) 소관 — 그래프는 관계만(채널 분담). 세션 만료(HTTP 400) 사건·재개 프로토콜: `CONSTITUENTS_PLAN.md` 3.1 |
| **구현 순서 ⑥ API 서버 (8/14)** | `server/app.py` + `engine/deadline.py` + `tests/test_server.py` | 공식 규격 `GET /answer?question_id=&question=`(5필드 전부 문자열) + `/health` + `/`(브라우저 질문 시험대 — 답변·근거·처리 과정 표시). 시작 시 저장소 일괄 적재(질문마다 재적재 없음), 요청별 **시간 예산 강등**(의미 검색 6초·HCX 생성 8초 초과 시 생략하고 가벼운 방법으로), 같은 질문 캐시(메모리+디스크 jsonl, **실패·강등 응답 캐시 금지** — 재시도 오염 방지), 전역 예외 처리(어떤 오류에도 HTTP 200 + 유효 5필드). HCX 클라이언트 타임아웃 단축(계획 8초·생성 10초). 실 가동 검증: 그래프 11.8만 트리플+벡터+HCX 구성, HTTP 실측 6.28초("삼성전자 포함 ETF")·브라우저 시험대 9.7초("위험 낮은 ETF" — 5~6등급 해석 명시). **테스트 248개 통과(+8)**. 의존성 추가: fastapi·uvicorn |
| **구현 순서 ④ 답변 검증 + ⑤ 답변 생성기 (8/14)** | `engine/`(validation·generator·answer_service) + `tests/`(test_validation·test_generator) | **④ 답변 검증**: 답이 나가기 직전 5중 검문소(값·존재·시점·항목·충분성) — AI 없이 순수 코드로, 라우터와 독립으로 질문 원문을 재검사(이중 방어). 거절은 "없다는 적극적 증거"가 있을 때만(과잉 거절 방지 — 정상 표본 7문항 테스트로 잠금). **거절이 정답인 16문항(함정 15+M-29) 전부 자동 방어, 모의고사 파일에서 직접 읽어 매 테스트마다 재검증(회귀 상시)**. 테스트가 실제 구멍 2개 발견·수정: ① 'kimi'가 해외 지수명 'MSCI Denmark IMI'의 공백 제거 문자열에 우연히 포함 → 원문 표기 재대조(token_matches)로 해결 ② '챗GPT'의 GPT가 실제 미국 ETF 티커(GPT)와 일치 → 한글이 붙은 영문 토큰은 한 단어로 처리. 'N호' 변형 상품명(국민성장펀드 2호 — 실존 유사명 있음) 거절+안내 처리. **⑤ 답변 생성기**: 검증 통과 근거만 HCX-005에 주입해 문장 생성 → **사후 대조**가 근거 밖 상품명·숫자가 든 줄을 삭제(숫자는 소수부 반올림만 허용 — 자릿수 다른 앞자리 우연 일치 차단), 전부 삭제되면 규칙 요약으로 강등, 해석·한계 노트와 기준일은 생성문에 강제 부착. 거절 경로는 생성기를 호출하지 않음(테스트로 잠금). 실 호출 검증: L-01 생성 6.2초, 사후 대조 통과. **테스트 240개 통과(+41)** |
| **S2 순서 ③ Router + 채널 실행기 — M1 로컬 E2E 관통 (8/14)** | `engine/`(router·router_llm·channels·answer_m1·policy·answer_cli) + `config/policy.json` + `pipeline/themes.py` | **rule-first 2단 Router**: Stage A 규칙(엔티티 scan grounding·등급/수치/시간 추출) — 하 난이도 fast path·**트랩 12유형 전부 LLM 0콜 확정**(값 도메인·미존재 자산·시간 위반·없는 필드·미존재 상품) / Stage B HCX-005 FC(템플릿 id enum 강제·스키마 검증 실패 시 수리 1콜·규칙 폴백) — **라이브 검증: 교집합 질문에서 삼성전자→005930·SK하이닉스→000660 정확 grounding, 1콜 유효 플랜**. **4채널 실행기** + 벡터·키워드 RRF 결합 — **"반도체 집중" 회귀 라이브 검증: 로보틱스 1위였던 것이 PHLX Semiconductor 1위로 정정**(테마 사전 50건 한→영 anchor). **M1 조립**: 5필드 전부 string · refuse 는 사유 코드 템플릿 거절문(최종형·환각 표면 0) · 존재 질의 직접 일치 시 과잉 거절 방지. 미확정 해석은 `config/policy.json` 룩업(무배포 교체). 응답 2.3~7.5s. 매수가능 질의에 만기 경과 채권이 나오던 것 발견·정정(활성 필터 자동 결합). **테스트 199개 통과** |
| **S2 순서 ② 검색 기반 (8/13)** | `pipeline/entity_index.py` + `engine/`(sql_templates·keyword_channel) | **통합 엔티티 인덱스** — 상품 4종(정식명+약칭)·구성종목(현금 센티널 제외)·한글 별칭·운용사(복구값)·지수를 한 사전으로, **exact(존재 검증용 — 직접 매칭만)/search(안내용 부분 일치) 2의미론 분리**(트랩 방어 정책의 코드화). **SQL 템플릿 카탈로그 25종**(105문항 gold 역산 — 채권 6·ETP 8·해외 3·펀드 4·구성종목 3·커버리지 1) — LLM 은 id+파라미터만 선택(환각 SQL 구조 차단), TRY_CAST·ILIKE 이스케이프·enum 검증·Evidence 자동 생성. 키워드 채널(exact 우선→부분 일치 폴백). 통합 테스트: KODEX 200 1위·펀드 11,138/95,618·삼성전자 편입 200+·운용사 집계 오염 0·보수 커버리지<20% 정합. **테스트 169개 전체 통과** |
| **S2 순서 ① 기반 계층 (8/13)** | `storage/load_duckdb.py` + `pipeline/`(mgmt_resolution·constituent_aliases·evidence) | **RDB 적재**: DuckDB 6테이블(31MB) 기대 행수 전량 일치 + 스팟 검증(AUM 상위 5 = KODEX 200 1위 — 주최 채점 예시 통과) · KG 인메모리 실측 620MB/21초(4GB 예산 통과) · **운용사 오염 64행 100% 복구**(접두 매핑, unresolved 0) · **해외 종목 한글 별칭 사전 66행**(캠브리콘·엔비디아·알리바바 등 — 실측 ISIN만, `query_kg --holds 캠브리콘` 실증) · **Evidence 계약**(4채널 공통 근거 규격 — 소스별 기준일 7/11·7/10 보존, retrieved_context 직렬화). 테스트 156개 전체 통과 |

### 7.2 미착수 — 우선순위 순 (1~2 = S1 잔여, 3~7 = S2)

> ~~온톨로지 TTL 초안~~ · ~~KG 추출 파이프라인 골격~~ — **8/11 완료**(§7.1로 이동). 아래는 잔여분.

1. ~~**ETF 구성 종목 수집·KG 연결**~~ **8/13 전량 완료(§7.1로 이동).** 후속만 잔여: 해외 종목 한글명 별칭("캠브리콘"→CAMBRICON) 사전 등재 · `mf:subsidiaryOf`(자회사, DART 후보 — 상-2 유형) · 해외 상장 ETF(PREF02N001)의 구성종목 소스(§8.3 — 국내 커버 발견으로 긴급도 하향).
2. **저장소 적재.** RDB 적재 + KG `.nt`의 정식 저장소 적재(제품 미정 — §8.1, 8/8 선정 즉시 착수 가능). KG는 저장소 중립 N-Triples로 이미 산출돼 있어 적재만 남음. 공모펀드 RDB는 `fund_master`(11,138) / `fund_attribute`(95,618, 2컬럼 키) 분리 적재.
3. ~~**Federated Retrieval + Query Router.**~~ **8/14 완료(§7.1 "S2 순서 ③"로 이동)** — rule-first Stage A + HCX-005 FC Stage B + 4채널 실행기 + RRF 결합, M1 로컬 E2E 관통. 잔여는 ④ Validation 과 ⑤ 생성기(교집합 등 결과 조합 연산 포함).
4. ~~**Answer Validation.**~~ **8/14 완료(§7.1 "구현 순서 ④⑤"로 이동)** — 5중 검문소 + 함정 16문항 상시 자동 방어 + 답변 생성기(사후 대조)까지 구현. 잔여는 ⑥ API 서버부터.
5. **`/answer` API 서버.** FastAPI, `GET /answer` 5필드 JSON, 동일 질의 캐싱, 타임아웃·에러 시에도 유효 JSON 반환. **8/23(일) E2E 관통이 하드 게이트** — 미달 시 기능 축소(벡터 검색 후순위).
6. **자체 평가셋 — 105문항 초안 완료(8/13, §7.1), 회귀 러너만 잔여.** 러너: 문항별 `GET /answer` 실행 → behavior 일치(트랩 오답변 최우선)·gold 스펙 대조·근거 표시 확인 + 응답 시간 계측(목표 15초). API 서버(§7.2-5)와 함께 S2 구현.
7. ~~**벡터 인덱스 재생성**~~ **8/13 완료(§7.1로 이동)** — 인덱스 5,567×1024 완성, 한↔영 교차 의미검색 가동. 잔여는 Federated Router(§7.2-3)에서 keyword 채널과의 결합뿐.

### 7.3 Blocker

1. ~~**CLOVA Studio API 키**~~ **[8/13 해소]** 키 발급·환경변수(`CLOVASTUDIO_API_KEY`) 설정 완료 → **HCX-005 실 호출 FC 왕복 성공**(도구 호출→로컬 실행→최종 답변, 감사 로그 mode=live 기록). **실측 발견: HCX-007은 40100 "No Service App"** — 승인된 서비스 앱이 HCX-005 대상. 007은 안 쓰기로 확정(8/13 사용자 — 005 메인, 파인튜닝은 후반 선택 최적화). 잔여: **AI 페스티벌 크레딧(20만) 신청 경로 미상**(§8.2), 확보분 40만의 유효기간 확인.
2. ~~**임베딩 v2 서비스 앱**~~ **[8/13 해소]** 사용자 신청·승인 완료 → 실측 성공(경로 `/v1/api-tools/embedding/v2` · 지연 p50 0.16초 · 레이트리밋 이슈 없음) → 전체 5,567건 인덱싱 실행. **외부 blocker 0 상태.**
3. ~~**KRX 로그인 쿠키 저장**~~ **[8/13 해소 — 전량 수집 완료]** 쿠키 저장 → 프로브 성공 → 1,139종목 전량 수집(세션 만료 1회는 쿠키 갱신으로 해소 — `CONSTITUENTS_PLAN.md` 3.1). 재수집 필요 시에만 재발생.
4. **전처리 결정성 간헐 이슈 (관측 1회, 원인 미상 — 실무 영향 낮음).** 8/7 검증 중 채권 CSV의 SHA-256이 1회 상이했고 이후 12회+ 재현 실패. 대응 완료: `preprocessing/verify_determinism.py`를 증거 보존형으로 재작성 — 재발 시 셀 단위 diff·입력 해시·실행 메타가 `determinism_diag/`에 자동 보존돼 원인 규칙을 특정할 수 있다. 정책: 누적 20회+ 통과 전까지 "바이트 결정성 확정"을 주장하지 않는다(문서에도 보류 명기). 기능 blocker가 아니라 신뢰성 주석.
5. **(minor) 코드 개선 4건 — 검증에서 CONFIRMED, 8/8 검토 후 적용.** ① `clova_client`의 model/base_url이 공개 가변 속성이라 생성 후 재할당으로 provider 검증을 우회할 수 있음 → 읽기전용 property화 ② `fc_hello.run_fc_loop`가 스키마 밖 인자에 TypeError로 크래시 → 오류 dict 반환으로 ③ 채권 `drv_is_buyable`이 결측 98%를 'N'으로 합침 → 3-상태(Y/N/unknown) 분리 검토 ④ 자동화 환경에서 pytest `--basetemp` 지정 필요.

## 8. 미확정 — 결정·확인 대기 (항목별: 상황 → 다음 행동)

### 8.1 팀이 결정할 것 — 8/8(금) 총검토 안건 (결과 확정 시 이 문서 갱신)

- **그래프 저장소 선정.** 후보 4개: rdflib+SPARQL(순수 파이썬·RDF 표준 준수·경량) / Neo4j Community(전용 그래프DB·Cypher·기능 풍부하나 운영 무게) / NetworkX+자체 인덱스(가장 가볍지만 표준 질의 언어 없음) / Apache AGE(PostgreSQL 확장 — RDB를 PostgreSQL로 갈 때만 성립). 판단 제약: **NCP 2vCPU·4GB 단일 서버 구동 + 15초 응답 + 9/7~30 무인 운영**(프로세스 수·장애 포인트 최소화).
- **RDB 선정 — DuckDB vs PostgreSQL.** 145K건은 어느 쪽이든 ms 단위라 순수 성능은 무관. 실질 기준은 ① KG 저장소와의 통합 운영(프로세스 수·백업·장애 포인트) ② 분석 SQL 편의(윈도우 함수·QUALIFY) ③ 적재 편의. AGE를 쓰려면 PostgreSQL이 강제되고, 아니면 in-process인 DuckDB가 운영상 단순하다.
- ~~**HCX-005 vs HCX-007 역할 분담.**~~ **[8/13 사용자 확정] HCX-005 메인, 007 안 씀(서비스 앱도 신청 안 함).** 근거: 005 = 공식 권장 + 파인튜닝 지원 + FC 실 호출 검증 완료 / 007 = 40100 "No Service App"(실측) + 튜닝 미지원. **파인튜닝은 "필수"가 아니라 후반 선택 최적화** — 정확성은 검색+검증 계층 소관(근거 표시 요건), 튜닝 모델은 FC 미지원이라 쓰더라도 답변 생성 단계 전용, E2E+평가셋 기준선 확보 후 판단. HCX-DASH-002(32K 경량)는 라우팅·조건추출 후보로 유지. 잔여 부속 안건: SHACL(pyshacl)류 온톨로지 저장 전 검증 구현 방안, 비정형 문서 임베딩 규모 추정.
- **브랜치 가설 재정의(§9 관련).** 온톨로지·KG가 전 팀 필수가 되면서 브랜치 차별화 축이 "SQL이냐 그래프냐"에서 **"온톨로지/KG를 어떻게 구축·활용하느냐"**(추출 방법·라우팅 전략·검증 계층)로 이동. jhnam 가설은 재선정 필요 — 기존 후보였던 "멀티턴 상태관리 강화형"은 멀티턴 없음 확정으로 가치 소멸.

### 8.2 주최 측 공지 대기

- ~~**HTTPS/도메인 요건.**~~ **[8/13 해결]** 공식 확정: **HTTP 기본**(HTTPS 선택·self-signed 무방), 표준 포트 80/443, **도메인 불필요 — 공인 IP 제출 가능**, 인증 헤더 없음. Global DNS·Certificate Manager 불필요. 잔여: **주최 측 발신 IP 대역 공지 대기**(공지 후 ACG를 해당 대역만 허용으로 좁힐 수 있음 — 선택).
- **서버 운영 기간 확정 공지 대기.** 운영 기간은 9/7~9/20 중 별도 공지(주제별 최대 1주) — 공지 전까지 9/7~9/20 전체 가동 가정으로 준비.
- **"상위" 등 랭킹 비즈니스 로직 기준.** "AUM 기준 상위 5개에 1위(KODEX 200) 누락이면 감점"이라는 예시만 공개됐고 세부 가이드는 추후 공지 예정. → 그때까지 자체 기준(AUM=`pd_net_tamt` 등 사전 9종의 규칙)으로 구현하고, 가이드가 나오면 기준만 교체할 수 있게 룩업으로 분리해 둔다.
- **규정·제출 추가 안내 PDF.** 기술 세션(온톨로지/KG) 자료 PDF는 존재하지 않음 — 녹음(교정본)이 유일 기록. ~~기술제안서 목차 "5번 항목"~~ **[8/13 해결 — 참가자 필기 정리본으로 목차 확정: 5번 = 기대효과·확장성**(§2 표 참조)**]**. 이 PDF로 확정할 잔여: 평가 문항 수·트랩 수의 공식 수치, **AI 페스티벌 크레딧(20만) 신청 페이지 경로**(신청 방법 미상 — 디스코드에서도 병행 확인).

### 8.3 디스코드·Q&A 게시판 질문 목록 (이관 완료 — 답변 수신 시 반영)

- **[8/13 접수됨(사용자)] 트랩 판정과 외부 수집 데이터의 관계** — "kimi 관련 상품" 유형에서, 참가팀이 수집한 외부 데이터(구성종목 등)에 관련 정보가 있을 경우의 채점 기준. 답변 전까지 자체 정책: **모집단=제공 마스터 고정 + 직접 개체 매칭만 + 간접 연상 금지**(`evalset/SEED_QUESTIONS.md` 트랩 방어 정책). 실측: 현 데이터 전체에 kimi 매칭 0건

- **HCX RPM/TPM 호출 한도** — 평가 기간 레이트리밋 초과는 0점 리스크. 순차 호출 + 타임아웃 300초 확정으로 부담은 크게 완화됐지만 수치 확인 필요
- ~~마감 후 장애 대응 허용 범위~~ **[8/13 해결]** 불가피한 사유(서버 장애 등)의 재기동은 실격 아님 — "결과물 변경"만 금지
- ~~`retrieved_context`/`think_trace` 형식 자유도~~ **[8/13 해결]** 모든 필드 string, 복수 문서는 문자열 내 구분 태그 자유(태그는 평가 대상 아님), 길이 제한 없음 — 내부 구조화 유지 + 어댑터 직렬화 설계 그대로 유효
- **해외ETF 구성종목 소스** — 중 난이도 유형 예시("캠브리콘 편입 중국 반도체 ETF")로 해외 구성종목 **유형**이 출제 범위임을 확인(8/13). **[8/13 발견으로 긴급도 하향]** KRX 수집분에 국내 상장 ETF의 **해외 주식 편입 ~34,900행이 포함**돼 캠브리콘류 질의가 국내 데이터로 이미 답변됨(16종목 실증). 잔여 격차는 **해외 상장 ETF 마스터(5,646종) 자체의 구성종목** — **[8/13 조사 완료] 주 소스 후보 = SEC EDGAR N-PORT**(무료·구조화·월별 보유내역, 7/11 이전 as-of 확보 가능 — `CONSTITUENTS_PLAN.md` 5.1). 착수는 주최 문의 답변 + 105문항 실측 후 판단(S2 본공사 우선)
- 데이터 해석: `NDY_*` 5종 공식 정의(익일 적용 민평 추정) · `axis_*`의 의도(정답 라벨인지 단순 예시인지) · 금액·가격 단위(발행잔액 원/천원, 평가가격 액면 10,000원당 여부) · `prfd_attr_cd` 코드표 · **총보수 0의 의미**(국내 150건·해외 363건 — 실제 무보수인지 미수집인지에 따라 "보수 낮은 ETF" 답변 모수가 달라짐) · 손상 행 2건의 정정본 제공 여부

### 8.4 팀 내부 열린 결정 — 구현 전 확정 필요 (전체 목록: 이 문서 말미 부록 D)

- **위험등급 범위: 데이터 실측(1~6) 우선.** 설명회 발언은 "1~5등급"이지만 제공 데이터 실측은 0~6(0=미분류 센티널 58건 → NULL 처리, 유효 1~6, 1=매우 높은 위험). 발언은 단순화 또는 전사 오류로 판단. 해외ETF는 위험등급 컬럼 자체가 없어 unavailable.
- ~~**국내ETF 운용사 컬럼 오염 정정**~~ **[8/13 해소]** 실측 64행(상품명 전체 54 + 브랜드 결합 10) — `pipeline/mgmt_resolution.py` 접두 매핑으로 **100% 복구(unresolved 0)**, 전 1,733행 검증. 원시값 보존 + 검색·집계 계층 적용 방식. 법인 통합(삼성 vs 삼성액티브 vs 삼성증권 구분 유지)은 하지 않음. 상세: `kg/KG_METHOD.md` 6장.
- "위험등급이 낮은" 질의를 항상 5~6등급으로 해석할지, 매번 역질문형 답변으로 처리할지
- 부분 coverage일 때 모집단 답변을 거부할 최소 coverage 기준(예: 조건 부합 N개 중 값 있는 게 몇 %면 랭킹을 낼 것인가)
- ETF-공모펀드 중복 상품(같은 전략의 ETF·펀드 클래스)의 표시·연결 단위
- 채권 `BUYABLE_QUANTITY > 0`(325행)을 `is_tradable`로 볼지 업무 규칙 확정 · `CRD_GRD` 복수 평가등급 중 대표값 산정 규칙과 `C0` 137건의 실체 · `or_attr_desc='06'`(5,436행)을 파생형으로 매핑해도 되는지 · `du_lpr`의 스키마 한글명 '시가'가 저가(low)의 오기인지
- 개인화·금융 조언의 안전 경계 — 어디까지 정보 제공이고 어디부터 투자 권유로 거절할지

## 9. 팀 운영 방식

- **수직 분할**: 3인 각자 브랜치에서 E2E(적재→검색→에이전트→API→자체평가) 완주. 차별화 축은 **온톨로지/KG 구축·활용 방식**(온톨로지·KG 자체는 전 팀 필수)
- **공통 소유 3종**(중복 금지, main에 둠): 공통 계약 4종(채널 계약·DB 스키마·응답 JSON·러너 I/O) · 평가셋 105문항+회귀 러너 · 배포 골격
- **교차평가 통합**(8/27~29): 동결 → 공용 러너 정량 표 → 노드 단위 채택표 → main 병합 → **통합본이 개별 브랜치 최고 정답률 이상일 때만 확정**(미달 시 롤백)

## 10. 리스크 Top 6

| 리스크 | 대응 |
|---|---|
| 트랩 문항 오답(환각) — 답변 시 감점 | Answer Validation(존재 검증)을 응답 경로 필수 통과점으로. 자체 트랩 15문항 회귀 |
| 응답 60초 초과·지연 감점 | 목표 15초. S2부터 문항별 계측, LLM 호출 2~3회 제한·캐싱·채널 병렬화 |
| 9/6 이후 서버 다운 = 실격 수준 | 8/30 동결 후 1주 안정성 검증 · immutable release · 헬스체크·자동 재시작 |
| 온톨로지 제출물 미비 | S1부터 TTL을 리포지토리에 두고 버전 관리 |
| HCX 레이트 리밋·API 키 지연 | **NCP 크레딧 신청이 최우선 선행 작업.** 한도는 Q&A 확인 |
| 기술제안서 부실(40점) | 매주 누적 작성 + 요구사항 추적표(요구→구현→검증 3열) |

---

## 부록 — 실측 사실·설계 결정·열린 결정 원장 (구 memory.md에서 이관, 8/14)

> 구 memory.md의 참조 정보를 이관 시점 상태 그대로 보존한 것이다(작업 일지는 [PLAN.md](PLAN.md) 부록 A로 이관).
> 일부 열린 결정은 이후 해소됐을 수 있다 — **최신 판단은 본문 §8과 PLAN.md 가 우선**한다. 공식 세부 규정(8/13 필기 3종)은 본문 §2에 이미 반영돼 있어 여기서 제외했다.

### A. 확인된 데이터 사실 (실측)

- 국내채권: 42,394행 × 40컬럼. `PD_NO`가 공백과 중복 없이 유일하다.
- 국내채권 위험코드는 0~6이며 0은 58건이다(0=미분류 센티널 → NULL 처리). 유효 등급은 1~6 — 1=매우 높은 위험, 6=매우 낮은 위험으로 표준화한다.
- 국내채권 수익률 계열은 881행(2.1%)에만 값이 있다. `BUYABLE_QUANTITY > 0`은 325행이다.
- 국내채권 만기: 날짜 파싱 성공 기준 만기 도래 16,180건 / 불명 별도 분리.
- 국내 ETF/ETN: 1,734행 × 73컬럼(손상 1행 격리 후 1,733 = ETF 1,201 + ETN 532).
- 국내 ETF 총보수는 신뢰 가능한 양수가 67건뿐(값 217건 중 150건이 0), 기초지수는 활성 ETF 실효 2/1,140.
- 국내 ETF/ETN 운용사 컬럼 오염: 상품명이 들어간 55행+브랜드 결합 → 실측 64행, 8/13 접두 매핑으로 100% 복구.
- 해외 ETF: 5,646행 × 49컬럼. 위험등급 컬럼이 없고 총보수는 전 행 존재. `pd_isin_cd`는 결측·중복이 있어 기본 키로 쓰지 않는다.
- 해외 ETF `cu_strtegy`(전략 서술)는 5,638행 값 존재, 고유 문서 5,566개. `cu_base_index`에 Lipper 센티널 문구 2,705건(48%) — null 정규화 필요.
- 공모펀드: 95,619행 × 45컬럼. `itm_no` 11,139개(중복), `(itm_no, prfd_attr_cd)` 조합이 유일 키. 그룹 내 변동 컬럼은 `prfd_attr_cd`뿐(평균 8.6행).
- 공모펀드 위험명 표기 오염("높은 위험"/"높은위험" 혼재), 문자열 `NULL` 18,416건, 컬럼 밀림 비정상 1행(격리).
- 국내채권 `CRD_GRD`의 `AA0` 표기는 무부호(플랫) — 끝자리 0 제거로 정규화, 서열 AAA=1~D=20.
- KOFIA 20자리 펀드분류코드의 6번째 자리가 클래스 유형(금투협 공식 확인).
- 지역·자산군 값은 상품군마다 언어·세분화가 달라 수동 매핑 테이블 필요(런타임 LLM 판정 금지).
- (구 Lance 인덱스) fragment 누락으로 복원 불가 — 제거, CLOVA 임베딩으로 재구축 완료.

### B. 외부 소스·계정 실측 (8/12~13)

- KRX 정보데이터시스템은 로그인제 — 비로그인 API는 LOGOUT 반환. 세션 ~25분 만료 시 **HTTP 400**(쿠키 재추출이 1순위 해법, IP 차단 아님).
- SEIBro는 현재 시점만 조회 가능(기준일 파라미터 없음), 공공데이터포털에 구성종목 API 없음 — KRX가 유일 소스.
- KRX 구성종목 데이터에 해외 주식 편입분 포함(미국·중국·일본 등) — "캠브리콘 편입 ETF"가 국내 데이터만으로 답변됨을 실증.
- CLOVA 서비스 앱은 모델·도구 단위 — HCX-005 채팅용과 Embedding v2용을 각각 승인받음. HCX-007은 미신청(안 씀 확정).
- API 키는 Windows 사용자 환경변수 `CLOVASTUDIO_API_KEY`. NCP 크레딧 40만 확보(유효기간 확인 필요), 페스티벌 크레딧 20만 신청 경로 미상.

### C. 설계 결정 (이관 시점 유효분)

- 필터·정렬·집계는 SQL 채널, 관계·멀티홉은 그래프 채널, 의미 검색은 벡터 채널(해외 ETF 서술 한정), 이름 검색은 키워드 채널 — 라우터가 배분.
- 원본 식별자·날짜는 문자열로 읽어 선행 0 보존. 원본 값은 정규화 후에도 추적 가능하게 보존.
- 공모펀드는 상품(master)과 판매 클래스(attribute)로 분리 관리.
- 위험등급은 1=매우 높음~6=매우 낮음으로 통일, 해외 ETF는 "항목 없음" 처리.
- 데이터가 없거나 부분적이면 상태와 범위를 명시하고 값을 추론하지 않는다. 단정 추천 대신 조건 부합 사실과 근거 제시.
- 지역·자산군 매핑은 사람이 검토·버전 관리(런타임 LLM에 맡기지 않음).

### D. 열린 결정·확인 대기 (이관 시점 상태 — 해소분은 취소선 없이 본문 §7·§8 참조)

- 결정성 간헐 이슈(관측 1회, 원인 미상) — 증거 보존형 검증기로 계측 완비, 누적 20회+ 통과 전 단정 금지.
- 코드 개선 minor 4건(클라이언트 속성 읽기전용화, fc_hello 오류 dict, buyable 3-상태, pytest --basetemp).
- 트랩 판정 vs 외부 수집 데이터 — 주최 문의 접수(8/13), 답변 대기. 자체 정책: 모집단=제공 마스터 고정·직접 매칭만·간접 연상 금지.
- 해외 상장 ETF 구성종목 소스 — SEC N-PORT 후보 확정, 착수는 주최 답변+실측 후 판단.
- AI 페스티벌 크레딧(20만) 신청 경로 · 확보분 40만 유효기간 확인.
- 개인화·금융 조언 안전 경계 / 샘플 `axis_*`의 지위 / 공모펀드 비정상 1행 처리 / `or_attr_desc='06'` 매핑 / 금액 단위 / `NDY_*` 정의 / `CRD_GRD` 대표값 규칙 / `du_lpr` 명칭 오기 여부 — 주최 Q&A 확인 대상.
- ETF-공모펀드 중복 상품의 표시/연결 단위 결정.
- HCX 호출 한도(RPM/TPM) 확정 · 9/30 크레딧 만료 후 리소스 정리 절차.
