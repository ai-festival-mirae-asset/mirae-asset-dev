> **[보관용 아카이브]** 이 파일은 2026-08-11에 ROADMAP.md를 핵심 위주로 재구성하면서 보존한 직전 버전(v3.0, 2026-08-07) 전문이다.
> **현재 유효한 로드맵은 [ROADMAP.md](ROADMAP.md)를 볼 것.** 이 파일은 의사결정 이력·상세 근거 추적용으로만 참조한다.

# 제10회 미래에셋증권 AI Festival — 금융상품 Agent 프로젝트 로드맵

> 작성 기준일: 2026-08-07(금) · 예선 마감 2026-09-06(일) · **D-30** · **v3.0 — 8/6 설명회 녹음본 전문 반영 + 아키텍처 리빌딩 확정(Ontology + Knowledge Graph + Federated Retrieval 필수)** · 이력: v2.4 설명회 1차 메모(8/6) · v2.3 dev-kyung 교차검증 + 역할 분담 개편(8/5)
> 팀 구성: 3인 (개발·기획 겸임, 경험 있는 개발자 보유) · 원칙: **성능 최우선** · 개발 방식: **개인 브랜치에서 각자 E2E 완주 → 교차평가 → 노드 단위 통합** (5장)
> 관련 문서: 팀 공동 실행 가이드 [PROJECT_GUIDE.md (main 브랜치)](https://github.com/ai-festival-mirae-asset/mirae-asset-dev/blob/main/PROJECT_GUIDE.md) · 작업 로그 [memory.md](memory.md) — 이 문서는 개인 브랜치(papuagigi) 관점의 전략 로드맵이다. **[8/7] 아키텍처는 Ontology + KG + Federated Retrieval로 확정**("확정 아키텍처" 장) — RDB·그래프 저장소 선정은 8/8 총검토, 브랜치 차별화 축은 온톨로지/KG 구축·활용 방식(5.1절 [8/7] 주석)이며 통합 절차는 5.3절을 따른다.

---

## 8/4 리서치 확정 — 우리가 나아갈 방향 5가지

논문·벤치마크·산업 사례 리서치로 확정한 전략 방향이다. 상세 근거는 1·2장과 참고 출처 참조.

> **[8/7 갱신]** 방향 1(파라미터화 도구+FC 단독)은 8/6 설명회 이후 **"확정 아키텍처" 장의 Ontology+KG+Federated Retrieval로 대체**됐다 — 단 그 핵심 교훈("검증된 좁은 인터페이스", 결정성)은 SQL 채널 설계 원칙으로 유지된다. 방향 2(사전=성능)·4(런타임 HCX)·5(병목은 LLM 호출)는 유효하며, 방향 4는 8/6 완화 확정(전처리 자유·메인 HCX — 2장), 방향 3(3-Tier)은 Tier 2 격상으로 개정됐다(1장).

1. **아키텍처는 "파라미터화 도구 + Function Calling"으로 간다.** 자유 Text-to-SQL은 SOTA 파이프라인조차 BIRD 벤치마크 71~77%(인간 약 93%)이고, 그것도 GPT-4o급 모델 + 다단계 앙상블 기준이다. 금융 DB 질의 연구(FinAI Data Assistant)와 Uber QueryGPT·LinkedIn SQL Bot 프로덕션 사례의 결론이 모두 같다: **스키마 전체를 LLM에 주지 말고, 검증된 좁은 인터페이스로 제한하라.** 예시 질의 set 확인 후 롱테일 커버용 '제한된 Text-to-SQL fallback'만 옵션으로 검토한다.
2. **컬럼 사전·값 사전이 곧 성능이다.** semantic layer 연구에서 의미 계층(컬럼 정의·값 매핑·동의어) 유무가 정확도 **+17~23%p**(Cube 벤치마크), BIRD external knowledge **+20%p**를 가른다. S1의 사전 작업은 잡무가 아니라 핵심 성능 작업이며, 외부 데이터 수집 1순위도 이 사전을 채우는 자료다.
3. **외부 데이터는 3-Tier 원칙(1장)으로.** 해석 메타데이터(Tier 1)는 적극 수집, 결측 필드 보강(Tier 2)은 "제공 데이터 기준 답변 우선 + 출처 명시 부가정보"로만, 시세·수익률 대체 데이터(Tier 3)는 금지.
4. **런타임의 모든 LLM 추론은 HCX만.** OpenAI '모델'(GPT 계열)은 라우팅·조건추출 등 어떤 단계에도 불가(실격 조항). OpenAI 'SDK'를 CLOVA 호환 엔드포인트로 쓰는 것은 가능(모델은 HCX). 임베딩 모델 등 회색지대는 8/6 서면 확인(2장).
5. **성능 병목은 DB가 아니라 LLM 호출이다.** 14.5만 건은 어떤 DB든 ms 단위다. 질의당 LLM 호출 2~3회 제한, 규칙 우선 조건추출, 동일 질의 캐싱, 레이트리밋 대응이 진짜 성능 작업이다.

---

## 8/5 브랜치 교차검증 — dev-kyung 검토 결과 반영

> **[8/7]** 이 장의 "파라미터화 도구+FC 유지" 판단(아래 및 D절)은 이후 **"확정 아키텍처" 장으로 대체**됐다. 실측 정정(A)·신규 발견(B)·설계 채택(C) 항목은 아키텍처와 무관하게 전부 유효하다.

dev-kyung 브랜치(`PROJECT_GUIDE.md`, `reports/DATA_QUALITY_REPORT.md`, `pipeline/`, `tests/`)를 정독하고 이 브랜치 산출물과 대조했다. **아키텍처 방향(파라미터화 도구 + Function Calling)은 유지**하되, 아래는 아키텍처와 독립적인 발견·설계라 채택한다. 상세 정정은 1장 각 상품군 항목에 `[8/5 정정]`으로 표기했다.

### A. 실측 정정 — dev-kyung이 맞고 우리가 틀렸거나 놓친 것

| 항목 | 기존(우리) | 정정(dev-kyung 실측) | 영향 |
|---|---|---|---|
| 채권 `REMAINING_DAYS` | "이미 있으니 우선 활용" | 저장값의 기준일은 추출일이 아니라 **행별 `PD_STD_INFO_UPDATE`**(추출일 대비 중앙값 137일 지연)라 그대로 쓰면 안 됨 | 잔존만기는 `MAT_DT` 캐스팅 후 **요청 시점 재계산** 필수 |
| 채권 만기도래 건수 | (전처리 R8) 16,496건 | `MAT_DT=0` 316건이 오포함된 수치. 정정: **만기 16,180 / 당일 7 / 이후 25,884~25,885 / 불명 322~323** | 채권의 38%가 이미 만기 — **기본 검색에서 만기·불명 제외** 규칙 필요 |
| 국내ETF 총보수 풀 | "값 있는 217건 풀에서 랭킹" | 217건 중 **150건이 0**(실제 무보수인지 미수집인지 미확정) → 신뢰 가능한 양수는 **67건** | "총보수 낮은 국내ETF" 답변 모수가 217→67로 축소, 한계 명시 필수 |
| 국내ETF 격리 대상 | 공모펀드 1행만 격리 | **국내ETP 1,155행도 손상 행** — `pd_itm_no='KR'`, 이름 `'.'`. 같은 파일 299행(`KR70193M0005`)의 손상된 중복이므로 격리해도 상품 유실 없음 | `preprocess.py`에 격리 규칙 추가 필요(적재 시 키 중복은 안 나지만 쓰레기 행이 검색 오염) |
| 공모펀드 유효 상품 수 | `itm_no` 고유 11,139 | 손상 1행의 `itm_no='"'`를 빼면 **유효 11,138** | 클래스 대표 선정·집계 분모 정정 |
| 공모펀드 키 | 3컬럼 복합키(PK 성립) | `(itm_no, prfd_attr_cd)` **2컬럼만으로 유일** 성립(그룹 내 변동 컬럼은 `prfd_attr_cd` 하나) | DB 스키마 단순화 — bridge 키를 2컬럼으로 |

### B. 우리가 몰랐던 신규 발견 — 답변 정확도에 직결

1. **국내 "ETF" 파일의 30.7%가 ETN이다** (ETF 1,201 + ETN 532, 정제 기준). "ETF 추천해줘"에 ETN이 섞이면 오답 — `pd_grp_no` 기반 `instrument_type` 파생 필수. 해외도 ETF 5,587 + ETN 59 혼재.
2. **거래종료 상품 212건이 국내ETP에 섞여 있다** (`pd_lste_dt`가 추출일 이전). 기본 검색 대상은 거래종료 전 + 거래정지 아님 = **1,520건**. 종료 167건은 종가 0이고, **기간수익률 `-100`은 실손실이 아니라 이 0 종가에서 계산된 산출물** — 수익률 랭킹에 넣으면 안 되고, 상장폐지 판정은 `-100`이 아니라 종료일 기준으로 해야 한다.
3. **활성 ETF의 기초지수 실효 커버리지는 2/1,140 (0.18%)** — "값 있는 58건·고유 19개"보다 훨씬 나쁘다. 기초지수 질의는 국내ETF에서 사실상 전면 "확인할 수 없음".
4. **추출일 ≠ 필드 기준일.** 국내ETP 일간 데이터는 주로 06-15(추출 26일 전), 해외ETF 06-14~16(30일 초과 지연 252행), 채권 표준정보는 중앙값 137일 지연. "데이터 기준일 2026-07-11" 일괄 표기는 부정확 — **필드별 기준일 표기**로 바꾼다.
5. **함정 컬럼 추가**: `pd_tr_yn`은 '거래정지 여부'라 1을 "거래 가능"으로 읽으면 의미가 반전됨 · ETN의 `du_last_aum`은 전부 0이라 AUM은 `pd_net_tamt`가 안전 · 채권 `AVG_ANNUAL_TAX_YIELD`는 비결측 881건이 전부 0이라 사용 불가 · 해외ETF `pd_isin_cd`는 공백 9행 + 중복 50그룹이라 키로 부적합(`pd_itm_no` 유지) · 해외ETF에는 위험등급 컬럼 자체가 없음("해외ETF 저위험" 질의 = 확인 불가 케이스).
6. **채권 42,394건 중 1건(`XS3067881758`)은 비-KR ISIN 국제채권** — 국내채권 기본 검색에서 제외하고 예외 태깅.

### C. 설계 채택 — dev-kyung 방식이 더 체계적이라 가져오는 것

| 채택 항목 | 내용 | 반영 위치 |
|---|---|---|
| **답변 가능성 상태 머신 (8종)** | `answerable / partial_coverage / unsupported_field / ambiguous_condition / insufficient_condition / no_matching_rows / stale_or_inactive / invalid_data` — 우리 가드레일 4항목을 포괄하는 상위 설계. coverage 분모는 전체 테이블이 아니라 **사용자 조건 적용 후 eligible 집합**("조건에 맞는 N개 중 값 있는 M개만 비교") | 2장 ⑤ 가드레일, S3 |
| **as_of_date 이원화** | 재현용 스냅샷 통계는 2026-07-11 고정, API 응답의 잔존만기·만기상태는 **요청 시점 서울 날짜로 계산**(명시적 사용자 날짜 우선). dev-kyung `pipeline/time_policy.py`(46줄) + 단위 테스트가 이미 있어 **모듈 그대로 이식 가능** — DB 선택과 무관 | S2 도구 구현 |
| **구조화 `retrieved_context`** | 문자열이 아니라 근거 행 배열(출처 테이블·행 키·사용 컬럼·값·기준일) + 내부 구조화 유지, 공식 스키마가 문자열을 요구하면 어댑터에서 직렬화. 8/6 질문 리스트의 "형식 자유도" 항목과 연결 | S2 로깅, 공통 계약 3번 |
| **HCX 단일 provider 강제** | 규정 준수를 문서가 아니라 **코드로 강제**: provider allowlist, 설정 누락 시 시작 실패(fallback 금지), 모델 식별자 감사 로그. "타 LLM 0건"을 로그로 증명 가능 → 정성평가 리스크 관리 어필 | S2 에이전트 루프 |
| **immutable release 운영** | 8/30 동결 시 commit SHA·image digest·dependency lock·설정 checksum 보관, 평가 기간 모니터링·복구 runbook. "마감 후 허용되는 장애 대응 범위"를 8/6 질문에 추가 | S4 |
| **요구사항 추적표** | 공식 요구사항 → 구현 산출물 → 완료 검증 3열 매핑 — 기술제안서 뼈대 + acceptance test 목록으로 전환 가능 | S4 제안서 |
| **taxonomy 매핑 계약** | 지역·자산군 매핑에 `mapping_status(mapped/ambiguous/unmapped)`·버전·검토자 기록, 미등록 값 발견 시 자동 추론 대신 실패 처리. `product_axis` 생성 규칙의 품질 장치 | S1 통합 축 |
| **파이프라인 테스트·결정성** | dev-kyung은 `tests/` 단위 테스트 + 2회 연속 실행 SHA-256 동일성 검증. 우리 `preprocess.py`는 assertion만 있음 — 테스트 추가 | S1 |
| **수동 보강값 분리 관리** | `config/manual_overrides.csv` 패턴: 사람이 승인한 해석값을 원본과 분리된 파일에 source/status/reason과 함께 기록, 원본 공란은 덮어쓰지 않음 — Tier 1 사전의 개별 상품 보강에 그대로 적용 가능 | S1 사전 |

### D. 판단 유지 — 비교 결과 우리 안이 여전히 우세한 것

- **아키텍처**: dev-kyung의 "allowlist Text-to-SQL(view 제한 + AST 검증 + read-only)"은 자유 SQL보다 훨씬 안전하지만, 방향 1의 근거(BIRD 71~77%, FinAI/Uber/LinkedIn)대로 **파라미터화 도구가 결정성에서 우위**. 단 dev-kyung 안은 우리 "제한된 Text-to-SQL fallback"(2장 옵션) 설계와 사실상 동일하므로, fallback을 붙이게 되면 dev-kyung 구현을 기준으로 삼는다. 최종 판단은 5장 공통 계약 기반 브랜치별 정답률 비교로.
- **Tier 1 해석 메타데이터**(사전 9종, 웹 리서치 428건)와 **성능 리서치 근거**는 dev-kyung에 없는 이 브랜치 고유 자산 — semantic layer +17~23%p 근거대로 유지.
- **LLM 호출 예산**: dev-kyung ≤2회 vs 우리 2~3회 — 상충 없음. 2회를 목표로, 교차 질의만 3회 허용.

---

## 8/6 설명회 확정 기록 — 녹음본 전문 반영 (8/7 · 기술 세션은 녹음본이 유일 기록)

> 출처: 8/6(목) 오프라인 설명회 **녹음 전사본 전문**(36,809자) 정독 — `../미래에셋 ai페스티벌 오프라인 설명회_녹음본.txt`. 괄호 숫자는 녹음 타임스탬프. 전사본이 AI 생성이라 숫자·고유명사 오전사 가능성이 있는 항목은 말미 D절에 모았다. 발표 구성: ① NCP 크레딧·서버(네이버클라우드) ② 테크 세션 Ontology/KG/Retrieval(미래에셋) ③ 과제 설명·Q&A.
>
> **[8/7 확인] 자료 배포 계획**: 기술 세션(Ontology/KG 적용 방법)의 **자료 PDF는 존재하지 않는다 — 음성 녹음이 유일한 기록**이며, 이 장 B절이 그 확정 문서화다. 추후 제공 예정인 PDF는 **규정·제출 자료 관련 추가 안내**에 한정된다 → D절 중 규정·제출 성격 항목만 그 PDF로 확정 가능하고, 기술 세션 유래 항목은 Q&A·데이터 실측으로 판정한다.

### A. 과제 규칙 확정 사항 — 기존 이해와 달라진 것

| # | 확정 사실 | 근거 | 영향 |
|---|---|---|---|
| 1 | **온톨로지 소스코드 제출 필수** — "소스코드에 도메인별 온톨로지 필수. TTL일 필요는 없지만 온톨로지를 적는 방법으로 적혀 있어야 한다" + 기술제안서에 온톨로지 구축 방법·활용 데이터 서술 요구 | 01:00:00~01:00:48 | 온톨로지는 전략 선택이 아니라 **제출물 요건** |
| 2 | **배점 100 = 소스코드 20 / 기술제안서 40 / 평가 API 질의 답변 40** | 59:23 | "예선=API 정확도 싸움" 전제 수정 — **제안서가 답변과 동급 배점** |
| 3 | 평가 질의 **30문항(상·중·하 각 10)** + **답변 불가 트랩 5문항**(점수 미포함, 답변 시 감점) · 전 팀 동일 문항 · 순차 호출(동시 아님) | 59:38, 01:04:45, 01:07:18 | 자체 평가셋을 30문항 미러링 + 트랩 중심으로 재설계 |
| 4 | **응답 소요 시간이 평가 항목** — 문항당 60초 이내 인정(사실상 감점 경계), 담당자 실서비스 기준 15초, 기준이 어려우면 참가자 전체 평균 대비 상대 가감점 가능 | 59:50, 01:02:11~01:03:21 | 지연 최적화가 배점 항목. 15초 목표 |
| 5 | **멀티턴 없음** — 단일 질문·단일 답변. "이렇게 조건을 주시면 답변드릴 수 있습니다" 형태의 역질문형 답변은 인정하되 후속 질문은 오지 않음 | 01:05:00~01:05:42 | 역질문은 상태 관리 없이 답변 문면으로만 설계 |
| 6 | **HCX 제약 완화** — "모든 부분을 하이퍼클로바로 하라는 취지 아님. **최종 답변 생성·메인 에이전트 구성은 HCX 고정**, 데이터 전처리 등은 다른 방법 자유" | 55:44, 01:09:42~01:10:19 | 기존 "런타임 모든 LLM 추론 HCX만" 판정 완화. 단 메인 파이프라인은 보수적으로 HCX 유지 |
| 7 | **외부 비정형 데이터 활용이 과제의 핵심** — "제공 RDB에 없는 것들을 비정형에서 온톨로지를 활용해 추출한 다음 답변으로 연결하는 게 이 과제의 핵심" · ETF **구성 종목** 질의 출제 예고(제공 데이터에 없음) · 단 **7/11 이전 기준 데이터만**(9월에 나온 3분기 보고서 등 이후 데이터 불인정) | 57:22~58:00, 01:13:00~01:13:49 | 외부 데이터 3-Tier 중 Tier 2를 **핵심 과제로 격상** (1장 개정) |
| 8 | 트랩 예시 공개: 신용등급 AAAA 채권 / "키미" 관련 투자상품 / KODEX AI로봇 ETF — 7/11 기준 존재하지 않음 → "없다/확인 불가"가 정답. LLM 사전지식으로 답하면 감점("Answer Generation 전에 우리 상품 정보에 진짜 있는지 검증을 반드시 넣어야") | 01:03:33~01:04:24, 01:05:54~01:06:45 | Answer Validation이 명시적 배점 방어선 |
| 9 | **YTD = 2026-01-01 ~ 기준일(7/11)** 수익률로 해석 — 이런 비즈니스 규칙을 시스템에 정의해 둬야 함 | 38:45, 58:27 | time_policy·Answer Validation 방향 일치 확인 |
| 10 | "상위" 등 비즈니스 로직 기준은 추후 가이드 공지 예정. 예: "AUM 기준 상위 5개"에 1위(KODEX 200) 누락 시 감점 | 01:11:24~01:12:35 | 랭킹 기준 확정 대기 — 자체 기준은 가이드 나오면 교체 |
| 11 | 기술제안서 **PDF + Word 동시 제출**(사후 편집 방지 목적) · 권장 목차 중 **5번 항목이 수상 단계 핵심 평가 요소**("본선 진출이 아니라 수상이 목표라면 5번을 미리 고려") | 01:01:00~01:01:45 | 5번 항목명은 규정·제출 PDF(추후 제공)로 확인 (D절) |
| 12 | 채점 방식: **문항별 기대 엔티티·릴레이션이 답변에 전부 포함됐는지 + 근거의 유의성**으로 평가(기준값·가이드 보유, 비공개). 표현 차이는 무관. 주최 측이 같은 데이터로 자체 검색 테스트를 수행한 정황(키미 함정을 그렇게 발견, 01:06:13) | 01:11:04~01:11:49, 01:07:18 | 근거 표시를 채점 가능한 구조(엔티티·관계 명시)로 |
| 13 | **튜닝 자유**(HCX 모델 기반이면 제한 없음), 비용은 참가자 크레딧 부담 감안 | 01:09:00~01:09:23 | HCX-005 튜닝 경로 공식 인정 |

### B. 테크 세션 요지 — Ontology / Knowledge Graph / Retrieval (미래에셋 수석)

1. **Ontology = 개념과 규칙**(데이터 없는 스키마), **KG = 온톨로지를 적용해 걸러낸 실제 인스턴스** (25:50~26:52)
2. 금융에서 온톨로지가 중요한 4가지 이유 (27:11~30:18):
   - **용어 표준화** — 운용사 = 자산운용사 = AMC(Asset Management Company)를 하나의 표준 용어로 통일
   - **지능형 추론** — "운용사가 상품을 운용한다"를 정의하면 역관계("상품은 운용사에 의해 운용된다")와 상위클래스(ETF ⊂ 금융상품) 추론이 자동으로 가능 (35:08~36:30)
   - **데이터 무결성** — 위험등급 범위 밖 값(예: 99등급)이 나오면 저장 전에 걸러냄. "99등급이라고 답하는 것보다 '위험등급이 없다'가 고객에게 낫다"
   - **AI 신뢰성** — 어떤 관계를 거쳐 답이 나갔는지 추적 가능. 금융 VOC("추천해서 샀는데 떨어졌다") 대응에 필수 — "금융권에서 GenAI를 그대로 쓸 수 없는 이유"
3. 표현 체계: **URI로 이름 붙이고, RDF 트리플(주어-술어-목적어)로 사실을 쓰고, OWL로 의미·제약을 정의**. 직렬화는 **Turtle(TTL) 권장**(필수 아님 — 사람이 읽기 쉽고 실무에서 수월) (30:18~31:26)
4. 제약(constraint) 예시 (32:12~33:59): 위험등급 범위 검사 · **ETF와 ETN은 disjoint**(한 상품이 둘 다일 수 없음) · **하나의 ETF는 하나의 운용사**(법령) · 전략은 패시브/액티브 중 정확히 1개
5. 설계 순서: **business goal → competency question(어떤 질문이 들어올까) → ontology 설계 → 비정형 데이터 분석 → knowledge graph → GraphRAG로 저장(벡터·RDB·그래프DB) → LLM이 온톨로지+데이터로 검색** (36:30~37:30)
6. 4대 태스크 (37:49~39:45): **Query Decomposition**(인텐트 분석) / **Entity Resolution**(청크 간 동일 개체 식별 — "여러분이 고민하게 될 난제": 2페이지부터는 상품명 없이 "이 상품은"으로만 등장) / **Answer Validation**(비즈니스 규칙 기반 답변 검증 — YTD 예시) / **Semantic Grounding**(있는 데이터에서만 가져오는가, 상상하는가)
7. Retrieval — 청크 RAG의 한계 실례 (45:09~46:01): 청크1에만 상품명이 있고 보수율(청크2)·투자위험(청크3)에는 없음 → "2차전지 테마의 보수율과 추적오차 위험" 질의에 유사도 검색만으로는 연결 불가
8. 해법 = **Federated Query** (46:01~46:37): "**벡터 검색 + 키워드 매칭 + 그래프 관계 검색 + 마지막에 가장 빠르고 강력한 SQL 필터** — 여러 소스를 융합". → *주: "Vector"는 별도 VectorDB 서버가 아니라 검색 채널로서의 벡터 유사도 검색을 의미. 저장 언급도 "벡터·RDB·그래프DB" 병렬(37:30)로, 벡터 인덱스면 충분*
9. 질의 처리 플로우 (46:59~49:33): **Query Understanding**(인텐트) → **Ontology Grounding**(온톨로지 대조로 정확한 엔티티·관계명 전환) → **Query Router**(무엇을 그래프/벡터/SQL 필터에 보낼지 + 어떤 순서가 빠를지 플랜·라우팅) → 검색 실행 → **통합 + 실데이터 존재·조건 충족 재확인** → 통과한 근거로만 LLM 답변 생성 → 왜 이 답변인지 로그가 남음
10. 채널별 강점 (49:57~51:17): SQL은 실행 성공 ≠ 정답(이름 LIKE "KOSPI" 매칭은 누락 발생) / **그래프는 RDB 스키마에 없는 값·관계를 커버**(비정형 유래 항목, "총보수 = 운용보수+기타보수" 같은 구성 관계) / **멀티홉 질의는 그래프가 가장 강력** — "에코프로의 상장 자회사를 편입한 ETF의 운용사"(4홉, 44:02~44:37)
11. **온톨로지는 문서가 아니라 런타임 통제 계층** — 추출·실행 시 LLM에 함께 주입하면 정확도 상승 (51:17)
12. NL2SQL은 스키마만 주면 정확도가 낮고 비즈니스 의미를 주면 크게 상승하나, 그 의미 계층 구축이 비싸다 — Palantir는 금융 석사급 50명 × 6개월 (41:46~42:12, 51:58). *우리는 사전 9종이 이미 그 의미 계층의 시드*

### C. NCP 크레딧·서버 안내 (네이버클라우드)

- **크레딧**: 계정 생성(신용카드 등록까지) → 크레딧 신청 페이지 → 쿠폰 메일 수령 → 마이페이지에서 쿠폰 등록. **20만원** + 별도 검색 신청으로 **웰컴 크레딧 10만원** 추가. **유효기간 9/30** — 만료 후 리소스가 남아 있으면 과금되므로 평가 종료 후 리소스 정리 필수 (02:49~05:33)
- 예상 사용액 ~15만원, 서버 월 4~7만원 (04:01, 11:16)
- **서버 구성 4단계**: VPC → Subnet → Server → ACG(방화벽). 권장 스펙 **2vCPU / 4GB / 디스크 20GB**(원본 데이터 1GB 미만이라 충분). ACG 인바운드: SSH(리눅스)/RDP(윈도우) + 평가용 **80/443**, 아웃바운드 전체 오픈(외부 데이터 수집용) (10:27~12:52)
- **HTTPS 평가 시 도메인 + 인증서 필요**(Global DNS + Certificate Manager). HTTPS 필수 여부·공인 IP 허용은 **운영 사무국 협의 후 재공지 대기** (08:35~09:49, 01:07:57~01:08:33)
- 모델: **"가급적 HCX-005를 쓰는 게 프롬프트·파인튜닝에 유리"** (17:02). CLOVA Studio(플레이그라운드·튜닝·익스플로러·스킬트레이너)는 실험·튜닝 도구이고, 실제 에이전트는 **API 키·시크릿으로 HCX와 직접 통신** (15:53~19:42)

### D. 전사 오류 의심 목록 — 확인 경로별 정리 (8/7: 기술 세션 PDF 없음 확정 → 경로 재배정)

| 항목 | 전사 표기 | 판단 | 확인 경로 |
|---|---|---|---|
| 위험등급 범위 | "한국 위험등급 구조상 1에서 5등급밖에 없습니다" (27:53, 32:12 반복) | 제공 데이터 실측은 **1~6등급 6단계** — 발화 단순화 또는 오전사로 보고 **데이터 실측(1~6)을 우선 신뢰** | 기술 세션 유래(PDF 없음) — **데이터 실측 우선 확정**, 필요 시 Q&A |
| NL2SQL 정확도 | "스키마만 주면 8.3%" (51:58) | 수치 전사 불안정 | 기술 세션 유래(PDF 없음) — **원문 확인 불가, 참고 수치로만 취급**(의사결정에 미사용) |
| 평가 문항 수 | "총 지리는 30만원 정도" (59:38) | 문맥상 **30문항**(상중하 각 10)의 오전사 | **규정·제출 PDF(추후 제공)** |
| 서버 메모리 | "2 cPU의 사기가 메모리" (11:48) | **4GB**(4기가) 추정 | NCP 세션 유래 — 규정·제출 PDF 또는 NCP 가이드 |
| 제안서 목차 5번 | "5번이 수상에 중요" (01:01:21) | 항목명 미상 — 과제소개서 권장 목차와 대조 필요 | **규정·제출 PDF(추후 제공)** |
| 트랩 문항 수 | "다섯 분항" (59:23 말미) | 5문항으로 판단 | **규정·제출 PDF(추후 제공)** |

---

## 확정 아키텍처 — Ontology + Knowledge Graph + Federated Retrieval 리빌딩 (8/7)

> **8/7 결정: Ontology와 Knowledge Graph를 필수 구성요소로 확정**하고(설명회 요구 + 팀 지시), 방법론을 아래 스택으로 리빌딩한다. 기존 "파라미터화 도구+FC 단독"·"GraphDB 배제" 판단은 폐기가 아니라 **재배치**다 — 결정성 원칙과 RDB/SQL 채널은 federated 검색의 한 축으로 그대로 유지된다.

### 스택 (Ontology + KG + Vector 검색 + GraphRAG + RDB + LLM)

| 계층 | 구성 | 역할 |
|---|---|---|
| **Ontology** (TTL 권장) | 클래스(Product / ETF / ETN / Bond / Fund / ManagementCompany / RiskGrade / Region / AssetClass / Index …) + 관계(운용하다↔운용되다 등) + 제약(disjoint, 등급 범위, 단일 운용사) | 개념·규칙. 용어 표준화 · 저장 전 무결성 검증 · 역관계/상위클래스 추론 · **런타임 통제 계층**(프롬프트 주입). **소스코드 제출물(필수)** |
| **Knowledge Graph** | 온톨로지를 적용해 정형(마스터 4종) + 비정형(외부 수집 문서)에서 추출한 트리플 인스턴스 | 실데이터 연결. 멀티홉 질의 · RDB에 없는 값(구성 종목 등) · 관계 검색 |
| **RDB** | 전처리 정정판 CSV 적재 (DuckDB vs PostgreSQL 재평가 — 조사 과제) | 정량 필터·정렬·집계 — "가장 빠르고 강력한 필터" 채널 |
| **Vector 검색** | CLOVA Embedding v2 + FAISS flat 유지 — **VectorDB 서버 불필요**(검색 채널로서의 벡터 유사도, 46:01 확인) | 의미 연관 텍스트 검색(전략 서술, 비정형 문서) |
| **Federated Query** | Query Understanding → Ontology Grounding → **Query Router**(채널 배분 + 실행 순서 플랜) → 결과 통합 | 질의별 최적 채널 조합. GraphRAG의 실행부 |
| **Answer Validation** | 비즈니스 규칙(YTD=1/1~7/11, 등급 범위, 존재 검증) + 근거 실재 확인 | 트랩 5문항 방어 · 환각 차단 — 배점 직결 |
| **LLM (HCX)** | 최종 답변 생성 + 인텐트 분석은 HCX 고정 구간. HCX-005(권장·튜닝 유리) vs HCX-007(FC·추론) 역할 분담은 조사 후 확정 | 근거 기반 생성. 전처리·비정형 추출 등 비평가 경로는 자유(A-6) |

### 기존 자산 재배치 — 낭비 없음

| 기존 자산 | 리빌딩 후 역할 |
|---|---|
| 해석 사전 9종 · taxonomy 매핑 계약 | 온톨로지 어휘(schema vocabulary)·클래스/관계 정의의 시드 — "비싼 의미 계층"(B-12)을 이미 보유 |
| 전처리 파이프라인(R1~R31 정정판) | RDB 적재와 KG 트리플 추출의 **공통 입력** — 저장소 무관 자산 |
| time_policy (as_of_date 이원화) | Answer Validation의 시간 규칙(YTD 범위 계산, 잔존만기 재계산) |
| FC Hello World · HCX provider 강제 | HCX 통신 계층 그대로 사용 (모델명 결정만 대기) |
| 자체 평가셋 설계(4장) | competency questions로 재편 — 30문항 미러링 + 트랩 5 |
| 8/4 결정성 리서치(LLM에 산술 금지, 검증된 SQL) | Query Router의 SQL 채널 설계 원칙으로 유지 |

### 조사 과제 — 8/8(금) 3인 총검토 전 준비

- [ ] **그래프 저장소 후보 비교**: rdflib(+SPARQL) / Neo4j Community / NetworkX+자체 인덱스 / (PostgreSQL 채택 시 Apache AGE) — 제약: NCP 2vCPU·4GB 서버 구동 + 응답 15초 목표 + 9/7~9/30 무인 운영
- [ ] **RDB 재평가**: DuckDB vs PostgreSQL — KG 저장소와의 통합 운영(프로세스 수·장애 포인트) 관점에서 재판단
- [ ] 온톨로지 검증: SHACL(pyshacl) 류로 "저장 전 규칙 검사" 구현 방안
- [ ] HCX-005 vs HCX-007: FC 지원·튜닝 가능·추론 모드·비용 → 역할 분담표
- [ ] **외부 비정형 데이터 소스 목록**: ETF 구성 종목(운용사 공시·PDF, **7/11 이전 스냅샷 확보 방법이 관건**), 상품 설명서·투자설명서 — 수집 우선순위와 저작권/기준일 검증 절차
- [ ] 임베딩: CLOVA Embedding v2 유지(규정 리스크 없음) — 비정형 문서 인덱싱 규모 추정

### 후속 작업

- [ ] **규정·제출 관련 추가 안내 PDF(추후 제공 예정) 수령 → D절 규정 항목 확정 + 기술제안서 뼈대 반영.** 기술 세션(Ontology/KG)은 자료 PDF가 없어 **이 장 B절(녹음본 기반)이 최종 기록** — 추가 확인은 Q&A·디스코드로
- [ ] 온톨로지 스키마 초안(TTL): 사전 9종·taxonomy 계약에서 어휘 추출 → competency question과 함께 설계
- [ ] 8/8(금) 3인 총검토 안건: 리빌딩 아키텍처 확정 · 그래프 저장소/RDB 선정 · HCX 모델 역할 분담 · 외부 비정형 수집 분담

---

## 0. 먼저 알아야 할 3가지

### ① 진짜 마감은 9/6이 아니라 8/30이다

과제소개서에 이렇게 적혀 있다.

> 마감 이후 커밋-push, 서버 배포 등 코드 및 결과물을 변경하는 행위 발견 시 **실격 처리**

그리고 평가는 9/7~9/30에 주최 측이 **우리 API 서버로 직접 GET 요청**을 보내는 방식이다. 즉 9/6 이후에도 서버는 살아있어야 하고, 고칠 수는 없다. 마지막 주에 급하게 배포하다 서버가 죽으면 복구 불가다.

→ **8/30 기능 동결(feature freeze), 9/6까지는 검증과 문서만.** 이 원칙 하나가 로드맵 전체를 결정한다.

### ② 예선은 "API 답변 40 + 기술제안서 40"의 이원 게임이다 [8/6 설명회로 확정]

평가 방식이 곧 요구사항이다. **8/6 설명회에서 배점과 문항 구조가 공개됐다** (상세 근거는 "8/6 설명회 확정 기록" 장 A절).

| 구분 | 내용 |
|---|---|
| **배점** | 총 100점 = **소스코드 20 / 기술제안서 40 / 평가 API 질의 답변 40** (59:23) |
| 정량평가(40) | **30문항(상·중·하 각 10) + 답변 불가 트랩 5문항**(점수 미포함·답변 시 감점). 전 팀 동일 문항, 순차 호출. **응답 소요 시간 평가 포함**(60초 이내 인정, 지향 15초). **멀티턴 없음** — 단일 질문·단일 답변 |
| 제안서(40) | **PDF + Word 동시 제출**. 온톨로지 구축 방법·활용 데이터 서술 필수. 권장 목차 5번 항목이 수상 단계 핵심 |
| 소스코드(20) | **도메인별 온톨로지 포함 필수**(TTL 권장, 형식 자유) |
| 결선 | 팀 선발 후 PT + **라이브 시연** (10월 오프라인) — UI는 예선 평가 대상 아님 |

예선에서 프론트엔드는 평가 대상이 아니다(01:10:19). 화면은 결선(10월) 시연용이므로 9월 이후에 만들어도 된다. 단 기존의 "예선 5주는 전부 백엔드 정확도" 전제는 수정한다 — **기술제안서가 API 답변과 동급(40점)이므로, 제안서를 매주 누적 작성하는 것이 백엔드 개발과 같은 비중의 작업이다.**

또한 응답 JSON 스키마가 아키텍처를 강제한다.

```json
{
  "question_id": "Q-001",
  "question": "평가 질의 원문",
  "retrieved_context": "답변 생성에 참고한 검색 문서",
  "think_trace": "사고·추론·도구 사용 과정",
  "answer": "최종 생성 답변"
}
```

`retrieved_context`와 `think_trace`를 요구한다는 것은 **에이전트의 도구 호출 과정이 처음부터 기록 가능한 구조**여야 한다는 뜻이다. 나중에 붙이려면 전체를 뜯어고쳐야 한다. 1주차부터 모든 도구 호출을 구조화 로그로 남기도록 설계한다.

### ③ 정성평가에 '리스크 관리' 항목이 있다

환각 방지, "확인할 수 없음" 명시, 조건 부족 시 역질문은 **선택 기능이 아니라 배점 항목**이다. 데이터 활용 규칙에도 필수/금지로 명시돼 있다.

- (필수) 답변은 데이터를 근거로 생성하고 **근거(참조 데이터)를 표시**
- (필수) 확인 불가한 질의는 **'확인할 수 없음' 명시 또는 필요한 조건 역질문**
- (금지) 데이터에 근거 없는 **수익률 전망·단정적 투자 추천 생성 금지**

→ 이건 3주차에 몰아서 붙이는 게 아니라, **응답 생성 레이어의 기본 동작**으로 1주차부터 설계한다.

---

## 1. 데이터 실측 결과

제공된 스키마 파일 4종을 직접 열어 확인한 내용이다. **여기가 이 과제의 난이도 대부분을 차지한다.**

| 테이블 ID | 상품군 | 건수 | 컬럼수 | 명명 규칙 | 한글 컬럼명 |
|---|---|---|---|---|---|
| PRBD01N001 | 국내채권 | 42,394 | 40 | `PD_NO` (대문자) | **전부 비어 있음** |
| PREF01N001 | 국내ETF | 1,734 | 73 | `pd_itm_no` (소문자) | 대부분 있음 |
| PREF02N001 | 해외ETF | 5,646 | 49 | `pd_itm_no` (소문자) | **전부 비어 있음** |
| PRFD01N001 | 공모펀드 | 95,619 | 45 | `itm_no` (소문자) | 예시값까지 완비 |

합계 약 145,393건. 데이터 기준일은 **2026-07-11 스냅샷** — 모든 답변에 이 기준일을 명시해야 한다. **[8/5 보강] 단, 추출일 ≠ 필드 기준일이다** (국내ETP 일간 데이터 ~06-15, 해외ETF 06-14~16, 채권 표준정보 중앙값 137일 지연). 근거 표기는 "스냅샷 추출일 + 해당 필드의 실제 기준일" 2단으로 하고, 잔존만기 같은 시간 파생값은 요청 시점 기준으로 재계산한다.

### 상품군별 함정

**국내채권 (가장 어렵다)**

- 컬럼 한글명과 예시값이 **스키마 파일에 전혀 없다.** `DUR`(듀레이션), `COV`(컨벡시티), `DIRTY`(더티프라이스), `NDY_*`(익일자 추정) 같은 약어를 팀이 직접 해석해 사전을 만들어야 한다.
- `ISU_DT`(발행일), `MAT_DT`(만기일), `CRD_GRD_DT`가 **`double precision` 타입**이다. 날짜가 숫자로 들어있다 → 날짜 연산(잔존만기 등) 전에 캐스팅 필수. ~~`REMAINING_DAYS`가 이미 있으니 우선 활용.~~ **[8/5 정정] `REMAINING_DAYS` 저장값의 기준일은 추출일이 아니라 행별 `PD_STD_INFO_UPDATE`(중앙값 137일 지연)라 그대로 쓰면 틀린다. 잔존만기는 요청 시점에 `MAT_DT`로 재계산한다.** 추가 센티널: `ISU_DT=0` 336건, `MAT_DT=0` 316건.
- 수익률 컬럼이 7종(`BUY_YIELD`, `CORP_PRETAX_YIELD`, `AFTER_TAX_YIELD`, `PREF_TAX_YIELD`, `AVG_ANNUAL_TAX_YIELD`, `DEPO_EQUIV_YIELD_154` 등)이고, 과제소개서상 **매수 가능 종목 일부만 수록**. **8/4 실측: 결측 97.9% — 수익률이 있는 종목은 약 881건(2.1%)뿐.** "수익률 높은 채권" 질의는 사실상 이 매수가능 풀 안에서만 랭킹된다. 단 `APPLIED_YIELD`(결측 25%)·`NDY_APPLIED_YIELD`(32%)는 대부분 채워져 있어 대체 지표로 검토.
- **8/4 실측 추가:** `MAT_DT` 최대값 99991231 — 영구채 센티널 값. 잔존만기 계산 전 예외 처리 필요. `CRD_GRD`(신용등급)도 41.6% 결측 — "AA 이상" 필터 시 NULL 규칙 필요.
- **[8/5 dev-kyung 반영]** 추출일 기준 **이미 만기 도래 16,180건(38%) + 만기 불명 322건** — 기본 검색은 만기·불명 제외, 사용자가 과거 상품을 요구할 때만 포함. `AVG_ANNUAL_TAX_YIELD`는 비결측 881건이 전부 0이라 사용 불가. `XS3067881758` 1건은 비-KR ISIN 국제채권이라 국내채권 기본 검색에서 제외(예외 태깅). `APPLIED_YIELD > 100`인 66건은 다수가 C등급 유동화채라 오류 단정·절삭 금지.

**국내ETF (컬럼은 친절, 값이 지저분)**

- `cu_base_index`(기초지수)가 **공백 40칸 문자열**로 채워진 행이 다수다. `NULL`이 아니라 `'          ...'` 이므로 `IS NULL` 필터가 통하지 않는다. → 적재 시 `TRIM` 후 빈 문자열은 NULL로 정규화. **8/4 실측: 공백 1,551건 + NULL 125건 = 실질결측 96.7%, 값이 있는 종목의 고유 지수는 19개뿐.** 기초지수 질의는 국내ETF에선 거의 답변 불가(→ "확인할 수 없음" 케이스), 해외ETF는 결측 0%라 가능.
- `cu_charge_rt`(총보수요율)는 값이 없는 행이 많다. **8/4 실측: 실질결측 87.5%(1,517/1,734건).** **[8/5 정정] 값 있는 217건 중 150건이 0(무보수인지 미수집인지 미확정) — 신뢰 가능한 양수는 67건뿐이고 ETN 532건은 값 자체가 없다.** "총보수 낮은 국내ETF" 랭킹은 이 67건 풀에서만 가능 — 답변에 이 한계를 명시해야 한다.
- **8/4 실측 추가:** 100% 결측 컬럼 6개(`nru_mkt_diff_rt`, `nru_mkt_inav`, `pd_dvid_cycl`, `pd_sect_nm`, `ru_mkt_price`, `ru_mkt_volume`) — 적재 시 제외하고 컬럼 사전에 "미제공"으로 기록.
- `du_bpr`, `du_clpr` 등이 스키마상 `numeric`인데 실제로는 **문자열 `'13675.00'`**로 들어온다 → 전 컬럼 타입 캐스팅 필요.
- **[8/5 dev-kyung 반영]** ① 이 파일의 30.7%는 ETF가 아니라 **ETN**(ETF 1,201 + ETN 532) — `pd_grp_no` 기반 `instrument_type` 분리 필수. ② **1,155행은 손상 행**(`pd_itm_no='KR'`, 이름 `'.'`) — 같은 파일 299행 `KR70193M0005`의 손상 중복이라 격리해도 유실 없음. ③ **거래종료 212건 혼재** — 기본 검색 대상은 미종료+미정지 1,520건. 종료 167건은 종가 0이고 **기간수익률 `-100`은 이 0 종가에서 파생된 가짜 손실**이라 랭킹 제외. ④ 기초지수의 활성 ETF 실효 커버리지는 **2/1,140(0.18%)**. ⑤ `pd_tr_yn`은 '거래정지 여부'(1='정지') — 해석 반전 주의. ⑥ ETN은 `du_last_aum`이 전부 0 — AUM은 `pd_net_tamt` 사용. ⑦ 일간 데이터 기준일은 주로 06-15로 추출일보다 26일 이전.
- 반면 수익률은 `du_er_1d/1m/3m/6m/1y/ytd`로 잘 갖춰져 있고, `pd_risk_nm`("다소높은위험(3등급)"), `pd_pen_tr_yn`(연금거래가능), `wu_inv_ast_type`(투자자산군), `wu_inv_rgn`(투자지역)이 있어 조건 검색에 바로 쓸 수 있다.

**해외ETF (유일하게 벡터 검색이 필요한 지점)**

- 한글 컬럼명이 전혀 없고, `cu_strtegy`(운용전략)가 **영문 서술 텍스트**다.
- 반대로 `cu_charge_rt`(총보수, **8/4 실측 결측 0%**), `du_last_aum`(AUM, 결측 3.3%)는 잘 채워져 있어 **정량 비교 질의에 가장 적합한 테이블**이다. 단 `cu_base_index`(기초지수)는 **8/4 재실측 정정: NULL은 0%지만 "Index is not provided by Management Company"(1,984건) 등 센티널 문자열이 약 48%** — 기초지수 질의의 절반은 해외ETF에서도 '확인할 수 없음' 대상이다. 적재 시 센티널→NULL 정규화 필수([external_data/COLLECTION_SUMMARY.md](external_data/COLLECTION_SUMMARY.md) 3장).
- **8/4 실측 정정:** `cu_lev_fector`(배수)는 **전량(100%) 결측**이라 쓸 수 없다. `cu_inverse_short_yn`(인버스)·`cu_etn_yn`·`wu_core_yn`은 결측 97~99%에 고유값이 'Y' 하나 — **플래그성 컬럼으로, 채워진 것만 Y이고 NULL은 N으로 해석**하는 규칙이 필요하다(값 사전에 기록). 레버리지 탐지는 `cu_strtegy` 텍스트("2x", "leveraged")나 종목명 패턴으로 보완해야 한다.
- 4종 중 자연어 임베딩이 의미 있는 유일한 필드가 `cu_strtegy`다. 벡터 DB는 여기에만 쓴다. (dev-kyung 실측: 5,638건 보유·고유 5,566건 — 의미 검색 적합성 재확인)
- **[8/5 dev-kyung 반영]** ① 여기도 **ETN 59건 혼재**(ETF 5,587). ② `pd_isin_cd`는 공백 9행 + **중복 50그룹**이라 키로 부적합 — 키는 `pd_itm_no` 유지. ③ 핵심 필드가 대부분 빈 희소 행 8건은 기본 랭킹에서 제외 태깅. ④ **위험등급 컬럼이 아예 없다** — "해외ETF 중 저위험" 질의는 확인 불가 케이스. ⑤ 총보수 0인 363건은 실제 무보수인지 미수집인지 미확정 — 관측값으로 보존하되 8/6 질문. ⑥ 일간 데이터 기준일 06-14~16, 30일 초과 지연 252행 — 가격·거래량 답변에 신선도 명시.

**공모펀드 (가장 크고, PK가 함정)**

- 95,619건으로 전체의 66%. 컬럼 한글명·예시값이 완비돼 있어 가장 다루기 쉽다.
- PK가 `itm_no` + `prfd_attr_cd` + `zrin_fd_ivst_risk_gcd` **복합키**다. **8/4 실측: 복합키 중복 0건(PK 성립 확인).** **[8/5 정정] 유효 `itm_no`는 11,138개(손상 1행의 `itm_no='"'` 제외), 그리고 `(itm_no, prfd_attr_cd)` 2컬럼만으로 유일성이 성립한다 — 동일 `itm_no` 그룹에서 달라지는 컬럼은 `prfd_attr_cd` 하나뿐(dev-kyung 검증). DB는 `fund_master`(11,138) / `fund_attribute`(95,618, 2컬럼 키) 분리가 정석.** 평균 8.6개 행이 같은 펀드라 "순자산 상위 10개 펀드"를 물으면 같은 펀드가 수 번 나올 수 있다. `rptt_ksd_itm_no`(대표종목)는 99.9% 채워져 있어 클래스 대표 선정 규칙에 쓸 수 있다.
- **8/4 실측 추가:** 수익률 결측이 기간이 길수록 커진다(1M 27.6% → 1Y 32.6% → 5Y 46.8%). 장기 수익률 랭킹 질의는 모수 절반이 빠진다는 한계를 답변에 명시.
- 수익률이 `fd_wk1 / mm1 / mm3 / mm6 / mm18 / yr1 / yr2 / yr3 / yr5`로 9종. ETF(`du_er_*`)와 **기간 체계가 다르다** — 교차 비교 시 공통 기간(1M/3M/6M/1Y)만 매핑.
- **보수 정보 미포함.** "보수 낮은 펀드" 질의는 답변 불가 → "확인할 수 없음"으로 처리해야 하는 대표 케이스다.
- **[8/5 dev-kyung 반영]** 1개 상품(`KCGI베트남증권투자신탁`)의 18M·2Y·3Y·5Y 수익률이 -3,593%~-4,381% — -100% 미만은 불가능하므로 단위 오류 후보로 **null 처리**(추정 복구 금지, 원본 값은 품질 이력 보존). `or_attr_desc='06'`(5,436행)은 상품명 678건에 '파생'이 있어 파생형 코드 후보 — 자동 확정하지 말고 8/6 코드표 질문과 연결.

### 가장 중요한 발견: `axis_*` 컬럼

schema 파일의 `Sheet2_Sample` 시트에는 실제 데이터에 **없는** 컬럼들이 붙어 있다.

| 상품군 | Sample에만 있는 축 컬럼 |
|---|---|
| 국내채권 | `axis_issuerType`, `axis_maturityClass`, `axis_couponType`, `axis_creditRating`, `axis_collateralType`, `axis_currency`, `axis_issuanceMarket`, `axis_issuerCategory` |
| 국내ETF | `axis_assetType`, `axis_region`, `axis_strategy`, `axis_replicationMethod`, `axis_leverageType`, `axis_underlyingScope`, `axis_distributionType` |
| 공모펀드 | `axis_fundType`, `axis_redemptionType`, `axis_issuanceType`, `axis_listingType`, `axis_classDifferentiation`, `axis_investorEligibility` |
| 해외ETF | 없음 (원본 49컬럼 그대로) |

국내ETF의 실제 `datarows` 파일을 열어 확인한 결과 **컬럼 73개 = 스키마 73개, `axis_*` 없음**이었다.

**해석:** 주최 측이 "상품군을 넘나드는 교차 질의를 하려면 이런 공통 분류 축이 필요하다"는 힌트를 예시로 남긴 것이다. 그리고 그 축을 만드는 일은 참가팀 몫이다. 과제 요구사항 1번이 "팀이 판단한 최적의 방식으로 구조화"인 이유가 여기 있다.

→ **이 통합 축 테이블(`product_axis`)을 만드는 것이 이 과제의 핵심 차별점이다.** 채권·ETF·펀드를 같은 언어로 질의할 수 있게 만드는 유일한 방법이고, 기술제안서에서 가장 설득력 있는 챕터가 된다. **[8/7] 이 역할은 리빌딩 후 온톨로지의 공통 클래스/속성 정의로 흡수하는 방향으로 재검토한다** — "상품군을 같은 언어로 질의"라는 목표 자체가 온톨로지의 용어 표준화와 동일한 문제이며, `axis_*` 힌트는 온톨로지 클래스 설계의 입력이 된다(8/8 확정).

### 외부 데이터 수집 전략 — 3-Tier (8/4 확정 → **8/6 설명회로 대개정**)

데이터 활용 규칙(수집 **가능** / 제공 데이터가 평가 **기준** / 상충 시 제공 데이터 **우선**)은 유지되나, 8/6 설명회에서 결정적 사실이 추가됐다: **"제공 RDB에 없는 것들을 비정형에서 온톨로지를 활용해 추출한 다음 답변으로 연결하는 게 이 과제의 핵심"이며, ETF 구성 종목 질의가 출제된다**(57:22~58:00). 즉 외부 데이터는 '해석 메타데이터'에 그치지 않고 **답변 커버리지 자체를 결정하는 핵심 과제**다. 단 **7/11 이전 기준 데이터만 인정**된다(01:13:49).

| Tier | 대상 | 방침 |
|---|---|---|
| **1 — 적극 수집 (완료)** | 채권 용어·약어 사전(`DUR`·`COV`·`DIRTY`·`NDY_*`), 신용등급·위험등급 체계, 통화·시장 코드표, 별칭 사전, 기초지수 명칭 정규화 | 해석 메타데이터 — **온톨로지 어휘(schema vocabulary)로 승격**. semantic layer +17~23%p 근거 유지 |
| **2 — 핵심 과제로 격상 [8/6]** | **제공 RDB에 없는 정보의 비정형 수집**: ETF 구성 종목(출제 예고), 상품 설명서·투자설명서, 결측 필드(펀드 보수, 기초지수 등) | 온톨로지 기반 추출 → KG 트리플로 연결. **조건 3가지 준수**: ① 7/11 이전 기준 데이터만 ② 상충 시 제공 데이터 우선 ③ 출처·기준일을 근거에 명시. 제공 데이터에 없는 종목 자체는 추가해도 검색 대상이 아님(58:00) |
| **3 — 금지 (유지)** | 시세·수익률 등 시계열 대체 데이터, **7/11 이후 생성 데이터**(9월 실적 보고서 등) | 기준일 스냅샷과 상충 → 평가 리스크만 증가. "7/11 이후 상품을 답하면 감점"(57:00) |

→ 수집 방향 전환: "메타데이터만 좁고 깊게" → **"메타데이터(완료) + 출제 예고된 비정형 정보(구성 종목 우선)를 기준일 검증과 함께"**. 수집 소스 목록은 "확정 아키텍처" 장 조사 과제 참조.

---

## 2. 아키텍처 설계

### 왜 Vector RAG 단독으로는 진다

평가문제는 이런 형태로 나올 가능성이 높다.

> "총보수 0.3% 이하인 미국 주식형 ETF 중 AUM 상위 5개를 알려줘"

정렬·필터·집계는 임베딩 유사도로 풀 수 없다. 14.5만 건을 청킹해 벡터 DB에 넣으면 top-k 검색이 정답 행을 놓친다. 숫자 조건은 **결정적(deterministic) 연산**이어야 한다.

### 왜 Text-to-SQL 단독도 위험한가

전체 컬럼이 200개가 넘고, 상품군마다 명명 규칙이 다르고(`PD_NO` vs `pd_itm_no` vs `itm_no`), 한글명조차 없는 테이블이 둘이다. LLM에게 스키마 전체를 주고 SQL을 짜게 하면 **존재하지 않는 컬럼을 지어내는 환각**이 반드시 발생한다.

8/4 리서치로 이를 정량 확인했다.

- Text-to-SQL SOTA조차 BIRD 벤치마크에서 **71~77%** (CHASE-SQL 76.0%, XiYan-SQL 75.6%, AskData 77.1% / 인간 약 93%) — GPT-4o급 모델 + 다단계 앙상블 파이프라인 기준. HCX-007 단독 자유 SQL 생성은 이보다 낮다고 봐야 하며, 비공개 평가에서 오답 20~30%를 감수할 이유가 없다.
- 반대로 스키마에 **의미 계층(컬럼 사전·값 매핑·동의어)**을 얹으면: BIRD external knowledge 유무로 GPT-4가 34.9% → 54.9% (**+20%p**), Cube 벤치마크에서 프론티어 모델 3종 모두 **+17~23%p**. → S1의 컬럼 사전·값 사전이 곧 성능이다.
- 금융 DB 질의 연구(FinAI Data Assistant, 2025)의 결론: 자유 SQL 생성 대신 **사전 검증된 파라미터화 쿼리 라이브러리 + Function Calling 라우팅**이 결정성·감사가능성·지연·비용 모두 우위.
- 프로덕션 사례도 동일 — Uber QueryGPT는 의도 분류 → 도메인별 큐레이션 workspace로 좁힌 뒤에만 생성, LinkedIn SQL Bot은 벡터 검색 + LLM 리랭크로 테이블 후보를 7개까지 압축. 공통 교훈: **검증된 좁은 인터페이스로 제한하라.**

### ~~권장안~~ [8/7 대체됨]: 정형 우선 하이브리드 (Tool-calling Agent)

> **[8/7]** 이 절의 "도구 5종 + FC 단독" 권장안은 **"확정 아키텍처" 장의 Ontology+KG+Federated Retrieval로 대체됐다.** 단 이 절의 핵심 원칙 — SQL은 사전 작성·검증하고 LLM은 파라미터만 채운다, 환각 SQL의 구조적 차단, 도구 호출의 구조화 로깅 — 은 **SQL 채널 설계안으로 그대로 유지**된다. 아래 도식은 그 원칙의 기록으로 보존.

```
[질의]
  │
  ├─ ① 의도·상품군 라우팅 (HCX-DASH-002, 저비용)
  │
  ├─ ② 조건 추출 → 정규화된 JSON 파라미터
  │      "미국 주식형" → {region:"US", assetType:"EQUITY"}
  │      "총보수 0.3% 이하" → {expenseRatioMax:0.3}
  │
  ├─ ③ 도구 실행 (HCX-007 Function Calling)
  │      ├─ search_products(필터+정렬+상위N)   ← SQL은 우리가 작성, LLM은 파라미터만
  │      ├─ get_product_detail(상품번호)
  │      ├─ compare_products(상품 리스트, 비교축)
  │      ├─ aggregate_products(집계함수, group by)
  │      └─ search_by_text(해외ETF 운용전략 시맨틱 검색)  ← 유일한 벡터 검색
  │
  ├─ ④ 근거 조립: 구조화 retrieved_context (출처 테이블·행 키·사용 컬럼·값·필드별 기준일)
  │      공식 스키마가 문자열을 요구하면 어댑터에서 손실 없이 직렬화
  │
  └─ ⑤ 답변 생성 + 답변 가능성 판정 (8/5 dev-kyung 상태 머신 채택)
         · answerable / partial_coverage / unsupported_field / ambiguous_condition
           / insufficient_condition / no_matching_rows / stale_or_inactive / invalid_data
         · coverage 분모 = 사용자 조건 적용 후 eligible 집합
           ("조건에 맞는 N개 중 값 있는 M개만 비교"를 답변에 명시)
         · 전망·단정 추천 표현 필터링 + "추천합니다" 대신 "조건에 부합하는 상품은 …"
```

**핵심 아이디어:** LLM이 SQL 원문을 작성하게 하지 않는다. SQL은 우리가 미리 작성해두고, LLM은 **JSON 파라미터만 채운다.** 이렇게 하면 환각 SQL이 구조적으로 불가능해지고, `think_trace`에 도구 호출 내역이 그대로 남는다.

### 오케스트레이션 패턴 (8/4 확정 → **[8/7] 재확정 대상**)

> **[8/7]** 메인 모델(HCX-005 vs HCX-007)과 오케스트레이션 형태(ReAct 루프 → Query Understanding·Ontology Grounding·Query Router 플로우)는 8/8 총검토에서 재확정한다. 아래의 "최대 반복 제한·경량 플래너 분기·상태 그래프 로그=think_trace" 원칙은 Query Router 설계에 계승된다.

ReAct vs Plan-and-Execute 비교 연구의 결론: ReAct 루프는 단순·적응적이지만 실패 시 같은 행동을 반복하며 갇히는 약점이 있고, P&E는 다단계 질의에서 경로가 안정적이다. 우리 도구는 결정적(SQL)이라 실패 모드가 적으므로:

- **기본**: HCX-007 Function Calling 기반 ReAct 루프 + **최대 반복 횟수 제한**(무한 루프 차단)
- **난이도 '상' 교차 질의만**("채권·ETF·펀드 각 1개 비교" 류): 경량 플래너로 분기 — 질의를 서브 질의로 분해 → 도구 병렬 실행 → 결과 병합
- LangGraph 상태 그래프 하나에서 두 패턴이 분기되고, 노드 전이 로그가 그대로 `think_trace`가 된다 (Agentic RAG Survey의 single-agent router + tool-use + guardrail 패턴에 해당 — 이 과제 규모에서 multi-agent는 지연·복잡도만 늘린다)

### 옵션: 제한된 Text-to-SQL fallback → [8/7] SQL 채널 구현안으로 흡수 (8/8 총검토에서 dev-kyung 안과 비교)

도구 5종으로 커버되지 않는 롱테일 질의가 예시 set에 보이면, 2단계 fallback으로 **제한된 Text-to-SQL**을 추가한다: read-only 뷰 + 컬럼 화이트리스트 + SQL 파서 검증 + 실행 오류 시 self-correction 1회 (CHASE-SQL의 "후보 생성 → 실행 검증 → 선택"의 미니 버전). **도구로 커버되면 붙이지 않는 게 정확도상 이득이다.**

### 기술 스택 (8/4 갱신 — 성능 최우선 기준 · **[8/7] 리빌딩으로 부분 재평가**)

> **[8/7]** 아래 표는 8/4 시점 기준이다. 리빌딩 후: DB 행은 "RDB 채널"로 역할 축소·재평가(DuckDB vs PostgreSQL — "확정 아키텍처" 장 조사 과제), **그래프 저장소 행이 신설 대상**, 벡터·리랭킹·임베딩·API·배포 행은 유효, LLM 행은 HCX-005 권장 반영해 재검토, Agent 행(LangGraph)은 Query Router 오케스트레이션으로 역할 변경 검토.

| 레이어 | 선택 | 이유 |
|---|---|---|
| DB | **DuckDB** (read-only 모드, SQLite는 대안) | 컬럼너 저장으로 집계·정렬 분석 쿼리에 최적. in-process 파일 하나라 Docker 재현성은 SQLite급이고 read-only 다중 접근 가능. 엑셀/CSV 직접 로드로 적재도 짧다. 단, 145K건은 SQLite도 ms 단위 — 실질 선택 기준은 분석 SQL 편의성(QUALIFY·윈도우 함수)과 ETL 편의성 |
| 적재 | pandas/DuckDB → 정규화 스크립트 | 타입 캐스팅·NULL 정규화 포함. **멱등성** 확보(재실행해도 같은 결과) |
| 종목명 검색 | **별칭·정규화 사전 + LIKE/FTS** | 종목명 검색은 형태소 분석보다 별칭 정규화("타이거"=TIGER=미래에셋)가 결정적. 필요 시 FTS5 + `kiwipiepy` 병행 |
| 벡터 | CLOVA **Embedding v2** + **FAISS flat(정확 검색)** | 해외ETF `cu_strtegy` 5,646건뿐 — 근사 인덱스·벡터 DB 서버(Qdrant/Milvus)는 수백만 벡터부터 의미 있음. flat 검색이 밀리초에 끝나고 정확도 손실 0 |
| 리랭킹 | **CLOVA Reranker 2단계** | 벡터 top-20 → 리랭크 top-5. 검색 성능을 올리려면 DB 교체가 아니라 이걸 붙인다 |
| LLM | **HCX-007** (메인), HCX-DASH-002 (라우팅·조건추출) | 필수 제약. 규칙 우선 + LLM 보조로 **질의당 호출 2~3회 제한** |
| Agent | **LangGraph + langchain-naver(ChatClovaX)** | 공식 통합(`bind_tools` 지원, `max_tokens ≥ 1024` 동일 적용). 상태 그래프 전이가 그대로 `think_trace`, 가드레일 분기(확인불가/역질문)가 구조화됨. 노드 단위 모듈화로 3인 브랜치 부분 통합 가능 |
| API 서버 | FastAPI | `GET /answer` 하나. 동일 질의 캐싱·재시도 큐 포함 |
| 배포 | 네이버 클라우드 (Server/Container) | 대회 크레딧 제공. 상시 가동 필수 |

**GraphDB 배제 판단은 8/7 폐기 — Ontology + KG 채택 확정** ("확정 아키텍처" 장 참조). 8/4 배제 논리("평면 마스터 4개 + 1홉 관계, 필터·정렬·집계 중심 = SQL 지배 워크로드")는 **제공 정형 데이터만 볼 때는** 여전히 사실이다. 그러나 8/6 설명회에서 전제가 바뀌었다: ① 온톨로지가 소스코드 제출 필수 요건이고 ② 출제가 예고된 구성 종목 등 **비정형 유래 정보가 들어오는 순간 데이터는 더 이상 1홉 평면이 아니며**(멀티홉 예시: "에코프로의 상장 자회사를 편입한 ETF의 운용사"), ③ 채점이 기대 엔티티·릴레이션 포함 여부로 이루어진다. 따라서 SQL 채널의 결정성 우위는 federated 검색의 한 축으로 유지하되, 그래프는 비정형 확장·멀티홉·근거 추적을 담당한다. 기술제안서에는 "8/4 SQL-only 검토 → 8/6 요구 변화 → federated 재설계" 의 판단 변경 과정 자체를 쓰면 검토 깊이를 보여주는 가점 요소가 된다.

### HyperCLOVA X 확인 사항

과제소개서상 **HyperCLOVA X 외 다른 LLM을 쓰면 평가 대상에서 제외**된다. 리서치로 확인한 제약과 기능은 다음과 같다.

| 항목 | 내용 |
|---|---|
| Function Calling 지원 모델 | **HCX-007, HCX-005, HCX-DASH-002** |
| Function Calling 미지원 | HCX-003, HCX-DASH-001 (튜토리얼에 자주 나오는 구버전 — 쓰면 안 됨) |
| HCX-007 | 128K 컨텍스트, **하이브리드 추론 모델** → 메인 에이전트로 적합 |
| HCX-005 | 128K, 멀티모달(이미지 해석) |
| HCX-DASH-002 | 32K, 경량·고속 → 라우팅/조건 추출용 |
| FC 엔드포인트 | `POST /v3/chat-completions/{modelName}`, `tool_choice`: auto / none / 특정 지정 |
| **FC 제약** | `maxTokens` 또는 `maxCompletionTokens`를 **1024 이상**으로 설정해야 함. 튜닝 모델은 FC 미지원 |
| Embedding v2 | `POST /v1/api-tools/embedding/v2`, 최대 8,192 토큰, **1,024차원** |
| 그 외 유용한 API | **Reranker**, RAG Reasoning, Segmentation, Router, Summarization — 직접 구현 대신 활용 검토 |
| OpenAI 호환 | 주요 API에 OpenAI SDK/API 호환 제공 → LangChain `ChatClovaX` 연동 가능 |

> **[8/6 설명회 확정]** "가급적 HCX-005를 쓰는 게 프롬프트·파인튜닝에 유리"(17:02) + HCX 기반 튜닝은 자유·비용 자부담(01:09:00). 단 기존 리서치상 **튜닝 모델은 FC 미지원** — 튜닝 채택 시 도구 호출(비튜닝 모델)과 답변 생성(튜닝 모델)의 역할 분리가 필요하다. HCX-005 vs HCX-007 역할 분담은 "확정 아키텍처" 장 조사 과제로 8/8 총검토에서 결정.

### 타 LLM 사용 가능 여부 판정 (8/4 확정 → **8/6 설명회로 완화 확정**)

과제소개서 원문은 조건 없는 단정형("HyperCLOVA X만 사용 가능")이었으나, **8/6 설명회에서 취지가 공식 완화됐다**(01:09:42~01:10:19): *"모든 부분을 다 하이퍼클로바 모델을 사용하라는 취지는 아니다. 최종적인 답변 생성이라든가 메인 에이전트 구성에 있어서 하이퍼클로바를 적극 활용하라는 것이고, 데이터 전처리라든가 이런 부분에서는 다른 방법들도 자유롭게 사용해도 상관없다."*

| 구분 | 판정 | 근거 |
|---|---|---|
| **최종 답변 생성 · 메인 에이전트 구성** | **HCX 고정** (필수) | 설명회 명시(55:44, 01:09:51). 인텐트 분석도 HCX 사용 안내(56:07) |
| 데이터 전처리 · 비정형 분석·정제 · KG 구축 등 오프라인 단계 | 자유 (비-HCX 도구·LLM 허용) | 설명회 명시(01:09:51~01:10:19, 56:07 "널리지 베이스로 만드는 부분까지는 자유롭게") |
| Retrieval 자체 | LLM 불필요 — "4번(retrieval)은 사실상 LLM을 쓸 필요 없다. 다양한 기법이 많다"(56:07) | 검색은 결정적 엔진으로 |
| OpenAI **SDK**를 CLOVA 호환 엔드포인트에 연결(모델은 HCX) | ✅ 가능 | CLOVA Studio OpenAI 호환 API 공식 제공 |
| 임베딩 모델 | Embedding v2 유지 권장 | 완화 발언이 임베딩을 명시하진 않음 — 논란 여지를 없애는 보수적 선택 유지 |

→ 운영 원칙: **평가 API가 실행하는 온라인 경로(질의 이해→답변 생성)는 CLOVA Studio(HCX / Embedding v2 / Reranker)로 통일**하고, 오프라인 파이프라인(전처리·비정형 추출·KG 구축·자체 평가)은 도구 자유. 이 구분을 기술제안서에 명시해 규정 준수를 보인다.

> **잔여 확인 사항:** 호출량 제한(RPM/TPM) — 평가 기간 레이트 리밋은 여전히 실격 리스크(순차 호출 확인됨(01:04:45)이라 부담은 완화). 크레딧은 확정: 20만원 + 웰컴 10만원, 9/30까지("8/6 설명회 확정 기록" C절).

---

## 3. 5주 스프린트 로드맵

각 스프린트는 일요일 종료 기준이다.

> **[8/7 리빌딩 반영]** 아키텍처가 "확정 아키텍처" 장의 스택(Ontology + KG + Federated Retrieval)으로 바뀌면서 스프린트별 중심 작업을 아래처럼 재조정한다. 세부 작업표의 개별 항목 중 구 아키텍처 전제(도구 5종 단독, product_axis 단일 테이블 등)는 8/8(금) 3인 총검토에서 확정 후 갱신한다.
>
> | 스프린트 | 리빌딩 후 중심 작업 |
> |---|---|
> | S1 (8/10~8/16) | 온톨로지 스키마(TTL) 초안 + 사전 9종→어휘 승격 · RDB 적재(정형) · **비정형 수집 1순위(ETF 구성 종목) 착수** · KG 추출 파이프라인 골격 |
> | S2 (8/17~8/23) | Federated Retrieval(그래프/벡터/SQL 채널 + Query Router) · Query Understanding·Ontology Grounding(HCX) · Answer Validation · `/answer` API E2E 관통 |
> | S3 (8/24~8/30) | 평가셋(30문항 미러 + 트랩) 회귀 · 응답시간 15초 최적화 · 브랜치 교차평가·통합 · 기능 동결 |
> | S4 (8/31~9/6) | **기술제안서(배점 40) 완성** — PDF+Word, 온톨로지 구축 서술 필수 · 안정성 · 제출 |

### Sprint 0 — 8/2(일) ~ 8/9(일) · 킥오프와 설명회

**D-35 ~ D-28**

가장 중요한 이벤트는 **8/6(목) 오프라인 설명회**였다 — 참석 완료, 녹음본 전문을 "8/6 설명회 확정 기록" 장에 반영했다.

- [x] **8/6(목) 13:00~17:30 네이버 그린팩토리 설명회 — 참석 완료** · 녹음본 전문 반영("8/6 설명회 확정 기록" 장). **기술 세션 자료 PDF는 없음(음성이 유일 기록) — 규정·제출 관련 추가 안내만 추후 PDF 제공 예정**
  - 확보 결과: 평가 구조(배점 20/40/40, 30문항+트랩5, 응답시간) · 크레딧(20만+웰컴10만) · 온톨로지 제출 필수 · 참고 질의 예시(실제 출제 아님·트랩 예시 공개). 별도 "예시 질의 set" 파일은 배포되지 않음
- [ ] 디스코드 채널 참가 (`https://discord.gg/qjCUvN4eWR`) — 주제별 Q&A가 여기서 진행됨
- [ ] 주최 측 GitHub Organization private repo 접근 확인
- [ ] 네이버 클라우드 계정 생성 + CLOVA Studio 신청 + 크레딧 확인
- [x] HCX Function Calling **"Hello World"** — **코드 완료(8/6~7)**: `agent/fc_hello.py`(더미 도구 FC 왕복, --dry-run) + `agent/clova_client.py`(HCX 단일 provider 강제·감사 로그) + mock 테스트 통과. **실 호출 검증은 API 키 발급 후**(NCP 계정·크레딧 신청 선행)
- [x] 데이터 4종 엑셀 → pandas 로드, 각 테이블 결측률·고유값 프로파일링 (**8/4 완료** — `preprocessing/profiling/` 산출물 참조)
- [ ] **[8/5] 브랜치별 설계 가설 확정 + 서로 다른지 확인**, 공통 소유 3종(계약 4종·평가셋 러너·배포 골격) 담당자 지정 (5.1~5.2절)

**완료 기준(DoD):** HCX-007이 우리가 정의한 더미 함수를 호출해 응답을 돌려줬다. **3인의 설계 가설이 서로 다르고 공통 계약 4종이 고정됐다.** ~~4종 데이터의 컬럼별 결측률 표가 있다.~~ ✅ 8/4 완료

---

### Sprint 1 — 8/10(월) ~ 8/16(일) · 데이터 레이어

**D-27 ~ D-21** · 이 스프린트가 프로젝트의 성패를 가른다.

> **[8/7 갱신]** DB 적재·`product_axis`는 8/6 지시로 보류됐다가, 리빌딩 확정으로 **"RDB 채널 적재 + 온톨로지/KG 구축"의 2트랙**으로 재편된다(저장소 선정은 8/8 총검토 — "확정 아키텍처" 장 조사 과제). 전처리 정정·컬럼/값 사전·time_policy 등 **저장소 무관 작업은 유효하며 계속 진행** — 정제 데이터와 표준화 어휘는 RDB 적재와 KG 추출의 공통 입력이다. `product_axis`(통합 축)의 역할은 온톨로지의 클래스·속성 정의로 흡수되는 방향으로 재검토한다.

| 작업 | 내용 |
|---|---|
| 적재 파이프라인 | 엑셀 4종 → **DuckDB**. 타입 캐스팅(문자열 숫자 → numeric, `double precision` 날짜 → date), 공백 문자열 → NULL 정규화. **재실행 가능한 단일 스크립트** + **[8/5] 단위 테스트·2회 실행 결정성(SHA-256 동일) 검증** |
| **[8/5] 전처리 정정분** | 국내ETP 1,155행(손상 중복) 격리 규칙 · `instrument_type`(ETF/ETN) · `listing_status`(거래종료 212건) · 종료 상품 `-100` 수익률 무효화 · 채권 만기도래 16,180건 재계산(`MAT_DT=0` 제외) · 공모펀드 -100% 미만 수익률 4값 null — `preprocessing/preprocess.py`에 반영 |
| 컬럼 사전 | `column_dictionary.csv`: 테이블 / 영문컬럼 / 한글명 / 동의어 / 단위 / 결측여부. **채권·해외ETF는 직접 작성해야 함** — [8/5] `pd_tr_yn` 해석 반전, `AVG_ANNUAL_TAX_YIELD` 사용불가, `du_last_aum`→`pd_net_tamt` 같은 "함정 주석"을 사전에 함께 기록 |
| 값 사전 | 위험등급 코드 매핑(`PD_RISK_GCD_13` ↔ "다소높은위험(3등급)" ↔ 숫자 3), 통화코드, 시장코드, 판매여부. **[8/5] 개별 상품 수동 보강값은 `manual_overrides.csv` 패턴으로 분리**(source/status/reason 기록, 원본 불변) |
| **통합 축 테이블** | `product_axis`: `product_id`, `product_type`(BOND/ETF_KR/ETN_KR/ETF_GL/ETN_GL/FUND), `asset_class`, `region`, `risk_grade`(1~6 정규화), `expense_ratio`, `aum`, `return_1m/3m/6m/1y`, `is_sellable`. **`axis_*` 힌트를 참고해 규칙 기반으로 생성** + **[8/5] 값별 `*_available` 플래그와 taxonomy 매핑 계약**(`mapping_status`·버전·검토자, 미등록 값은 실패 처리 — LLM 자동 매핑 금지) |
| 펀드 중복 처리 | `fund_master`(유효 itm_no 11,138) / `fund_attribute`(95,618, **`(itm_no, prfd_attr_cd)` 2컬럼 키**) 분리. 클래스 대표 종목 선정 규칙 확정 (`rptt_ksd_itm_no` 활용 검토). 집계 시 중복 제거 |
| 인덱스 | 자주 필터링되는 컬럼(위험등급, 투자지역, 자산군, 판매여부, AUM, 보수) |

**DoD [8/7 갱신]:** ① SQL 한 줄로 "국내ETF 중 위험등급 3 이하, AUM 500억 이상, 1년 수익률 상위 10개"가 나온다(RDB 채널). ② 온톨로지 TTL 초안이 리포지토리에 있고, 마스터 데이터 일부가 트리플로 추출되어 "TIGER 200의 운용사는?" 류를 그래프에서 답할 수 있다(KG 골격). ③ 상품군 공통 축 질의는 온톨로지 공통 클래스/속성으로 표현된다(구 `product_axis`의 역할 흡수 여부 8/8 확정).



---

### Sprint 2 — 8/17(월) ~ 8/23(일) · 에이전트 + API 첫 관통

**D-20 ~ D-14**

> **[8/7 리빌딩]** 아래 표의 "도구 5종"은 federated 검색 채널(그래프 탐색 / 벡터 검색 / SQL 필터 / 키워드 매칭)과 Query Router로 재편된다. 조건 정규화는 Ontology Grounding(온톨로지 대조 엔티티·관계명 전환)으로 확장, 가드레일에 Answer Validation(존재 검증 — 트랩 방어) 추가. 구조화 로깅·시간 정책·HCX provider 강제·API 서버 항목은 그대로 유효하다. 응답 시간 목표 15초(문항당 60초 인정 한계) 계측을 이 스프린트부터 로그에 포함한다.

| 작업 | 내용 |
|---|---|
| 도구 5종 구현 | `search_products` / `get_product_detail` / `compare_products` / `aggregate_products` / `search_by_text`. 각각 **JSON Schema로 파라미터 정의** (Function Calling 스펙) |
| 조건 정규화 | "미국 주식" → `{region:"US", asset_class:"EQUITY"}`. LLM + 규칙 하이브리드 |
| 에이전트 루프 | HCX-007 + `tool_choice:"auto"`, `maxTokens ≥ 1024`. 도구 호출 → 결과 관찰 → 재호출 → 최종 답변. **[8/5] HCX 단일 provider 코드 강제**: allowlist + 설정 누락 시 시작 실패(타 LLM fallback 금지) + 모델 식별자 감사 로그 |
| **구조화 로깅** | 모든 도구 호출(이름/파라미터/결과 행수/소요시간)을 기록 → 그대로 `think_trace`가 됨. **[8/5] `retrieved_context`는 근거 행 배열(출처 테이블·행 키·컬럼·값·기준일)로 내부 유지**, 공식 스키마가 문자열이면 어댑터 직렬화 |
| **[8/5] 시간 정책** | dev-kyung `pipeline/time_policy.py` 이식: 요청 시점 서울 `as_of_date` 확정(사용자 명시 날짜 우선) → 같은 요청의 모든 계산·근거가 공유. 잔존만기·만기상태는 조회 시 계산 |
| API 서버 | `GET /answer?question_id=&question=` → 4필드(+question 에코) JSON. **타임아웃·에러 시에도 반드시 유효한 JSON 반환** |
| 벡터 인덱스 | 해외ETF `cu_strtegy` 5,646건 → Embedding v2 → FAISS flat + CLOVA Reranker 2단계 |

**DoD (8/23):** 배포된 URL에 `curl`을 날려서 실제 데이터 기반 답변이 JSON으로 돌아온다. **이 시점에 E2E가 안 뚫리면 일정을 재조정한다.**

---

### Sprint 3 — 8/24(월) ~ 8/30(일) · 교차 질의 · 가드레일 · 평가셋

**D-13 ~ D-7** · **8/30 기능 동결**

| 작업 | 내용 |
|---|---|
| 상품군 교차 질의 | "비슷한 위험도의 채권과 ETF 비교" 류. `product_axis` 기반 다중 테이블 조회 + 결과 병합 |
| 다단계 연산 | 정렬·순위·집계·비율 계산. LLM에게 산술을 시키지 않고 **SQL/Python이 계산하고 LLM은 서술만** |
| 가드레일 | ① 근거 없는 수치 차단 ② 데이터에 없는 항목("펀드 보수" 등) → "확인할 수 없음" ③ 조건 불충분 → 역질문 ④ 전망·단정적 추천 표현 필터 |
| 근거 표기 | 답변 하단에 참조 테이블명 + 종목번호 + **데이터 기준일 2026-07-11** 자동 부착 |
| **자체 평가셋(실전 미러 3세트)** | [8/6 확정 구조 미러링] 상/중/하 각 30 + 답변 불가 트랩 15 = 105문항 — 실전(30+트랩5)과 같은 구성비 (아래 4장) · **문항별 응답 시간 계측 포함** |
| 회귀 테스트 | 평가셋 자동 실행 → 정답률 리포트. 프롬프트 수정할 때마다 돌린다 |
| **[8/5] 브랜치 교차평가 → 통합 (8/27~8/29)** | 3개 브랜치 동결 → 공용 러너로 정량 표 산출 → Claude Code 교차평가로 **노드 단위 채택표** → main 통합 → 재실행해 **개별 브랜치 최고 정답률 이상**이면 확정, 미달 시 롤백 (5.3절) |

**DoD:** 자체 평가셋 정답률 측정치가 있고, "확인할 수 없음"과 역질문이 의도대로 동작하며, **통합본이 개별 브랜치 최고 정답률 이상**이다. **8/30 이후 기능 추가 금지.**

---

### Sprint 4 — 8/31(월) ~ 9/6(일) · 튜닝 · 문서 · 제출

**D-6 ~ D-0**

| 작업 | 내용 |
|---|---|
| 프롬프트 튜닝만 | 코드 구조 변경 금지. 평가셋 정답률 기준으로 프롬프트·파라미터만 조정 |
| 안정성 | 부하 테스트(연속 100회 호출), 타임아웃 대응, 재시작 시 자동 복구, 로그 로테이션 |
| **[8/5] immutable release** | 8/30 동결 시 commit SHA·image digest·dependency lock·설정 checksum 보관. 평가 기간(9/7~9/30) 헬스체크·모니터링·알림 + 복구 runbook — "마감 후 허용되는 장애 대응 범위"는 미해결, Q&A 게시판 문의(7장). **[8/7 추가] 9/30 크레딧 만료 후 리소스 정리 절차 포함** |
| **[8/5] 요구사항 추적표** | 공식 요구사항 → 구현 산출물 → 완료 검증 3열 매핑(dev-kyung PROJECT_GUIDE 4장 형식) — 기술제안서 뼈대 + acceptance test 목록으로 전환 |
| **기술제안서 (배점 40 — 답변과 동급)** | 제안 요약 / 문제 정의 / 제안 방법 / 시스템 구성도 / 주요 기능 흐름도 / 사용자 시나리오 / 기대효과·확장성. **[8/6] PDF+Word 동시 제출 · 온톨로지 구축 방법·활용 데이터 서술 필수 · 권장 목차 5번이 수상 단계 핵심(항목명은 규정·제출 PDF로 확인)** |
| README | 환경 구성, 실행 명령어, Dockerfile, requirements.txt — **재현 가능해야 함** |
| API 명세서 | 요청/응답 JSON 스키마 (필수 명시 항목) |
| 최종 제출 | private repo push + endpoint URL |

**제출 3종 체크리스트**

- [ ] 소스코드 + Dockerfile + requirements.txt + README.md — **[8/6] 도메인별 온톨로지 포함 필수(TTL 권장, 형식 자유)**
- [ ] 기술 제안서 — **[8/6] PDF + Word 동시 제출** (배점 40)
- [ ] 평가용 API End-point URL + API 명세서 — HTTPS(도메인+인증서) 요건은 사무국 재공지 대기

> ⚠️ **9/6 23:59 이후 커밋·push·배포 금지.** 9/7~9/30 평가 기간 내내 서버가 살아있어야 한다.

---

## 4. 자체 평가셋 설계 (= competency questions)

**[8/6 확정] 실전 평가는 상·중·하 각 10문항(30문항) + 답변 불가 트랩 5문항, 전 팀 동일, 순차 호출, 응답 시간 평가 포함.** 자체 평가셋은 이 구성을 3배수로 미러링한다: **상 30 / 중 30 / 하 30 + 트랩 15 = 105문항.** 이게 유일한 객관적 성능 지표이자, 리빌딩 아키텍처의 **competency question**(온톨로지가 답해야 할 질문 정의 — 설계 순서상 온톨로지보다 먼저 작성)이다. 러너는 문항별 정답 여부와 **응답 시간(목표 15초)** 을 함께 기록한다. 채점은 8/6 방식 미러링 — 기대 엔티티·릴레이션 포함 여부 + 근거 유의성.

**하 (30문항) — 단일 상품 조회**

- "KODEX 200의 총보수는?"
- "종목번호 KR5014410050 펀드의 운용속성은?"

**중 (30문항) — 조건 필터 + 정렬/집계**

- "위험등급 3 이하 국내ETF 중 1년 수익률 상위 5개"
- "잔존만기 3년 이내, 신용등급 AA 이상 회사채 중 세후수익률 높은 순"
- "미국 투자 해외ETF 중 총보수 0.1% 미만이면서 AUM 10억 달러 이상인 것"

**상 (30문항) — 교차 질의 + 다단계 + 멀티홉 + 예외 처리**

- "위험등급 2등급대에서 채권·ETF·펀드를 각각 하나씩 추천하고 근거를 비교해줘" → **3개 테이블 교차**
- "TIGER 미국S&P500과 유사한 해외ETF를 찾아서 보수와 AUM을 비교해줘" → **시맨틱 + 정량**
- "삼성전자가 포함된 ETF 알려줘" → **구성 종목(비정형 유래) 질의 — 8/6 출제 예고 유형(57:45)**
- "에코프로의 상장 자회사를 편입한 ETF의 운용사는?" → **멀티홉 그래프 탐색 — 8/6 시연 예시(44:02)**
- "수익률 좋은 상품 추천해줘" → **정답: 역질문형 답변** ("투자 기간·위험 성향을 알려주시면 답변드릴 수 있습니다" — 멀티턴 없으므로 답변 문면으로만, 01:05:31)

**트랩 (15문항) — 답변 불가 검증 [8/6 예시 그대로 미러링]**

- "신용등급 AAAA인 채권 알려줘" → 존재하지 않는 등급 — **"없다"가 정답**(01:03:33)
- "키미 관련 투자 상품 알려줘" → 7/11 기준 관련 상품 없음 — LLM 사전지식으로 답하면 감점(01:05:54)
- "KODEX AI로봇 ETF의 보수는?" → 7/11 기준 미존재 상품(01:03:59)
- "보수가 가장 낮은 공모펀드는?" → 제공 데이터에 펀드 보수 정보 없음 → "확인할 수 없음"
- 위험등급 범위 밖(99등급), 7/11 이후 상장 상품, 데이터에 없는 필드 요구 등 변형 11문항

트랩은 실전에서 점수 미포함·**답변 시 감점**이다(59:23). Answer Validation(존재 검증)이 이 15문항을 전부 막아야 한다 — 정성평가 '리스크 관리' 항목과도 직결된다.

---

## 5. 3인 역할 분담 — 브랜치 병렬 개발 + 교차평가 통합 (8/5 개정)

기존의 수평 분할(A=데이터 / B=검색 / C=서빙)을 **폐기**한다. 우리 팀은 각자 브랜치에서 독립 개발한 뒤 세 브랜치를 교차평가해 최적 설계를 뽑는 방식이고, 수평 분할은 이 방식과 양립하지 않는다. 이유는 두 가지다.

- **비교할 대상이 생기지 않는다.** 데이터·검색·서빙을 한 사람씩 나눠 가지면 각 브랜치는 서로의 부품일 뿐 완결된 시스템이 아니다. 브랜치별 정답률이라는 개념 자체가 성립하지 않는다.
- **한 명이 막히면 전원이 멈춘다.** 적재가 늦으면 에이전트도 서빙도 대기다. 각자 E2E를 완주하면 최악의 경우에도 동작하는 시스템이 최소 하나는 남는다.

### 5.1 원칙 — 수직 분할(각자 E2E 완주) + 서로 다른 설계 가설

세 명 모두 **적재 → 도구/검색 → 에이전트 → API → 자체 평가**까지 자기 브랜치에서 끝까지 만든다. 똑같은 것을 세 번 만들지 않기 위해, 각자 **검증하는 설계 가설을 다르게** 잡는다.

| 브랜치 | 검증하는 가설 | 확인할 리스크 |
|---|---|---|
| **papuagigi** | 파라미터화 도구 5종 + HCX-007 Function Calling (SQL 사전 작성, LLM은 JSON 파라미터만 채움) | 도구 5종으로 못 푸는 질의의 비율 |
| **dev-kyung** | read-only view 대상 allowlist Text-to-SQL (AST 검증) + pg_trgm·pgvector | 생성 SQL 오류율·지연, 검증 통과율 |
| **jhnam** | **S0에서 확정** — 후보: 하이브리드 검색(BM25+벡터) 강화형 / 멀티턴 상태관리·역질문 강화형 | 미확정 시 papuagigi 변형으로 수렴할 위험 |

가설이 겹치면 비교의 의미가 사라진다. **S0 종료 시점에 3인 가설이 서로 다른지 확인**하는 것이 첫 게이트다.

> **[8/7 리빌딩에 따른 가설 재정의 — 8/8(금) 총검토 안건]** 온톨로지·KG는 이제 선택이 아니라 **전 팀 공통 필수**(소스코드 제출물 요건)다. 따라서 브랜치 간 차별화 축은 "SQL이냐 그래프냐"가 아니라 **"온톨로지/KG를 어떻게 구축·활용하느냐"**(추출 방법, federated 라우팅 전략, 검증 계층 설계 등)로 이동한다. papuagigi 가설은 "Ontology + KG + Federated Retrieval(그래프/벡터/SQL 채널)"로 재정의하며, dev-kyung의 allowlist Text-to-SQL은 SQL 채널 구현안으로서 여전히 비교 가치가 있다. jhnam 가설 검토는 팀 지시로 8/8 총검토로 보류(단 기존 후보 중 "멀티턴 상태관리 강화형"은 8/6 "멀티턴 없음" 확정으로 가치가 사라졌으므로 재선정 필요).

### 5.2 공통 소유 3종 — 중복 금지, 한 명이 소유하고 main에 둔다

브랜치를 비교하려면 **잣대는 하나**여야 한다. 아래 3종은 브랜치마다 만들지 않는다.

| 공통 자산 | 내용 | 담당 |
|---|---|---|
| **공통 계약 4종** | 도구 JSON Schema · DB 스키마 · 응답 4필드 JSON 내부 형식 · 평가셋 러너 I/O (5.5절) | (S0 지정) |
| **평가셋 105문항 + 회귀 러너** | [8/6 확정 구조 미러링] 상/중/하 각 30 + 트랩 15. 정답률과 **문항별 응답 시간**을 브랜치 무관하게 같은 명령으로 산출 | (S0 지정) |
| **배포 골격** | Docker·헬스체크·도메인·모니터링. 최종 서비스는 **1개**만 뜬다 | (S0 지정) |

### 5.3 교차평가 통합 절차 — S3 말 (8/27~8/29), **8/30 기능 동결 직전에 끝낸다**

1. **동결.** 세 브랜치가 같은 시점에 커밋을 멈추고, 공용 러너로 동일 평가셋을 돌린다. 산출물은 브랜치 × (정답률 · 환각률 · 평균 지연 · LLM 호출 수 · 실패 유형 분포) 표.
2. **교차평가 의뢰.** 세 브랜치 전체를 Claude Code에 입력하고 위 정량 표를 근거로 평가시킨다. 요구 산출물은 총평이 아니라 **노드 단위 채택표**다 — 라우터 / 도구·SQL / 검색 / 근거 조립 / 가드레일 / 프롬프트를 각각 어느 브랜치 것으로 할지, 근거와 함께.
3. **통합.** 채택표대로 main에 병합한다. LangGraph 노드 단위 모듈화 덕분에 "A의 라우터 + B의 도구 + C의 가드레일" 식 부분 채택이 가능하다.
4. **통합 검증 게이트.** 통합본으로 평가셋을 재실행해 **개별 브랜치 최고 정답률 이상**이 나와야 통합을 확정한다. 미달이면 최고 정답률 브랜치를 베이스로 롤백하고 채택 항목을 하나씩 다시 얹는다. "합쳤더니 더 나빠졌다"를 방치하지 않기 위한 장치다.

절차 전체가 **공통 계약 4종이 S0에 고정되어 있다는 전제** 위에서만 돌아간다. 계약 없이 각자 만들면 2번 단계에서 비교 자체가 불가능하다.

### 5.4 통합 이후(S4) 문서 분담

역할을 나누는 것은 여기서부터다. 다만 기술제안서를 마지막 주에 몰아 쓰면 반드시 부실해지므로, **각자 자기 브랜치를 개발하는 동안 해당 챕터를 누적 작성**한다.

| 데이터·전처리 챕터 | 아키텍처·에이전트 흐름도 | 총괄 + README + API 명세 + 데모 |
|---|---|---|
| 정제 규칙 · 품질 리포트 · 해석 사전 9종 | 노드 구성 · 도구 설계 · 교차평가 결과 | 제안서 병합 · 서빙 운영 · 불변 릴리스 |

### 5.5 공통 계약 4종 — Sprint 0에 고정 (8/4 추가 → **[8/7] 8/8 총검토에서 개정 예정**, 7장 안건 ⑥)

> **[8/7]** 1번 "도구 5종 JSON Schema"는 **검색 채널 계약**(그래프/벡터/SQL/키워드 채널의 입출력)으로, 2번 DB 스키마의 `product_axis`는 온톨로지 공통 클래스/속성으로의 흡수 여부 확정 후 개정한다. 3번(응답 JSON)·4번(평가셋 러너)은 유지하되 러너에 응답 시간 계측을 추가한다.

1. **도구 5종의 JSON Schema** (Function Calling 파라미터 정의)
2. **DB 스키마** (`product_axis` 포함 — 테이블·컬럼·타입)
3. **응답 4필드(+question 에코) JSON** (question_id / question / retrieved_context / think_trace / answer의 내부 형식)
4. **자체 평가셋 + 공용 회귀 러너** (브랜치 무관하게 같은 명령으로 정답률 산출)

이 4개가 같아야 **브랜치별 정답률이 객관적 통합 기준**이 된다. 특히 3번(응답 형식)이 어긋나면 채점 스크립트가 브랜치마다 달라져 정답률을 나란히 놓을 수 없다.

---

## 6. 리스크와 대응

| 리스크 | 영향 | 대응 |
|---|---|---|
| ~~8/6 설명회 불참~~ **[해소]** 참석 완료 — 잔여 리스크는 **전사 오류 미확정 + 규정·제출 세부 미공지** | D절 의심 수치(위험등급 범위 등)를 오해한 채 구현할 위험. 기술 세션은 PDF가 없어 녹음본 외 교차 검증 수단 없음 | 데이터 실측 우선 원칙(D절) + 규정·제출 PDF(추후) 수령 즉시 해당 항목 확정 + 불명확 사항은 Q&A·디스코드 |
| 채권 컬럼 의미 해석 실패 | 42,394건 테이블을 못 씀 | S1에 시간 배분. 디스코드로 주최 측에 직접 질의 |
| 공모펀드 클래스 중복 | 순위·집계 답변이 전부 틀림 | S1에서 대표 종목 규칙 확정. 평가셋에 반드시 포함 |
| HCX API 레이트 리밋 | **평가 기간 중 호출 실패 = 0점** | S0에서 한도 확인. 캐싱·재시도·큐잉 구현. 설명회에서 질문 |
| S2 종료 시 E2E 미완성 | 일정 붕괴 | 8/23을 하드 게이트로. 미달 시 기능 축소(벡터 검색 후순위) |
| 9/6 이후 서버 다운 | 평가 불가 = 실격 수준 | 8/30 동결 후 1주 안정성 검증. 헬스체크·자동 재시작·모니터링 |
| 기술제안서 부실 | 정성평가 5개 축 중 '문제정의·창의성' 손실 | 매주 30분씩 누적 작성 |
| 외부 데이터와 충돌 | 규칙 위반 | **주최 측 제공 데이터 우선** 원칙 코드로 강제 |
| **[8/6] 응답 시간 초과** | 문항당 60초 초과 시 불인정, 늦을수록 감점(평균 대비 상대평가 가능) | 목표 15초. S2부터 문항별 시간 계측, federated 채널 병렬화·캐싱, 그래프/LLM 호출 수 제한 |
| **[8/6] 온톨로지 제출물 미비** | 소스코드 배점(20) 직격 + 제안서(40) 서술 불가 | S1부터 TTL 초안을 리포지토리에 두고 버전 관리 — 마지막 주 몰아서 작성 금지 |
| **[8/6] 트랩 문항 오답(환각)** | 답변 시 감점 명시 — 5문항 전패 시 치명적 | Answer Validation(존재 검증) 필수 경로화 + 자체 트랩 15문항 회귀 |
| **[8/6] 크레딧 만료(9/30) 후 과금·리소스 정리** | 평가 종료 직후 크레딧 만료 — 방치 시 과금 | 9/30 평가 종료 확인 후 리소스 정리 절차를 runbook에 포함 |
| 비정형 수집물의 기준일 위반 | 7/11 이후 데이터 인입 시 감점 | 수집물마다 기준일 메타데이터 검증 게이트 — 기준일 불명 문서는 미사용 |

---

## 7. 이번 주 즉시 할 일

**8/7(금) ~ 8/9(일), Sprint 0 마감 주간** — 8/7 갱신 (아래 완료 항목의 이력은 유지)

- [x] 데이터 4종 결측률·고유값 프로파일링 (**8/4 완료** — [preprocessing/profiling/PROFILING_SUMMARY.md](preprocessing/profiling/PROFILING_SUMMARY.md))
- [x] **Tier 1 해석 메타데이터 수집 — 컬럼 사전 207컬럼 전체 + 값 사전 + 별칭·등급·코드·지수·펀드클래스 사전 9종** (**8/4 완료** — [external_data/COLLECTION_SUMMARY.md](external_data/COLLECTION_SUMMARY.md), 웹 리서치 428건 + 적대적 검증 정정 13건 반영)
- [x] **전처리 1차 파이프라인 — 함정 제거된 스테이징 CSV 4종 + 비정상행 격리 + 규칙 리포트 56건** (**8/4 완료** — [preprocessing/PREPROCESSING_METHOD.md](preprocessing/PREPROCESSING_METHOD.md), `preprocessing/preprocess.py`, S1 적재 파이프라인의 선행 완료분)
- [ ] **(8/5 개정) 브랜치별 설계 가설 확정 — 3인 가설이 서로 다른지 확인** (특히 jhnam 가설 미정) + 공통 소유 3종 담당자 지정 (5장)
- [ ] **공통 계약 4종 초안 고정** (도구 JSON Schema · DB 스키마 · 응답 JSON 내부 형식 · 평가셋 러너) — 브랜치 개발 시작 전 필수 (5장)
- [x] **8/6 설명회 참석 + 질문 리스트** — 참석 완료, 녹음본 전문 반영("8/6 설명회 확정 기록" 장). **질문 리스트 소진 현황**:
  - 해결됨: 크레딧(20만 + 웰컴 10만, 9/30까지) · 평가 방식(30문항+트랩5, 순차 호출, 60초 인정/15초 지향) · 외부 수집 데이터 인정 범위(핵심 과제로 격상, 7/11 이전 한정, 출처·근거 표시) · 타 LLM 허용 범위(전처리 자유, 최종 생성·메인 에이전트 HCX) · 멀티턴 여부(없음 — 역질문형 답변만 인정)
  - **미해결 → 디스코드·Q&A 게시판으로 이관**(추가 질문 창구 안내 01:14:36): RPM/TPM 한도 · `NDY_*`·`COV`·`DIRTY` 정의 문서 · `axis_*` 의도 · 금액 단위 · `prfd_attr_cd` 코드표 · 총보수 0의 의미 · 손상 행 2건 정정본 · `retrieved_context`/`think_trace` 형식 자유도 · **마감 후(9/7~9/30) 장애 대응 허용 범위** · HTTPS/도메인 요건(사무국 재공지 대기) · "상위" 비즈니스 로직 기준(가이드 공지 예정 01:12:20)
- [ ] **규정·제출 관련 추가 안내 PDF(추후 제공) 수령 시 → D절 규정 항목(문항 수·트랩 수·제안서 목차 5번 등) 확정 + 기술제안서 뼈대 반영.** 기술 세션(Ontology/KG)은 PDF 없음 — 녹음본 기반 B절이 최종 기록
- [ ] **NCP 계정 + 신용카드 등록 + 크레딧 쿠폰 신청(20만) + 웰컴 크레딧(10만) 신청 + 쿠폰 등록** — 쿠폰 등록을 마쳐야 과금이 발생하지 않음(03:46)
- [ ] **8/8(금) 3인 총검토 준비** — 안건: ① 리빌딩 아키텍처(Ontology+KG+Federated) 확정 ② 그래프 저장소·RDB 선정(조사 과제 결과 지참) ③ HCX-005/007 역할 분담 ④ 외부 비정형 수집(구성 종목 우선) 분담 ⑤ 브랜치 가설 재정의(5장) ⑥ 공통 계약 4종 개정(응답 JSON 유지 · 도구 스키마 → 검색 채널 계약)
- [ ] 그래프 저장소 후보 조사 + 온톨로지 TTL 초안(사전 9종에서 어휘 추출) — 총검토 자료
- [ ] 디스코드 채널 참가 — 미해결 질문 게시
- [ ] GitHub private repo 접근 확인
- [ ] 데이터 4종 엑셀 열어서 각자 담당 상품군 훑어보기 (프로파일링 요약을 먼저 읽고 시작)

---

## 8. 일정 요약

| 날짜 | 이벤트 |
|---|---|
| 8/6 (목) | ~~오프라인 설명회~~ **참석 완료** — 녹음본 전문 반영(기술 세션 PDF 없음), 규정·제출 추가 안내 PDF 대기 |
| **8/8 (금)** | **3인 총검토** — 리빌딩 아키텍처·저장소·모델 역할·수집 분담 확정 |
| 8/9 (일) | Sprint 0 종료 — FC Hello World(코드 완료, 키 발급 후 실증) + 가설 재정의 |
| 8/16 (일) | Sprint 1 종료 — 온톨로지 TTL 초안 + RDB 적재 + KG 추출 골격 + 비정형 수집 착수 |
| **8/23 (일)** | Sprint 2 종료 — **E2E 관통 (하드 게이트)** |
| **8/30 (일)** | Sprint 3 종료 — **기능 동결** |
| **9/6 (일) 23:59** | **예선 제출 마감** (이후 변경 시 실격) |
| 9/7 ~ 9/30 | 예선 평가 (주최 측이 우리 API 순차 호출 — 서버 상시 가동, 응답 시간 측정) |
| **9/30** | **NCP 크레딧 만료** — 평가 종료 확인 후 리소스 정리(방치 시 과금) |
| 10/1 (목) | 결과 발표 (6팀) |
| 10/1 ~ 10/16 | 본선 및 멘토링 (오프라인, 전원 대면 원칙) |
| 10월 중 | 결선 및 최종 발표 (PT + 라이브 시연) |

---

### 참고 출처

- 금융상품Agent_과제소개.pdf (제10회 2026 미래에셋증권 AI Festival)
- **8/6 오프라인 설명회 녹음 전사본** — `../미래에셋 ai페스티벌 오프라인 설명회_녹음본.txt` (UTF-16, 36,809자 · 원본 녹음 `../미래에셋 ai페스티벌 오프라인 설명회.m4a`) — "8/6 설명회 확정 기록" 장의 1차 출처. **기술 세션은 자료 PDF가 없어 이 녹음이 유일 기록**이며, 규정·제출 관련 추가 안내 PDF(추후 제공)만 별도 대조 예정
- 제공 데이터 스키마 4종 (PRBD01N001 / PREF01N001 / PREF02N001 / PRFD01N001) 직접 분석
- [CLOVA Studio 모델 — NAVER Cloud](https://guide.ncloud-docs.com/docs/clovastudio-model)
- [Function Calling — CLOVA Studio API 가이드](https://api.ncloud-docs.com/docs/clovastudio-chatcompletionsv3-fc)
- [CLOVA Studio 개요 — API 가이드](https://api.ncloud-docs.com/docs/ai-naver-clovastudio-summary)
- [Embedding v2 — CLOVA Studio API 가이드](https://api.ncloud-docs.com/docs/clovastudio-embeddingv2)

**8/4 리서치 출처 (아키텍처·기술스택 근거)**

- [FinAI Data Assistant — 금융 DB 질의: Function Calling 기반 파라미터화 쿼리 (arXiv 2510.14162)](https://arxiv.org/pdf/2510.14162)
- [XiYan-SQL: Multi-Generator Ensemble for Text-to-SQL (arXiv 2411.08599)](https://arxiv.org/pdf/2411.08599) — BIRD SOTA 수준 비교 근거
- [Cube — Why semantic layers make LLM analytics reliable (paired benchmark, +17~23%p)](https://cube.dev/blog/why-semantic-layers-make-llm-analytics-reliable-a-paired-benchmark-across-three-frontier-models)
- [Semantic Layers for Reliable LLM-Powered Data Analytics (arXiv 2604.25149)](https://arxiv.org/abs/2604.25149)
- [TableRAG: Million-Token Table Understanding (NeurIPS 2024)](https://proceedings.neurips.cc/paper_files/paper/2024/file/88dd7aa6979e352fda7c4952ca8eac59-Paper-Conference.pdf)
- [Agentic RAG: A Survey (arXiv 2501.09136)](https://arxiv.org/abs/2501.09136)
- [Uber QueryGPT — Natural Language to SQL](https://www.uber.com/blog/query-gpt/) · [LinkedIn·Uber Text-to-SQL 교훈 정리](https://erincon01.medium.com/building-modern-text-to-sql-systems-with-genai-lessons-from-linkedin-uber-and-open-source-69c829fa16cf)
- [Architecting Resilient LLM Agents: Plan-then-Execute (arXiv 2509.08646)](https://arxiv.org/pdf/2509.08646)
- [Graph vs Relational — Microsoft Fabric 문서](https://learn.microsoft.com/en-us/fabric/graph/graph-relational-databases)
- [DuckDB vs SQLite — PostHog 심층 비교](https://posthog.com/blog/duckdb-vs-sqlite)
- [ChatClovaX — LangChain 공식 통합](https://python.langchain.com/docs/integrations/chat/naver/) · [CLOVA Studio OpenAI 호환 API](https://api.ncloud-docs.com/docs/en/clovastudio-openaicompatibility)

.