# 팀원용 설치 안내 — 내 컴퓨터에서 프로그램 돌리기

이 문서 하나만 따라 하면 됩니다. 순서대로 **1 → 2 → 3 → 4 → 5** 입니다. 20분이면 끝납니다.

---

## 0. 먼저 이해하고 갑시다

우리 프로그램은 **세 덩어리**로 이루어져 있습니다.

| 덩어리 | 무엇 | 어떻게 얻나 |
|---|---|---|
| ① 코드 | 프로그램 본체 (조리법) | **GitHub에서 자동으로** |
| ② 열쇠 | CLOVA API 키 | **팀장이 따로 보내줌** — 비밀번호라서 GitHub에 못 올림 |
| ③ 재료 | 정제된 상품 데이터 | **GitHub에서 자동으로** |

**따로 받아야 하는 건 ② 열쇠 하나뿐입니다.** 나머지는 전부 GitHub에서 옵니다.
마지막에 명령 2줄로 데이터를 조립하는데, 돈은 들지 않습니다.

---

## 1. 코드 받기

**처음이라면:**
```bash
git clone https://github.com/ai-festival-mirae-asset/mirae-asset-dev.git
```
```bash
cd mirae-asset-dev
```

**이미 받아 뒀다면:**
```bash
git pull
```

> 작업 브랜치는 `papuagigi` 입니다. 다른 브랜치에 있다면 `git checkout papuagigi` 를 먼저 하세요.

---

## 2. 준비물 설치

```bash
pip install -r requirements.txt
```

한 번만 하면 됩니다. (파이썬 3.13이 필요합니다.)

---

## 3. 열쇠 넣기 — 여기가 핵심입니다

**3-1.** 설정 파일을 만듭니다.

```bash
copy .env.example .env
```

맥·리눅스는 `cp .env.example .env` 입니다.

**3-2.** 방금 만들어진 `.env` 파일을 메모장으로 엽니다.

**3-3.** 이 줄을 찾습니다.

```
CLOVASTUDIO_API_KEY=
```

**3-4.** `=` 바로 뒤에 팀장이 보내준 키를 붙여넣습니다.

```
CLOVASTUDIO_API_KEY=nv-여기에받은키가통째로들어갑니다
```

> **자주 하는 실수 3가지**
> - `=` 앞뒤에 **띄어쓰기를 넣지 마세요.**
> - 키를 **따옴표로 감싸지 마세요.**
> - 다른 줄(`KRX_COOKIE` 등)은 **비운 채로 두세요.** 지금은 필요 없습니다.

**3-5.** 저장하고 닫습니다.

**3-6.** 확인합니다.

```bash
python config/env_loader.py
```

맨 아랫줄에 **`CLOVA 호출 준비 완료.`** 가 나오면 성공입니다.

---

## 4. 데이터 만들기 — 2줄, 돈 안 듭니다

**원본 엑셀은 필요 없습니다.** 정제된 자료가 GitHub에 이미 들어 있어서 1번에서 같이 받아졌습니다.
아래 2줄을 **차례대로** 실행하면 나머지가 만들어집니다. 각각 몇 분 걸립니다.

```bash
python storage/load_duckdb.py
```
```bash
python kg/build_kg.py
```

> **8/19 이후 `git pull` 한 분**: 온톨로지 어휘가 바뀌어(공식 형식 5파일, `fp:` 접두어) **`python kg/build_kg.py` 를 한 번 다시 실행**해야 합니다. 옛 그래프 파일이 남아 있으면 서버가 켜질 때 "재생성 필요" 오류로 멈춥니다(조용히 틀리는 것보다 낫습니다). `pip install -r requirements.txt` 도 다시 한 번(검사용 패키지 2개 추가).

> **이 두 개는 실행하지 마세요.**
> - `python preprocessing/preprocess.py` — 결과물이 이미 저장소에 있습니다. 게다가 대회 원본 엑셀이 있어야 돌아갑니다.
> - `python vector/build_index.py` — 결과물이 이미 저장소에 있습니다. **다시 만들면 API 요금이 실제로 나갑니다**(호출 5,567번).
>
> 원본 데이터가 새 버전으로 갱신됐을 때만 팀장이 다시 만들어 올립니다.

---

## 5. 잘 되는지 확인하기

**5-1. 자동 검사** (약 15초, 돈 안 듭니다)

```bash
python -m pytest tests/ -q
```

맨 끝에 `... passed` 가 나오고 `failed` 가 없으면 정상입니다.

**5-2. 실제로 질문해 보기** (약 1분, 돈 안 듭니다)

```bash
python engine/answer_cli.py "순자산총액 기준으로 국내 ETF 상위 5개 알려줘"
```

ETF 5개 목록이 나오면 **설치 완료입니다.**

**5-3. 화면으로 써 보기** (선택)

```bash
python server/app.py
```

1분쯤 기다린 뒤 브라우저에서 `http://localhost:8000/` 을 엽니다.
**단, 질문마다 API 요금이 조금씩 나갑니다.** 요금 없이 쓰려면 `python server/app.py --light` 로 켜세요.

---

## 6. 안 될 때

| 이런 오류가 나오면 | 이렇게 하세요 |
|---|---|
| `CLOVA Studio API 키가 없습니다` | 3번을 다시 하세요. 특히 `=` 뒤 띄어쓰기를 확인하세요. |
| `No module named ...` | 2번(`pip install -r requirements.txt`)을 안 했거나 실패했습니다. |
| `FileNotFoundError` 에 `output` 이 보임 | 4번을 안 했거나, 2줄을 순서대로 안 했습니다. |
| `python` 이 없다고 나옴 | 파이썬이 안 깔렸거나, `python` 대신 `py` 를 써야 합니다. |
| 그래도 안 됨 | 오류 메시지 **맨 아래 3~4줄을 그대로 복사해서** 팀 단톡방에 올리세요. |

---

## 7. 꼭 지켜주세요

- **`.env` 파일과 그 안의 키를 GitHub에 올리지 마세요.** 막아뒀지만 다른 이름으로 복사하면 뚫립니다.
- 키를 **단톡방·블로그·스크린샷에 올리지 마세요.** 유출되면 남이 우리 API 요금을 씁니다.
- 키가 새어나간 것 같으면 **바로 단톡방에 알려주세요.** 새로 발급받아야 합니다.
