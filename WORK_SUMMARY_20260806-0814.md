# 작업 정리본 — 8/6 설명회 이후 ~ 8/14 (이번 푸시에 담긴 것)

> **이 문서는**: 직전 커밋(8/5) 이후 이번 푸시까지의 작업을 한 번에 파악할 수 있게 정리한 기록입니다(시점 고정 — 이후 갱신하지 않음).
> 최신 현황은 [README.md](README.md), 계획·작업 일지는 [PLAN.md](PLAN.md), 모르는 용어는 [GLOSSARY.md](GLOSSARY.md).

## 0. 한 줄 요약

**"질문을 받으면 데이터에 근거해서만 답하고, 모르면 모른다고 말하는 API 서버"의 전 구간이 완성되어 실제로 가동됩니다.** 구현 8단계 중 ⑥까지 완료 — 남은 것은 자동 채점기(⑦)와 클라우드 배포(⑧)입니다.

## 1. 한눈에 보기

| 영역 | 이번에 한 일 | 결과 |
|---|---|---|
| 규칙 확정 | 8/6 설명회 녹음·필기 전문 반영 | 채점 구조(40/40/20)·함정 감점·API 규격(5필드 문자열)·타임아웃 300초·HTTP 가능 등 확정 |
| 데이터 보강 | 제공 데이터에 없는 **ETF 구성종목을 KRX에서 직접 수집** | 활성 ETF 1,139개 전량, 75,081행(기준일 7/10), 실패 0 — "삼성전자 담은 ETF?" 유형의 재료 |
| 데이터 결함 수리 | 원본의 함정 교정 | 운용사 이름 오염 64행 복구 · 현금 표시를 종목으로 착각하던 가짜 관계 685개 제거 · 한글 별칭 사전 66개(캠브리콘 등) |
| 저장소 | 3종 구축 — 전부 파일, 상주 DB 프로그램 0개 | DuckDB 6개 표(31MB) · 지식그래프(관계 6.7만, 메모리 620MB 실측) · 의미검색 인덱스(해외 ETF 설명문 5,567건) |
| 엔진 | 질문→계획→4채널 조회→검증→생성→답변 | 라우터 2단(규칙 우선, 복잡할 때만 HCX 1번) · 5중 검문소 · HCX 생성+사후 대조 |
| 서버 | 공식 규격 `GET /answer` + 브라우저 질문 시험대 | 실측 응답 2.3~9.7초(목표 15초 내) · 어떤 오류에도 유효 JSON |
| 품질 | 자동 테스트 | **248개 전부 통과** — 함정 16문항 상시 자동 방어 포함 |
| 문서 | 사람이 읽을 수 있게 전면 개편 | 5개 체계(README·GLOSSARY·PLAN·ROADMAP·ARCHIVE) + 용어집 신설 |

## 2. 시스템이 작동하는 방식 (완성된 모습)

```
질문 → [1] 라우터: 무엇을 조회할지 계획 (규칙 우선 — 쉬운 문항·함정은 AI 호출 0번)
     → [2] 4개 채널 조회: 표(SQL)·관계(그래프)·이름(키워드)·의미(벡터)
     → [3] 5중 검문소: 값이 실존하나·상품이 진짜 있나·시점이 맞나·그 정보가 있나·데이터가 충분한가
     → [4] HCX-005가 근거만으로 문장 작성 → 근거에 없는 상품명·숫자가 섞이면 그 줄 자동 삭제
     → 5필드 JSON (질문·답변·근거·처리 과정 — 전부 문자열, 공식 규격)
```

- **함정 방어가 이중**입니다: 라우터가 걸러도 검문소가 질문 원문으로 다시 검사하고, 거절문은 AI가 쓰지 않고 정해진 틀만 사용합니다(함정 경로에서 AI가 지어낼 통로 자체가 없음).
- **해석이 갈리는 규칙은 설정 파일**(config/policy.json)에 있어서, 주최 답변이 오면 코드 수정 없이 파일 교체로 반영됩니다. 8/14에 실제로 "AA 이상 = AA- 미포함"(사용자 확정)을 이 방식으로 반영했습니다.

## 3. 이번 작업에서 발견해 고친 문제들 (테스트가 잡아낸 것 포함)

1. **현금 표시 코드가 종목으로 등록** — "설정현금액"이 6개 ETF에 편입된 종목처럼 그래프에 들어가 있었음 → 이중 차단 + 재발 방지 테스트
2. **운용사 칸에 상품명이 들어간 오염 64행** → 브랜드 대응표(KODEX→삼성 등)로 전부 복구
3. **"현재 판매 가능한 채권"에 만기 지난 채권이 나옴** — 매수가능 표시가 낡은 날짜 기준이라 → 만기 미경과 필터 자동 결합
4. **의미 검색 단독의 오답** — "반도체 집중 투자"에 로보틱스 ETF가 1위 → 한→영 테마 사전 + 순위 결합(RRF)으로 정정(실 호출 검증)
5. **'kimi'가 해외 지수명에 우연히 포함** — "MSCI Denmark IMI"의 띄어쓰기를 없앤 문자열(denmar**kimi**)에 걸림 → 원문 표기 재대조로 우연 겹침 제거
6. **'챗GPT'의 GPT가 실제 미국 ETF 티커(GPT)와 일치** — 엉뚱한 상품으로 답할 뻔 → 한글이 바로 붙은 영문은 한 단어로 취급
7. **AI가 지어낸 숫자** — 근거에 없는 수치(예: 25.55%)가 답변에 섞이면 사후 대조가 그 줄을 삭제(반올림만 허용)

## 4. 직접 시험해 보는 법

```bash
# 전체 자동 테스트 (약 15초)
python -m pytest tests/ -q

# 서버 켜기 (기동 약 1분) → 브라우저에서 http://localhost:8000/ 접속
python server/app.py
```

브라우저 질문 시험대에서 질문을 입력하면 답변·근거·처리 과정이 표시됩니다. 실제 평가와 동일한 경로(`GET /answer`)입니다. AI 호출 없이 가볍게 켜려면 `python server/app.py --light`.

## 5. 남은 것

- **⑦ 자동 채점기**: 자체 모의고사 105문항(실전의 3배수, 함정 15 포함)을 서버에 자동 실행·채점 — 첫 정량 성적표
- **⑧ 다듬기 + NCP 배포**: 성적표 기준 보정 후 클라우드에 올려 공인 IP로 원격 리허설
- 대기: 주최 문의 2건(함정 채점 기준 · 구성종목 수집 전제) — 답변 오면 정책 파일 교체로 반영

## 6. 파일 구성 변화 (이번 푸시 기준)

신규 폴더: `engine/`(라우터·검문소·생성기·서버 진입점) · `server/`(API 서버) · `kg/`(지식그래프) · `vector/`(의미 검색) · `storage/`(DuckDB 적재) · `pipeline/`(공용 부품) · `agent/`(CLOVA 클라이언트) · `config/`(정책 파일) · `evalset/`(모의고사 105문항) · `tests/`(자동 테스트 248개) · `ontology/`(제출 필수 TTL) · `infra/`(서버 구축 가이드). 문서는 5개로 통합(구 memory.md·PROGRESS·S2_PLAN은 PLAN.md와 ROADMAP 부록으로 흡수). 대용량 산출물(DB·그래프·벡터 파일)과 비밀값(쿠키·API 키)은 커밋에서 제외(.gitignore) — 스크립트 재실행으로 재생성 가능합니다.
