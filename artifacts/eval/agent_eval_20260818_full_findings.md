# Agent Evaluation Findings — 2026-08-18

## 실행 범위

- 평가셋: 105문항
- 호출 방식: FastAPI `GET /answer` E2E
- 연동: SQLite DB, Knowledge Graph, Vector Index, HCX Router, HCX Generator
- 결과 파일:
  - `agent_eval_20260818_full.jsonl`
  - `agent_eval_20260818_full_summary.json`
  - `agent_eval_20260818_full_report.md`

## 핵심 결과

| 지표 | 결과 |
|---|---:|
| 행동 판정 일치 | 97 / 105 |
| 답변 불가 표면 판정 | 105 / 105 |
| 응답 계약 준수 | 105 / 105 |
| 근거 충족 | 99 / 105 |
| 기대 채널 일치 | 78 / 105 |
| 엄격 통과 | 70 / 105 |
| 전역 오류 | 0 |
| 빈 일반 답변 | 3 |
| 저하(degraded) 응답 | 12 |

### 난이도별 행동 판정

| 난이도 | 통과 | 전체 |
|---|---:|---:|
| 하 | 29 | 30 |
| 중 | 29 | 30 |
| 상 | 24 | 30 |
| 트랩 | 15 | 15 |

## 성능

| 항목 | 시간 |
|---|---:|
| 평균 | 5.0441초 |
| 중앙값 | 4.8219초 |
| p95 | 10.1944초 |
| 최대 | 13.203초 |
| 15초 초과 | 0건 |

## 이전 오프라인 실행 대비

| 지표 | 이전 | 이번 |
|---|---:|---:|
| 행동 판정 | 97 | 97 |
| 근거 충족 | 96 | 99 |
| 기대 채널 | 74 | 78 |
| 엄격 통과 | 70 | 70 |
| 빈 답변 | 9 | 3 |

실제 HCX 및 벡터 연동으로 근거 충족과 채널 선택, 빈 답변 수는 개선됐지만 엄격 통과 수는 변하지 않았다.

## 행동 판정 불일치 8건

- `L-24`: 기대 `answer`, 실제 `partial`. 상위 5개와 커버리지 안내를 제공해 비교적 경미한 과잉 보수 판정이다.
- `M-10`: 기대 `partial`, 실제 `answer`. 본문은 국민성장펀드 구조·전략을 확인할 수 없다고 안내하지만 행동 마커가 `answer`로 잘못 표시됐다.
- `H-01`: 기대 `partial`, 실제 `answer`. 에코프로 자회사 대신 에코프로 자체를 조회했고, 근거가 부족한 일반 위험요인을 생성했다.
- `H-02`: 기대 `partial`, 실제 `answer`. 최근 6개월 연결 이력을 검증하지 않고 테마 검색 결과를 기간 조건이 충족된 것처럼 표현했다.
- `H-03`: 기대 `partial`, 실제 `answer`. 국내 ETF 보수 데이터가 일부 있음에도 최저 보수 비교가 불가능하다고 처리했다.
- `H-13`: 기대 `partial`, 실제 `answer`. 위험등급 1등급 상품의 상품군별 구분과 해외상품 미지원 설명이 부족하다.
- `H-15`: 기대 `partial`, 실제 `answer`. 회사채 ETF 신용등급 분포 질의가 잘못 라우팅되어 빈 답변과 생성 시간 초과가 발생했다.
- `H-19`: 기대 `partial`, 실제 `answer`. TDF 상품명은 제시하지만 실제 편입종목 대신 이름에서 추정한 범주를 설명했다.

## 근거 실패 6건

- `L-22`: 판매 중 공모주 펀드 필터 오류. DB 값은 `판매중`/`판매완료`인데 SQL은 `Y`/`TRUE`/`1`만 허용한다.
- `L-29`: A형과 C형 차이를 데이터 근거 없이 일반 지식으로 생성했고 사후 검증이 통과했다.
- `M-08`: `TIGER 차이나테크TOP10` 명칭을 ETF ID로 넘겨 구성종목 조회가 실패했다.
- `M-30`: 인도 대표기업 액티브 ETF 구성종목 질의가 키워드 fallback 후 무결과로 종료됐다.
- `H-15`: `회사채 ETF`라는 문자열을 ETF 식별자처럼 사용해 구성종목 신용등급 조회가 실패했다.
- `H-26`: `만기 3년 이하`를 지원되지 않는 `maturity_status` 값으로 전달해 채권 필터가 실패했다.

빈 일반 답변은 `M-30`, `H-15`, `H-26` 세 건이다.

## 생성 및 저하 현황

- HCX 생성 정상 통과: 61건
- 사후 검증으로 문장이 제거된 답변: 18건, 총 34문장 제거
- 생성 API 실패 후 규칙 기반 fallback: 5건 (`L-12`, `M-01`, `M-18`, `M-26`, `H-04`)
- 생성 시간 제한으로 생성 생략: 3건 (`M-30`, `H-15`, `H-26`)
- 생성 전체가 근거 검증에 실패: 1건 (`L-09`)
- 저하 응답 전체: 12건

## 테스트 스위트

- `pytest`: 265 passed, 2 failed, 2 skipped
- 실패 2건은 pandas 3의 결측값 표현 변화와 관련된다.
  - `tests/test_preprocess.py::TestYyyymmddToIso::test_conversion_and_sentinels`: `None` 기대, 실제 `nan`
  - `tests/test_preprocess.py::TestIsoLagDays::test_whitespace_stripped`: `pd.NA`의 boolean 판정이 모호해 `TypeError`

## 우선 개선 권고

1. `L-22`의 판매 상태 SQL 조건을 실제 코드값인 `판매중`에 맞춘다.
2. ETF 상품명·종목코드를 내부 ETF ID로 정규화한 뒤 구성종목 도구를 호출한다.
3. 채권의 잔존만기 자연어를 지원되는 필터 파라미터로 변환한다.
4. `answer`/`partial` 행동 마커를 응답 내용 및 근거 커버리지와 일치시킨다.
5. 데이터 부재 시 일반 금융지식 생성을 차단하도록 사후 근거 검증을 강화한다.
6. pandas 3에 맞게 전처리 결측값 처리를 명시적으로 정규화한다.
