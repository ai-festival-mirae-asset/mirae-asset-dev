"""금융상품 QA 에이전트 데이터 파이프라인 패키지.

무엇: 시간 정책(time_policy) 등 재현 가능한 데이터 처리 모듈을 담는다.
왜: 채권 REMAINING_DAYS 저장값은 행별 PD_STD_INFO_UPDATE 시점 기준(중앙값
    137일 지연)이라 그대로 답변에 쓰면 틀린다. 잔존만기·만기상태는 요청 시점
    서울 날짜(as_of_date)로 재계산해야 하며, 이 재계산 로직을 DB 선택과 무관한
    순수 모듈로 분리해 둔다.

사용처: S2 도구 구현(잔존만기 재계산 도구)에서 time_policy 모듈을 사용 예정.
스냅샷 재현 통계는 AS_OF=2026-08-22(8/26 재배포본) 고정, 서비스 응답은 요청 시점 서울 날짜 —
이 이원화가 time_policy 의 핵심 계약이다.
"""
