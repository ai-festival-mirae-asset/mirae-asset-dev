# 제10회 미래에셋증권 AI Festival — 금융상품 Agent 프로젝트 로드맵

> 작성 기준일: 2026-08-04(화) · 예선 마감 2026-09-06(일) · **D-33** · **v2.1 — 8/4 아키텍처·기술스택·규정 리서치 + Tier1 사전·전처리 1차 반영**
> 팀 구성: 3인 (개발·기획 겸임, 경험 있는 개발자 보유) · 원칙: **성능 최우선** · 개발 방식: 공동 repo + 개인 브랜치 → 비교 통합
> 관련 문서: 팀 공동 실행 가이드 [PROJECT_GUIDE.md (main 브랜치)](https://github.com/ai-festival-mirae-asset/mirae-asset-dev/blob/main/PROJECT_GUIDE.md) · 작업 로그 [memory.md](memory.md) — 이 문서는 개인 브랜치(papuagigi) 관점의 전략 로드맵이다. 아키텍처 선택지(DuckDB+Function Calling vs PostgreSQL+Text-to-SQL)는 브랜치 비교로 검증 후 통합한다(5장 공통 계약 참조).

---

## 8/4 리서치 확정 — 우리가 나아갈 방향 5가지

논문·벤치마크·산업 사례 리서치로 확정한 전략 방향이다. 상세 근거는 1·2장과 참고 출처 참조.

1. **아키텍처는 "파라미터화 도구 + Function Calling"으로 간다.** 자유 Text-to-SQL은 SOTA 파이프라인조차 BIRD 벤치마크 71~77%(인간 약 93%)이고, 그것도 GPT-4o급 모델 + 다단계 앙상블 기준이다. 금융 DB 질의 연구(FinAI Data Assistant)와 Uber QueryGPT·LinkedIn SQL Bot 프로덕션 사례의 결론이 모두 같다: **스키마 전체를 LLM에 주지 말고, 검증된 좁은 인터페이스로 제한하라.** 예시 질의 set 확인 후 롱테일 커버용 '제한된 Text-to-SQL fallback'만 옵션으로 검토한다.
2. **컬럼 사전·값 사전이 곧 성능이다.** semantic layer 연구에서 의미 계층(컬럼 정의·값 매핑·동의어) 유무가 정확도 **+17~23%p**(Cube 벤치마크), BIRD external knowledge **+20%p**를 가른다. S1의 사전 작업은 잡무가 아니라 핵심 성능 작업이며, 외부 데이터 수집 1순위도 이 사전을 채우는 자료다.
3. **외부 데이터는 3-Tier 원칙(1장)으로.** 해석 메타데이터(Tier 1)는 적극 수집, 결측 필드 보강(Tier 2)은 "제공 데이터 기준 답변 우선 + 출처 명시 부가정보"로만, 시세·수익률 대체 데이터(Tier 3)는 금지.
4. **런타임의 모든 LLM 추론은 HCX만.** OpenAI '모델'(GPT 계열)은 라우팅·조건추출 등 어떤 단계에도 불가(실격 조항). OpenAI 'SDK'를 CLOVA 호환 엔드포인트로 쓰는 것은 가능(모델은 HCX). 임베딩 모델 등 회색지대는 8/6 서면 확인(2장).
5. **성능 병목은 DB가 아니라 LLM 호출이다.** 14.5만 건은 어떤 DB든 ms 단위다. 질의당 LLM 호출 2~3회 제한, 규칙 우선 조건추출, 동일 질의 캐싱, 레이트리밋 대응이 진짜 성능 작업이다.

---

## 0. 먼저 알아야 할 3가지

### ① 진짜 마감은 9/6이 아니라 8/30이다

과제소개서에 이렇게 적혀 있다.

> 마감 이후 커밋-push, 서버 배포 등 코드 및 결과물을 변경하는 행위 발견 시 **실격 처리**

그리고 평가는 9/7~9/30에 주최 측이 **우리 API 서버로 직접 GET 요청**을 보내는 방식이다. 즉 9/6 이후에도 서버는 살아있어야 하고, 고칠 수는 없다. 마지막 주에 급하게 배포하다 서버가 죽으면 복구 불가다.

→ **8/30 기능 동결(feature freeze), 9/6까지는 검증과 문서만.** 이 원칙 하나가 로드맵 전체를 결정한다.

### ② 예선은 UI 싸움이 아니라 API 정확도 싸움이다

평가 방식이 곧 요구사항이다.

| 구분 | 내용 |
|---|---|
| 정량평가 | **비공개** 평가문제(난이도 상/중/하 혼합)를 참가팀 API endpoint에 GET 요청 → 응답 평가 |
| 정성평가 | 기술·도메인 전문가 검토: 문제정의 / 기술완성도·성능 / 창의성·확장성 / **답변의 정확성·완결성** / **현업 활용성·리스크 관리** |
| 결선 | 6팀 선발, PT + **라이브 시연** (10/1~10/16 오프라인) |

예선에서 프론트엔드는 평가 대상이 아니다. 화면은 결선(10월) 시연용이므로 9월 이후에 만들어도 된다. **예선 5주는 전부 백엔드 정확도에 쏟는다.**

또한 응답 JSON 스키마가 아키텍처를 강제한다.

```json
{
  "question_id": "Q-001",
  "question": "평가 질의 원문",
  "retrieved_context": "답변 생성에 참고한 검색 문서",
  "think_trace": "사고·추론·도구 사용 과정",
  "answer": "최종 생성 답변"
}
```

`retrieved_context`와 `think_trace`를 요구한다는 것은 **에이전트의 도구 호출 과정이 처음부터 기록 가능한 구조**여야 한다는 뜻이다. 나중에 붙이려면 전체를 뜯어고쳐야 한다. 1주차부터 모든 도구 호출을 구조화 로그로 남기도록 설계한다.

### ③ 정성평가에 '리스크 관리' 항목이 있다

환각 방지, "확인할 수 없음" 명시, 조건 부족 시 역질문은 **선택 기능이 아니라 배점 항목**이다. 데이터 활용 규칙에도 필수/금지로 명시돼 있다.

- (필수) 답변은 데이터를 근거로 생성하고 **근거(참조 데이터)를 표시**
- (필수) 확인 불가한 질의는 **'확인할 수 없음' 명시 또는 필요한 조건 역질문**
- (금지) 데이터에 근거 없는 **수익률 전망·단정적 투자 추천 생성 금지**

→ 이건 3주차에 몰아서 붙이는 게 아니라, **응답 생성 레이어의 기본 동작**으로 1주차부터 설계한다.

---

## 1. 데이터 실측 결과

제공된 스키마 파일 4종을 직접 열어 확인한 내용이다. **여기가 이 과제의 난이도 대부분을 차지한다.**

| 테이블 ID | 상품군 | 건수 | 컬럼수 | 명명 규칙 | 한글 컬럼명 |
|---|---|---|---|---|---|
| PRBD01N001 | 국내채권 | 42,394 | 40 | `PD_NO` (대문자) | **전부 비어 있음** |
| PREF01N001 | 국내ETF | 1,734 | 73 | `pd_itm_no` (소문자) | 대부분 있음 |
| PREF02N001 | 해외ETF | 5,646 | 49 | `pd_itm_no` (소문자) | **전부 비어 있음** |
| PRFD01N001 | 공모펀드 | 95,619 | 45 | `itm_no` (소문자) | 예시값까지 완비 |

합계 약 145,393건. 데이터 기준일은 **2026-07-11 스냅샷** — 모든 답변에 이 기준일을 명시해야 한다.

### 상품군별 함정

**국내채권 (가장 어렵다)**

- 컬럼 한글명과 예시값이 **스키마 파일에 전혀 없다.** `DUR`(듀레이션), `COV`(컨벡시티), `DIRTY`(더티프라이스), `NDY_*`(익일자 추정) 같은 약어를 팀이 직접 해석해 사전을 만들어야 한다.
- `ISU_DT`(발행일), `MAT_DT`(만기일), `CRD_GRD_DT`가 **`double precision` 타입**이다. 날짜가 숫자로 들어있다 → 날짜 연산(잔존만기 등) 전에 캐스팅 필수. `REMAINING_DAYS`가 이미 있으니 우선 활용.
- 수익률 컬럼이 7종(`BUY_YIELD`, `CORP_PRETAX_YIELD`, `AFTER_TAX_YIELD`, `PREF_TAX_YIELD`, `AVG_ANNUAL_TAX_YIELD`, `DEPO_EQUIV_YIELD_154` 등)이고, 과제소개서상 **매수 가능 종목 일부만 수록**. **8/4 실측: 결측 97.9% — 수익률이 있는 종목은 약 881건(2.1%)뿐.** "수익률 높은 채권" 질의는 사실상 이 매수가능 풀 안에서만 랭킹된다. 단 `APPLIED_YIELD`(결측 25%)·`NDY_APPLIED_YIELD`(32%)는 대부분 채워져 있어 대체 지표로 검토.
- **8/4 실측 추가:** `MAT_DT` 최대값 99991231 — 영구채 센티널 값. 잔존만기 계산 전 예외 처리 필요. `CRD_GRD`(신용등급)도 41.6% 결측 — "AA 이상" 필터 시 NULL 규칙 필요.

**국내ETF (컬럼은 친절, 값이 지저분)**

- `cu_base_index`(기초지수)가 **공백 40칸 문자열**로 채워진 행이 다수다. `NULL`이 아니라 `'          ...'` 이므로 `IS NULL` 필터가 통하지 않는다. → 적재 시 `TRIM` 후 빈 문자열은 NULL로 정규화. **8/4 실측: 공백 1,551건 + NULL 125건 = 실질결측 96.7%, 값이 있는 종목의 고유 지수는 19개뿐.** 기초지수 질의는 국내ETF에선 거의 답변 불가(→ "확인할 수 없음" 케이스), 해외ETF는 결측 0%라 가능.
- `cu_charge_rt`(총보수요율)는 값이 없는 행이 많다. **8/4 실측: 실질결측 87.5%(1,517/1,734건).** "총보수 낮은 국내ETF" 질의도 값 있는 217건 풀에서만 랭킹 가능 — 답변에 이 한계를 명시해야 한다.
- **8/4 실측 추가:** 100% 결측 컬럼 6개(`nru_mkt_diff_rt`, `nru_mkt_inav`, `pd_dvid_cycl`, `pd_sect_nm`, `ru_mkt_price`, `ru_mkt_volume`) — 적재 시 제외하고 컬럼 사전에 "미제공"으로 기록.
- `du_bpr`, `du_clpr` 등이 스키마상 `numeric`인데 실제로는 **문자열 `'13675.00'`**로 들어온다 → 전 컬럼 타입 캐스팅 필요.
- 반면 수익률은 `du_er_1d/1m/3m/6m/1y/ytd`로 잘 갖춰져 있고, `pd_risk_nm`("다소높은위험(3등급)"), `pd_pen_tr_yn`(연금거래가능), `wu_inv_ast_type`(투자자산군), `wu_inv_rgn`(투자지역)이 있어 조건 검색에 바로 쓸 수 있다.

**해외ETF (유일하게 벡터 검색이 필요한 지점)**

- 한글 컬럼명이 전혀 없고, `cu_strtegy`(운용전략)가 **영문 서술 텍스트**다.
- 반대로 `cu_charge_rt`(총보수, **8/4 실측 결측 0%**), `du_last_aum`(AUM, 결측 3.3%)는 잘 채워져 있어 **정량 비교 질의에 가장 적합한 테이블**이다. 단 `cu_base_index`(기초지수)는 **8/4 재실측 정정: NULL은 0%지만 "Index is not provided by Management Company"(1,984건) 등 센티널 문자열이 약 48%** — 기초지수 질의의 절반은 해외ETF에서도 '확인할 수 없음' 대상이다. 적재 시 센티널→NULL 정규화 필수([외부데이터/수집_요약.md](외부데이터/수집_요약.md) 3장).
- **8/4 실측 정정:** `cu_lev_fector`(배수)는 **전량(100%) 결측**이라 쓸 수 없다. `cu_inverse_short_yn`(인버스)·`cu_etn_yn`·`wu_core_yn`은 결측 97~99%에 고유값이 'Y' 하나 — **플래그성 컬럼으로, 채워진 것만 Y이고 NULL은 N으로 해석**하는 규칙이 필요하다(값 사전에 기록). 레버리지 탐지는 `cu_strtegy` 텍스트("2x", "leveraged")나 종목명 패턴으로 보완해야 한다.
- 4종 중 자연어 임베딩이 의미 있는 유일한 필드가 `cu_strtegy`다. 벡터 DB는 여기에만 쓴다.

**공모펀드 (가장 크고, PK가 함정)**

- 95,619건으로 전체의 66%. 컬럼 한글명·예시값이 완비돼 있어 가장 다루기 쉽다.
- PK가 `itm_no` + `prfd_attr_cd` + `zrin_fd_ivst_risk_gcd` **복합키**다. **8/4 실측: 복합키 중복 0건(PK 성립 확인), `itm_no` 고유값은 11,139개 — 평균 8.6개 행이 같은 펀드다.** "순자산 상위 10개 펀드"를 물으면 같은 펀드가 수 번 나올 수 있다. `rptt_ksd_itm_no`(대표종목)는 99.9% 채워져 있어 클래스 대표 선정 규칙에 쓸 수 있다.
- **8/4 실측 추가:** 수익률 결측이 기간이 길수록 커진다(1M 27.6% → 1Y 32.6% → 5Y 46.8%). 장기 수익률 랭킹 질의는 모수 절반이 빠진다는 한계를 답변에 명시.
- 수익률이 `fd_wk1 / mm1 / mm3 / mm6 / mm18 / yr1 / yr2 / yr3 / yr5`로 9종. ETF(`du_er_*`)와 **기간 체계가 다르다** — 교차 비교 시 공통 기간(1M/3M/6M/1Y)만 매핑.
- **보수 정보 미포함.** "보수 낮은 펀드" 질의는 답변 불가 → "확인할 수 없음"으로 처리해야 하는 대표 케이스다.

### 가장 중요한 발견: `axis_*` 컬럼

schema 파일의 `Sheet2_Sample` 시트에는 실제 데이터에 **없는** 컬럼들이 붙어 있다.

| 상품군 | Sample에만 있는 축 컬럼 |
|---|---|
| 국내채권 | `axis_issuerType`, `axis_maturityClass`, `axis_couponType`, `axis_creditRating`, `axis_collateralType`, `axis_currency`, `axis_issuanceMarket`, `axis_issuerCategory` |
| 국내ETF | `axis_assetType`, `axis_region`, `axis_strategy`, `axis_replicationMethod`, `axis_leverageType`, `axis_underlyingScope`, `axis_distributionType` |
| 공모펀드 | `axis_fundType`, `axis_redemptionType`, `axis_issuanceType`, `axis_listingType`, `axis_classDifferentiation`, `axis_investorEligibility` |
| 해외ETF | 없음 (원본 49컬럼 그대로) |

국내ETF의 실제 `datarows` 파일을 열어 확인한 결과 **컬럼 73개 = 스키마 73개, `axis_*` 없음**이었다.

**해석:** 주최 측이 "상품군을 넘나드는 교차 질의를 하려면 이런 공통 분류 축이 필요하다"는 힌트를 예시로 남긴 것이다. 그리고 그 축을 만드는 일은 참가팀 몫이다. 과제 요구사항 1번이 "팀이 판단한 최적의 방식으로 구조화"인 이유가 여기 있다.

→ **이 통합 축 테이블(`product_axis`)을 만드는 것이 이 과제의 핵심 차별점이다.** 채권·ETF·펀드를 같은 언어로 질의할 수 있게 만드는 유일한 방법이고, 기술제안서에서 가장 설득력 있는 챕터가 된다.

### 외부 데이터 수집 전략 — 3-Tier (8/4 확정)

데이터 활용 규칙의 구조(수집 **가능** / 제공 데이터가 평가 **기준** / 상충 시 제공 데이터 **우선**)를 뒤집어 읽으면: **채점 정답지는 제공 데이터로 만들어진다.** 따라서 외부 데이터는 '값 대체'가 아니라 '제공 데이터 해석'에 쓸 때만 ROI가 난다.

| Tier | 대상 | 방침 |
|---|---|---|
| **1 — 적극 수집** | 채권 용어·약어 사전(KOFIA 채권 용어집 등 — `DUR`·`COV`·`DIRTY`·`NDY_*` 해석), 신용등급·위험등급 체계, 통화·시장 코드표, 별칭 사전("타이거"=TIGER=미래에셋, "곱버스"=레버리지·인버스), 기초지수 명칭 정규화 | 컬럼 사전·값 사전을 채우는 **해석 메타데이터**. semantic layer 연구상 정확도 +17~23%p에 직결. 저위험·고수익 |
| **2 — 조건부** | 펀드 보수, 국내ETF 총보수·기초지수 등 **결측 필드 보강** | "보수 가장 낮은 펀드?"는 '확인할 수 없음' 트랩 문제일 가능성이 높다(과제소개서가 "보수 정보 미포함"을 명시 = 출제진이 인지하는 구멍). **1차 답변은 항상 제공 데이터 기준**("확인할 수 없음") → 출처 명시한 외부 정보는 부가로만. 별도 테이블 + 출처 컬럼으로 분리 저장, `retrieved_context`에 출처 구분 표기 |
| **3 — 금지** | 시세·수익률 등 시계열 대체 데이터 | 기준일(2026-07-11) 스냅샷과 반드시 상충 → '제공 데이터 우선' 규칙상 쓸 수 없고 평가 리스크만 증가 |

→ 수집 수준은 "많이"가 아니라 **"메타데이터 중심으로 좁고 깊게"**. 8/6 예시 질의 set 확인 후 Tier 2 진입 여부만 재판단한다.

---

## 2. 아키텍처 설계

### 왜 Vector RAG 단독으로는 진다

평가문제는 이런 형태로 나올 가능성이 높다.

> "총보수 0.3% 이하인 미국 주식형 ETF 중 AUM 상위 5개를 알려줘"

정렬·필터·집계는 임베딩 유사도로 풀 수 없다. 14.5만 건을 청킹해 벡터 DB에 넣으면 top-k 검색이 정답 행을 놓친다. 숫자 조건은 **결정적(deterministic) 연산**이어야 한다.

### 왜 Text-to-SQL 단독도 위험한가

전체 컬럼이 200개가 넘고, 상품군마다 명명 규칙이 다르고(`PD_NO` vs `pd_itm_no` vs `itm_no`), 한글명조차 없는 테이블이 둘이다. LLM에게 스키마 전체를 주고 SQL을 짜게 하면 **존재하지 않는 컬럼을 지어내는 환각**이 반드시 발생한다.

8/4 리서치로 이를 정량 확인했다.

- Text-to-SQL SOTA조차 BIRD 벤치마크에서 **71~77%** (CHASE-SQL 76.0%, XiYan-SQL 75.6%, AskData 77.1% / 인간 약 93%) — GPT-4o급 모델 + 다단계 앙상블 파이프라인 기준. HCX-007 단독 자유 SQL 생성은 이보다 낮다고 봐야 하며, 비공개 평가에서 오답 20~30%를 감수할 이유가 없다.
- 반대로 스키마에 **의미 계층(컬럼 사전·값 매핑·동의어)**을 얹으면: BIRD external knowledge 유무로 GPT-4가 34.9% → 54.9% (**+20%p**), Cube 벤치마크에서 프론티어 모델 3종 모두 **+17~23%p**. → S1의 컬럼 사전·값 사전이 곧 성능이다.
- 금융 DB 질의 연구(FinAI Data Assistant, 2025)의 결론: 자유 SQL 생성 대신 **사전 검증된 파라미터화 쿼리 라이브러리 + Function Calling 라우팅**이 결정성·감사가능성·지연·비용 모두 우위.
- 프로덕션 사례도 동일 — Uber QueryGPT는 의도 분류 → 도메인별 큐레이션 workspace로 좁힌 뒤에만 생성, LinkedIn SQL Bot은 벡터 검색 + LLM 리랭크로 테이블 후보를 7개까지 압축. 공통 교훈: **검증된 좁은 인터페이스로 제한하라.**

### 권장안: 정형 우선 하이브리드 (Tool-calling Agent)

```
[질의]
  │
  ├─ ① 의도·상품군 라우팅 (HCX-DASH-002, 저비용)
  │
  ├─ ② 조건 추출 → 정규화된 JSON 파라미터
  │      "미국 주식형" → {region:"US", assetType:"EQUITY"}
  │      "총보수 0.3% 이하" → {expenseRatioMax:0.3}
  │
  ├─ ③ 도구 실행 (HCX-007 Function Calling)
  │      ├─ search_products(필터+정렬+상위N)   ← SQL은 우리가 작성, LLM은 파라미터만
  │      ├─ get_product_detail(상품번호)
  │      ├─ compare_products(상품 리스트, 비교축)
  │      ├─ aggregate_products(집계함수, group by)
  │      └─ search_by_text(해외ETF 운용전략 시맨틱 검색)  ← 유일한 벡터 검색
  │
  ├─ ④ 근거 조립: 참조 테이블명 + 행 + 데이터기준일(2026-07-11)
  │
  └─ ⑤ 답변 생성 + 가드레일
         · 근거 없는 값 → "제공 데이터로 확인할 수 없음"
         · 조건 불충분 → 역질문
         · 전망·추천 표현 필터링
```

**핵심 아이디어:** LLM이 SQL 원문을 작성하게 하지 않는다. SQL은 우리가 미리 작성해두고, LLM은 **JSON 파라미터만 채운다.** 이렇게 하면 환각 SQL이 구조적으로 불가능해지고, `think_trace`에 도구 호출 내역이 그대로 남는다.

### 오케스트레이션 패턴 (8/4 확정)

ReAct vs Plan-and-Execute 비교 연구의 결론: ReAct 루프는 단순·적응적이지만 실패 시 같은 행동을 반복하며 갇히는 약점이 있고, P&E는 다단계 질의에서 경로가 안정적이다. 우리 도구는 결정적(SQL)이라 실패 모드가 적으므로:

- **기본**: HCX-007 Function Calling 기반 ReAct 루프 + **최대 반복 횟수 제한**(무한 루프 차단)
- **난이도 '상' 교차 질의만**("채권·ETF·펀드 각 1개 비교" 류): 경량 플래너로 분기 — 질의를 서브 질의로 분해 → 도구 병렬 실행 → 결과 병합
- LangGraph 상태 그래프 하나에서 두 패턴이 분기되고, 노드 전이 로그가 그대로 `think_trace`가 된다 (Agentic RAG Survey의 single-agent router + tool-use + guardrail 패턴에 해당 — 이 과제 규모에서 multi-agent는 지연·복잡도만 늘린다)

### 옵션: 제한된 Text-to-SQL fallback (8/6 예시 질의 확인 후 결정)

도구 5종으로 커버되지 않는 롱테일 질의가 예시 set에 보이면, 2단계 fallback으로 **제한된 Text-to-SQL**을 추가한다: read-only 뷰 + 컬럼 화이트리스트 + SQL 파서 검증 + 실행 오류 시 self-correction 1회 (CHASE-SQL의 "후보 생성 → 실행 검증 → 선택"의 미니 버전). **도구로 커버되면 붙이지 않는 게 정확도상 이득이다.**

### 기술 스택 (8/4 갱신 — 성능 최우선 기준)

| 레이어 | 선택 | 이유 |
|---|---|---|
| DB | **DuckDB** (read-only 모드, SQLite는 대안) | 컬럼너 저장으로 집계·정렬 분석 쿼리에 최적. in-process 파일 하나라 Docker 재현성은 SQLite급이고 read-only 다중 접근 가능. 엑셀/CSV 직접 로드로 적재도 짧다. 단, 145K건은 SQLite도 ms 단위 — 실질 선택 기준은 분석 SQL 편의성(QUALIFY·윈도우 함수)과 ETL 편의성 |
| 적재 | pandas/DuckDB → 정규화 스크립트 | 타입 캐스팅·NULL 정규화 포함. **멱등성** 확보(재실행해도 같은 결과) |
| 종목명 검색 | **별칭·정규화 사전 + LIKE/FTS** | 종목명 검색은 형태소 분석보다 별칭 정규화("타이거"=TIGER=미래에셋)가 결정적. 필요 시 FTS5 + `kiwipiepy` 병행 |
| 벡터 | CLOVA **Embedding v2** + **FAISS flat(정확 검색)** | 해외ETF `cu_strtegy` 5,646건뿐 — 근사 인덱스·벡터 DB 서버(Qdrant/Milvus)는 수백만 벡터부터 의미 있음. flat 검색이 밀리초에 끝나고 정확도 손실 0 |
| 리랭킹 | **CLOVA Reranker 2단계** | 벡터 top-20 → 리랭크 top-5. 검색 성능을 올리려면 DB 교체가 아니라 이걸 붙인다 |
| LLM | **HCX-007** (메인), HCX-DASH-002 (라우팅·조건추출) | 필수 제약. 규칙 우선 + LLM 보조로 **질의당 호출 2~3회 제한** |
| Agent | **LangGraph + langchain-naver(ChatClovaX)** | 공식 통합(`bind_tools` 지원, `max_tokens ≥ 1024` 동일 적용). 상태 그래프 전이가 그대로 `think_trace`, 가드레일 분기(확인불가/역질문)가 구조화됨. 노드 단위 모듈화로 3인 브랜치 부분 통합 가능 |
| API 서버 | FastAPI | `GET /answer` 하나. 동일 질의 캐싱·재시도 큐 포함 |
| 배포 | 네이버 클라우드 (Server/Container) | 대회 크레딧 제공. 상시 가동 필수 |

**GraphDB는 검토 후 배제.** 그래프가 이기는 조건은 "가변 홉의 관계 탐색"인데(Neo4j·Microsoft 비교 문서 공통), 이 데이터는 평면 마스터 4개 + 1홉 관계(상품→운용사/지수)뿐이고 질의는 필터·정렬·집계 중심 — SQL이 지배적인 워크로드다. GraphRAG는 비정형 코퍼스의 엔티티 관계망용. **기술제안서에 "GraphDB 검토 → 데이터 특성 근거로 배제" 한 단락을 쓰면 검토 깊이로 정성평가 가점 요소가 된다.**

### HyperCLOVA X 확인 사항

과제소개서상 **HyperCLOVA X 외 다른 LLM을 쓰면 평가 대상에서 제외**된다. 리서치로 확인한 제약과 기능은 다음과 같다.

| 항목 | 내용 |
|---|---|
| Function Calling 지원 모델 | **HCX-007, HCX-005, HCX-DASH-002** |
| Function Calling 미지원 | HCX-003, HCX-DASH-001 (튜토리얼에 자주 나오는 구버전 — 쓰면 안 됨) |
| HCX-007 | 128K 컨텍스트, **하이브리드 추론 모델** → 메인 에이전트로 적합 |
| HCX-005 | 128K, 멀티모달(이미지 해석) |
| HCX-DASH-002 | 32K, 경량·고속 → 라우팅/조건 추출용 |
| FC 엔드포인트 | `POST /v3/chat-completions/{modelName}`, `tool_choice`: auto / none / 특정 지정 |
| **FC 제약** | `maxTokens` 또는 `maxCompletionTokens`를 **1024 이상**으로 설정해야 함. 튜닝 모델은 FC 미지원 |
| Embedding v2 | `POST /v1/api-tools/embedding/v2`, 최대 8,192 토큰, **1,024차원** |
| 그 외 유용한 API | **Reranker**, RAG Reasoning, Segmentation, Router, Summarization — 직접 구현 대신 활용 검토 |
| OpenAI 호환 | 주요 API에 OpenAI SDK/API 호환 제공 → LangChain `ChatClovaX` 연동 가능 |

### OpenAI API 사용 가능 여부 판정 (8/4 확정)

과제소개서 원문은 조건 없는 단정형이다: *"LLM은 HyperCLOVA X만 사용 가능합니다. HyperCLOVA X 외 다른 LLM 모델을 사용할 경우, 평가대상에서 제외됩니다."*

| 구분 | 판정 | 이유 |
|---|---|---|
| OpenAI **SDK/라이브러리**를 CLOVA Studio 엔드포인트에 연결 | ✅ 가능 | CLOVA Studio가 OpenAI 호환 API를 공식 제공. 클라이언트만 OpenAI 형식, 모델은 HCX |
| OpenAI의 **모델**(GPT 계열) 호출 — 메인이든 라우팅·조건추출 등 보조 단계든 | ❌ 불가 | 라우팅용 경량 호출도 "LLM 사용". 제출물이 소스코드 전체라 `api.openai.com` 호출은 그대로 노출됨 |
| 회색지대 ①: 비-HCX **임베딩 모델**(text-embedding-3, BGE-m3 등) | ❓ 8/6 확인 | 규정 문언이 "LLM 모델"이라 임베딩 포함 여부 불명확. **Embedding v2로 통일해 논란 자체를 없애는 게 최선** |
| 회색지대 ②: 개발 보조(코딩 도구, 자체 평가셋 생성용 타 LLM) | ❓ 8/6 확인 | 제출 산출물의 런타임이 아니므로 통상 무관하나 명시 규정 없음 |

→ **런타임 파이프라인의 모든 신경망 호출은 CLOVA Studio(HCX-007 / HCX-DASH-002 / Embedding v2 / Reranker)로 통일한다.**

> **8/6 설명회 전까지 반드시 확인할 것:** 대회 제공 크레딧으로 HCX-007과 Embedding v2를 쓸 수 있는지, 호출량 제한(RPM/TPM)이 얼마인지. 평가 기간에 주최 측이 대량 호출을 보내므로 **레이트 리밋이 곧 실격 리스크**다.

---

## 3. 5주 스프린트 로드맵

각 스프린트는 일요일 종료 기준이다.

### Sprint 0 — 8/2(일) ~ 8/9(일) · 킥오프와 설명회

**D-35 ~ D-28**

가장 중요한 이벤트는 **8/6(목) 오프라인 설명회**다. 참고용 예시 질의 set이 여기서 공개된다고 명시돼 있다. 이게 없으면 5주 내내 과녁 없이 쏘게 된다.

- [ ] **8/6(목) 13:00~17:30 네이버 그린팩토리 설명회 — 팀당 최소 1명 필참** (주차 불가, 대중교통)
  - 확보할 것: **예시 질의 set**, 네이버클라우드 크레딧 사용법, 평가 세부 기준 Q&A
- [ ] 디스코드 채널 참가 (`https://discord.gg/qjCUvN4eWR`) — 주제별 Q&A가 여기서 진행됨
- [ ] 주최 측 GitHub Organization private repo 접근 확인
- [ ] 네이버 클라우드 계정 생성 + CLOVA Studio 신청 + 크레딧 확인
- [ ] HCX-007 Function Calling **"Hello World" 성공** — 툴 하나 정의해서 호출되는 것만 확인
- [x] 데이터 4종 엑셀 → pandas 로드, 각 테이블 결측률·고유값 프로파일링 (**8/4 완료** — `전처리/프로파일링/` 산출물 참조)

**완료 기준(DoD):** HCX-007이 우리가 정의한 더미 함수를 호출해 응답을 돌려줬다. ~~4종 데이터의 컬럼별 결측률 표가 있다.~~ ✅ 8/4 완료

---

### Sprint 1 — 8/10(월) ~ 8/16(일) · 데이터 레이어

**D-27 ~ D-21** · 이 스프린트가 프로젝트의 성패를 가른다.

| 작업 | 내용 |
|---|---|
| 적재 파이프라인 | 엑셀 4종 → **DuckDB**. 타입 캐스팅(문자열 숫자 → numeric, `double precision` 날짜 → date), 공백 문자열 → NULL 정규화. **재실행 가능한 단일 스크립트** |
| 컬럼 사전 | `column_dictionary.csv`: 테이블 / 영문컬럼 / 한글명 / 동의어 / 단위 / 결측여부. **채권·해외ETF는 직접 작성해야 함** |
| 값 사전 | 위험등급 코드 매핑(`PD_RISK_GCD_13` ↔ "다소높은위험(3등급)" ↔ 숫자 3), 통화코드, 시장코드, 판매여부 |
| **통합 축 테이블** | `product_axis`: `product_id`, `product_type`(BOND/ETF_KR/ETF_GL/FUND), `asset_class`, `region`, `risk_grade`(1~6 정규화), `expense_ratio`, `aum`, `return_1m/3m/6m/1y`, `is_sellable`. **`axis_*` 힌트를 참고해 규칙 기반으로 생성** |
| 펀드 중복 처리 | 클래스 대표 종목 선정 규칙 확정 (`rptt_ksd_itm_no` 활용 검토). 집계 시 중복 제거 |
| 인덱스 | 자주 필터링되는 컬럼(위험등급, 투자지역, 자산군, 판매여부, AUM, 보수) |

**DoD:** SQL 한 줄로 "국내ETF 중 위험등급 3 이하, AUM 500억 이상, 1년 수익률 상위 10개"가 나온다. 그리고 **같은 질문을 채권·펀드·해외ETF에 대해서도 `product_axis`로 던질 수 있다.**

> ⚠️ 이 주에 진도가 밀리면 전체가 밀린다. 3인이 각자 다른 상품군 1~2개씩 맡아 병렬로 처리한다.

---

### Sprint 2 — 8/17(월) ~ 8/23(일) · 에이전트 + API 첫 관통

**D-20 ~ D-14**

| 작업 | 내용 |
|---|---|
| 도구 5종 구현 | `search_products` / `get_product_detail` / `compare_products` / `aggregate_products` / `search_by_text`. 각각 **JSON Schema로 파라미터 정의** (Function Calling 스펙) |
| 조건 정규화 | "미국 주식" → `{region:"US", asset_class:"EQUITY"}`. LLM + 규칙 하이브리드 |
| 에이전트 루프 | HCX-007 + `tool_choice:"auto"`, `maxTokens ≥ 1024`. 도구 호출 → 결과 관찰 → 재호출 → 최종 답변 |
| **구조화 로깅** | 모든 도구 호출(이름/파라미터/결과 행수/소요시간)을 기록 → 그대로 `think_trace`가 됨 |
| API 서버 | `GET /answer?question_id=&question=` → 4필드 JSON. **타임아웃·에러 시에도 반드시 유효한 JSON 반환** |
| 벡터 인덱스 | 해외ETF `cu_strtegy` 5,646건 → Embedding v2 → FAISS flat + CLOVA Reranker 2단계 |

**DoD (8/23):** 배포된 URL에 `curl`을 날려서 실제 데이터 기반 답변이 JSON으로 돌아온다. **이 시점에 E2E가 안 뚫리면 일정을 재조정한다.**

---

### Sprint 3 — 8/24(월) ~ 8/30(일) · 교차 질의 · 가드레일 · 평가셋

**D-13 ~ D-7** · **8/30 기능 동결**

| 작업 | 내용 |
|---|---|
| 상품군 교차 질의 | "비슷한 위험도의 채권과 ETF 비교" 류. `product_axis` 기반 다중 테이블 조회 + 결과 병합 |
| 다단계 연산 | 정렬·순위·집계·비율 계산. LLM에게 산술을 시키지 않고 **SQL/Python이 계산하고 LLM은 서술만** |
| 가드레일 | ① 근거 없는 수치 차단 ② 데이터에 없는 항목("펀드 보수" 등) → "확인할 수 없음" ③ 조건 불충분 → 역질문 ④ 전망·단정적 추천 표현 필터 |
| 근거 표기 | 답변 하단에 참조 테이블명 + 종목번호 + **데이터 기준일 2026-07-11** 자동 부착 |
| **자체 평가셋 100문항** | 난이도 상/중/하 혼합 — 평가 방식 미러링 (아래 4장) |
| 회귀 테스트 | 평가셋 자동 실행 → 정답률 리포트. 프롬프트 수정할 때마다 돌린다 |

**DoD:** 자체 평가셋 정답률 측정치가 있고, "확인할 수 없음"과 역질문이 의도대로 동작한다. **8/30 이후 기능 추가 금지.**

---

### Sprint 4 — 8/31(월) ~ 9/6(일) · 튜닝 · 문서 · 제출

**D-6 ~ D-0**

| 작업 | 내용 |
|---|---|
| 프롬프트 튜닝만 | 코드 구조 변경 금지. 평가셋 정답률 기준으로 프롬프트·파라미터만 조정 |
| 안정성 | 부하 테스트(연속 100회 호출), 타임아웃 대응, 재시작 시 자동 복구, 로그 로테이션 |
| **기술제안서** | 제안 요약 / 문제 정의 / 제안 방법 / 시스템 구성도 / 주요 기능 흐름도 / 사용자 시나리오 / 기대효과·확장성 |
| README | 환경 구성, 실행 명령어, Dockerfile, requirements.txt — **재현 가능해야 함** |
| API 명세서 | 요청/응답 JSON 스키마 (필수 명시 항목) |
| 최종 제출 | private repo push + endpoint URL |

**제출 3종 체크리스트**

- [ ] 소스코드 + Dockerfile + requirements.txt + README.md
- [ ] 기술 제안서 (자유 양식)
- [ ] 평가용 API End-point URL + API 명세서

> ⚠️ **9/6 23:59 이후 커밋·push·배포 금지.** 9/7~9/30 평가 기간 내내 서버가 살아있어야 한다.

---

## 4. 자체 평가셋 설계

과제소개서에 평가문제가 **난이도 상/중/하 혼합**이라고 명시돼 있다. 그대로 미러링해서 100문항을 만든다. 이게 유일한 객관적 성능 지표다.

**하 (30문항) — 단일 상품 조회**

- "KODEX 200의 총보수는?"
- "종목번호 KR5014410050 펀드의 운용속성은?"

**중 (50문항) — 조건 필터 + 정렬/집계**

- "위험등급 3 이하 국내ETF 중 1년 수익률 상위 5개"
- "잔존만기 3년 이내, 신용등급 AA 이상 회사채 중 세후수익률 높은 순"
- "미국 투자 해외ETF 중 총보수 0.1% 미만이면서 AUM 10억 달러 이상인 것"

**상 (20문항) — 교차 질의 + 다단계 + 예외 처리**

- "위험등급 2등급대에서 채권·ETF·펀드를 각각 하나씩 추천하고 근거를 비교해줘" → **3개 테이블 교차**
- "TIGER 미국S&P500과 유사한 해외ETF를 찾아서 보수와 AUM을 비교해줘" → **시맨틱 + 정량**
- "보수가 가장 낮은 공모펀드는?" → **정답: "제공 데이터에 펀드 보수 정보가 없어 확인할 수 없습니다"** (환각 함정)
- "수익률 좋은 상품 추천해줘" → **정답: 역질문** (투자 기간·위험 성향·상품군 확인)

마지막 두 유형이 정성평가의 '리스크 관리' 항목과 직결된다. 반드시 넣는다.

---

## 5. 3인 역할 분담

세 명 모두 개발과 기획을 겸하므로, **트랙은 나누되 문서는 각자 자기 트랙 부분을 매주 누적 작성**한다. 기술제안서를 마지막 주에 몰아 쓰면 반드시 부실해진다.

| | **A — 데이터** | **B — 검색·에이전트** | **C — 서빙·평가** |
|---|---|---|---|
| S0 | 4종 데이터 프로파일링 | HCX-007 FC Hello World | 클라우드 계정·크레딧·repo |
| S1 | **적재 파이프라인 + 통합 축 테이블** | 컬럼/값 사전 (채권·해외ETF) | 인덱스·쿼리 성능, Docker 골격 |
| S2 | 도구용 SQL 작성 | **에이전트 루프 + FC 연동** | **FastAPI + 배포 + 로깅** |
| S3 | 교차 질의 데이터 로직 | 가드레일·프롬프트 | **평가셋 100문항 + 회귀 테스트** |
| S4 | 제안서: 데이터 구조화 챕터 | 제안서: 아키텍처·흐름도 | **제안서 총괄 + README + API 명세** |

**주간 리듬 (권장)**

- 월: 30분 스프린트 계획
- 수: 15분 중간 점검 (막힌 것만)
- 일: 1시간 데모 + 회고 + 문서 30분

### 브랜치 통합 전략 — 공통 계약 4종을 먼저 고정 (8/4 추가)

우리 팀은 각자 브랜치에서 우선 개발한 뒤 최적 사항만 통합하는 방식이다. 이게 작동하려면 **비교 가능한 공통 계약을 Sprint 0에서 고정**해야 한다.

1. **도구 5종의 JSON Schema** (Function Calling 파라미터 정의)
2. **DB 스키마** (`product_axis` 포함 — 테이블·컬럼·타입)
3. **응답 4필드 JSON** (question_id / retrieved_context / think_trace / answer의 내부 형식)
4. **자체 평가셋 + 공용 회귀 러너** (브랜치 무관하게 같은 명령으로 정답률 산출)

이 4개가 같으면 **브랜치별 정답률이 객관적 통합 기준**이 되고, LangGraph 노드 단위 모듈화 덕분에 "A의 라우터 + B의 도구 + C의 가드레일" 식 부분 통합도 가능하다. 계약 없이 각자 만들면 통합 시점에 비교 자체가 불가능해진다.

---

## 6. 리스크와 대응

| 리스크 | 영향 | 대응 |
|---|---|---|
| **8/6 설명회 불참** | 예시 질의 set을 못 얻어 5주간 과녁 없이 개발 | 최소 1명 필참. 불가 시 디스코드에서 즉시 확보 |
| 채권 컬럼 의미 해석 실패 | 42,394건 테이블을 못 씀 | S1에 시간 배분. 디스코드로 주최 측에 직접 질의 |
| 공모펀드 클래스 중복 | 순위·집계 답변이 전부 틀림 | S1에서 대표 종목 규칙 확정. 평가셋에 반드시 포함 |
| HCX API 레이트 리밋 | **평가 기간 중 호출 실패 = 0점** | S0에서 한도 확인. 캐싱·재시도·큐잉 구현. 설명회에서 질문 |
| S2 종료 시 E2E 미완성 | 일정 붕괴 | 8/23을 하드 게이트로. 미달 시 기능 축소(벡터 검색 후순위) |
| 9/6 이후 서버 다운 | 평가 불가 = 실격 수준 | 8/30 동결 후 1주 안정성 검증. 헬스체크·자동 재시작·모니터링 |
| 기술제안서 부실 | 정성평가 5개 축 중 '문제정의·창의성' 손실 | 매주 30분씩 누적 작성 |
| 외부 데이터와 충돌 | 규칙 위반 | **주최 측 제공 데이터 우선** 원칙 코드로 강제 |

---

## 7. 이번 주 즉시 할 일

**8/2(일) ~ 8/6(목)** — 8/4 갱신

- [x] 데이터 4종 결측률·고유값 프로파일링 (**8/4 완료** — [전처리/프로파일링/프로파일링_요약.md](전처리/프로파일링/프로파일링_요약.md))
- [x] **Tier 1 해석 메타데이터 수집 — 컬럼 사전 207컬럼 전체 + 값 사전 + 별칭·등급·코드·지수·펀드클래스 사전 9종** (**8/4 완료** — [외부데이터/수집_요약.md](외부데이터/수집_요약.md), 웹 리서치 428건 + 적대적 검증 정정 13건 반영)
- [x] **전처리 1차 파이프라인 — 함정 제거된 스테이징 CSV 4종 + 비정상행 격리 + 규칙 리포트 56건** (**8/4 완료** — [전처리/데이터_전처리_방법.md](전처리/데이터_전처리_방법.md), `전처리/preprocess.py`, S1 적재 파이프라인의 선행 완료분)
- [ ] 팀 3인 역할 확정 (A/B/C 트랙)
- [ ] **공통 계약 4종 초안 고정** (도구 JSON Schema · DB 스키마 · 응답 JSON 내부 형식 · 평가셋 러너) — 브랜치 개발 시작 전 필수 (5장)
- [ ] **8/6 설명회 참석자 지정 + 질문 리스트 작성**
  - HCX-007 크레딧 범위와 RPM/TPM 한도
  - 평가 시 호출 빈도와 타임아웃 기준
  - 채권 컬럼(`NDY_*`, `COV`, `DIRTY`) 정의 문서 제공 가능 여부
  - `axis_*` 컬럼의 의도 확인
  - **(8/4 추가) 비-HCX 임베딩 모델 사용 허용 범위** — 규정 문언은 "LLM 모델"
  - **(8/4 추가) 외부 수집 데이터를 출처 명시 '부가 정보'로 답변에 포함하는 것의 인정 여부** (Tier 2 전략 확정용)
  - **(8/4 추가) `retrieved_context`·`think_trace` 형식 자유도** — 구조화 JSON 허용 여부
  - **(8/4 추가) 데이터 정의 질문 14건** — `NDY_*` 정의, `CRD_GRD` 대표값 규칙, 금액 단위, `prfd_attr_cd` 코드표 등 ([외부데이터/수집_요약.md](외부데이터/수집_요약.md) 5장 우선순위 리스트)
- [ ] 디스코드 채널 참가
- [ ] 네이버 클라우드 계정 + CLOVA Studio 신청 (승인에 시간이 걸릴 수 있음 → **오늘 신청**)
- [ ] GitHub private repo 접근 확인
- [ ] 데이터 4종 엑셀 열어서 각자 담당 상품군 훑어보기 (프로파일링 요약을 먼저 읽고 시작)

---

## 8. 일정 요약

| 날짜 | 이벤트 |
|---|---|
| 8/6 (목) | **오프라인 설명회** — 예시 질의 set 공개, 팀당 1명 필참 |
| 8/9 (일) | Sprint 0 종료 — FC Hello World |
| 8/16 (일) | Sprint 1 종료 — DB + 통합 축 완성 |
| **8/23 (일)** | Sprint 2 종료 — **E2E 관통 (하드 게이트)** |
| **8/30 (일)** | Sprint 3 종료 — **기능 동결** |
| **9/6 (일) 23:59** | **예선 제출 마감** (이후 변경 시 실격) |
| 9/7 ~ 9/30 | 예선 평가 (주최 측이 우리 API 호출 — 서버 상시 가동) |
| 10/1 (목) | 결과 발표 (6팀) |
| 10/1 ~ 10/16 | 본선 및 멘토링 (오프라인, 전원 대면 원칙) |
| 10월 중 | 결선 및 최종 발표 (PT + 라이브 시연) |

---

### 참고 출처

- 금융상품Agent_과제소개.pdf (제10회 2026 미래에셋증권 AI Festival)
- 제공 데이터 스키마 4종 (PRBD01N001 / PREF01N001 / PREF02N001 / PRFD01N001) 직접 분석
- [CLOVA Studio 모델 — NAVER Cloud](https://guide.ncloud-docs.com/docs/clovastudio-model)
- [Function Calling — CLOVA Studio API 가이드](https://api.ncloud-docs.com/docs/clovastudio-chatcompletionsv3-fc)
- [CLOVA Studio 개요 — API 가이드](https://api.ncloud-docs.com/docs/ai-naver-clovastudio-summary)
- [Embedding v2 — CLOVA Studio API 가이드](https://api.ncloud-docs.com/docs/clovastudio-embeddingv2)

**8/4 리서치 출처 (아키텍처·기술스택 근거)**

- [FinAI Data Assistant — 금융 DB 질의: Function Calling 기반 파라미터화 쿼리 (arXiv 2510.14162)](https://arxiv.org/pdf/2510.14162)
- [XiYan-SQL: Multi-Generator Ensemble for Text-to-SQL (arXiv 2411.08599)](https://arxiv.org/pdf/2411.08599) — BIRD SOTA 수준 비교 근거
- [Cube — Why semantic layers make LLM analytics reliable (paired benchmark, +17~23%p)](https://cube.dev/blog/why-semantic-layers-make-llm-analytics-reliable-a-paired-benchmark-across-three-frontier-models)
- [Semantic Layers for Reliable LLM-Powered Data Analytics (arXiv 2604.25149)](https://arxiv.org/abs/2604.25149)
- [TableRAG: Million-Token Table Understanding (NeurIPS 2024)](https://proceedings.neurips.cc/paper_files/paper/2024/file/88dd7aa6979e352fda7c4952ca8eac59-Paper-Conference.pdf)
- [Agentic RAG: A Survey (arXiv 2501.09136)](https://arxiv.org/abs/2501.09136)
- [Uber QueryGPT — Natural Language to SQL](https://www.uber.com/blog/query-gpt/) · [LinkedIn·Uber Text-to-SQL 교훈 정리](https://erincon01.medium.com/building-modern-text-to-sql-systems-with-genai-lessons-from-linkedin-uber-and-open-source-69c829fa16cf)
- [Architecting Resilient LLM Agents: Plan-then-Execute (arXiv 2509.08646)](https://arxiv.org/pdf/2509.08646)
- [Graph vs Relational — Microsoft Fabric 문서](https://learn.microsoft.com/en-us/fabric/graph/graph-relational-databases)
- [DuckDB vs SQLite — PostHog 심층 비교](https://posthog.com/blog/duckdb-vs-sqlite)
- [ChatClovaX — LangChain 공식 통합](https://python.langchain.com/docs/integrations/chat/naver/) · [CLOVA Studio OpenAI 호환 API](https://api.ncloud-docs.com/docs/en/clovastudio-openaicompatibility)
