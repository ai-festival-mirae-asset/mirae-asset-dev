# 데이터 전처리 방법

> 작성일 2026-08-04 · 스크립트 [`scripts/preprocess.py`](scripts/preprocess.py) · 산출물 [`data/processed/`](data/processed)
> 원본: [`datasets/`](datasets) 4종 (2026-07-11 스냅샷, 총 145,393행) — 원본은 수정하지 않는다

---

## 1. 한눈에 보기 (핵심)

**무엇을 했나:** 원본 xlsx 4종을 읽어 ① 문자열·타입 오염을 청소하고 ② 무의미한 센티널 값을 NULL로 통일하고 ③ 해석이 필요한 값(신용등급, 위험등급 등)을 `drv_*` 파생 컬럼으로 표준화한 CSV 4종을 만들었다. 비정상 행 1건은 격리했다.

**왜 했나:** 이 데이터는 그냥 적재하면 **틀린 답을 만드는 함정**이 6종류 있다 — 숫자로 저장된 날짜, 공백 40칸짜리 "값", 결측이 아닌 척하는 센티널 문자열, 컬럼이 밀린 행, NULL의 의미가 'N'인 플래그 컬럼, 표기가 오염된 등급명. 전처리의 목적은 값을 예쁘게 만드는 게 아니라 **"있는 값은 정확히 계산되고, 없는 값은 정확히 없다고 판정되게"** 만드는 것이다. (평가 항목에 '확인할 수 없음' 명시가 있으므로 결측 판별력이 곧 점수다.)

**3대 원칙**

1. **원본 보존** — 원본 컬럼은 지우거나 덮어쓰지 않고(전량 무정보 컬럼 제외), 해석값은 `drv_*` 접두어 파생 컬럼으로 추가한다. 원본 대조가 항상 가능하다.
2. **결측은 메꾸지 않고 판별한다** — 센티널→NULL 변환은 하되, NULL을 다른 값으로 채우지 않는다. 0 대체·평균 대체 금지.
3. **모든 규칙은 기록된다** — 규칙ID·대상 컬럼·영향 행수가 [`data/processed/전처리_리포트.csv`](data/processed/전처리_리포트.csv)에 남는다(56건). "왜 이 값이 바뀌었지?"는 리포트에서 역추적한다.

**산출물**

| 파일 | 행 × 컬럼 | 변화 |
|---|---|---|
| `PRBD01N001_국내채권_전처리.csv` | 42,394 × 49 | 원본 40컬럼 + 파생 9 |
| `PREF01N001_국내ETF_전처리.csv` | 1,734 × 69 | 원본 73 − 전량결측 6 + 파생 2 |
| `PREF02N001_해외ETF_전처리.csv` | 5,646 × 49 | 원본 49 − 무정보 2 + 파생 2 |
| `PRFD01N001_공모펀드_전처리.csv` | 95,618 × 46 | 원본 45 + 파생 1, **1행 격리** |
| `quarantine_PRFD01N001_비정상행.csv` | 1 | 컬럼 밀림 비정상 레코드 |
| `전처리_리포트.csv` | 56규칙 | 규칙별 영향 행수 전체 기록 |

**재실행:**

```bash
python scripts/preprocess.py
```

같은 입력이면 항상 같은 출력이 나온다(멱등). 행수·키 유일성 assertion이 내장되어 있어 원본이 바뀌면 실패한다.

---

## 2. 전처리 규칙 요약표

| 규칙ID | 대상 | 내용 | 한 줄 이유 |
|---|---|---|---|
| R1 | 전 테이블 | 문자열 앞뒤 공백 제거 | 고정폭 패딩 탓에 trim 없이는 조인·필터 누락 |
| R2 | 전 테이블 | 공백-only·빈 문자열 → NULL | `'          '`은 값이 아니라 결측 (`IS NULL` 작동) |
| R3 | 전 테이블 | 문자열 `'NULL'` → NULL | 공모펀드 위험등급에 문자열 'NULL' 18,416건 |
| R4 | 전 테이블 | 스키마 선언 타입 기준 숫자 캐스팅 | `'13675.00'` 문자열로는 정렬·집계 불가 |
| R5 | 채권 | double 날짜 4종 → ISO 날짜 | `20260330.0`으로는 잔존만기 연산 불가 |
| R6 | 채권 | `CURR_CD='000'` → NULL | ISO 4217에 없는 센티널 |
| R7 | 채권 | `drv_is_perpetual` (만기 9999-12-31) | 영구채 센티널을 만기 연산에 넣으면 폭주 |
| R8 | 채권 | `drv_is_matured`, `drv_is_buyable` | 만기도래 16,496건·매수가능 325건 기본 필터 |
| R9 | 채권 | `drv_crd_grd_norm`, `drv_crd_grd_rank` | `AA0`=AA(플랫) 정규화 + 서열비교(AAA=1~D=20) |
| R10 | 채권 | `drv_evco_grd_cnt/worst/worst_rank` | 콤마 병기 복수등급 분해, 스플릿 시 보수적(최저) 채택 |
| R11 | 채권 | 위험등급 0 → NULL, `drv_risk_grade` | 0은 미분류 58건 (1=고위험~6=저위험 확인됨) |
| R12 | 국내ETF | 전량 결측 6컬럼 제거 | 100% 무정보 — 질의 불가 컬럼 (컬럼사전에 '미제공' 기록) |
| R13 | 국내ETF | 무정보 의심 3컬럼 유지+경고 | 비결측값 전부 0 — 실제 0인지 미수집인지 미확인이라 제거 안 함 |
| R14 | 국내ETF | `drv_curr_cd`, `drv_risk_grade` | `CURR_CD_KRW`→KRW, `PD_RISK_GCD_11`→1 |
| R15/R19 | ETF | timestamp → ISO 날짜 | 기준일 표기 통일 |
| R16 | 해외ETF | Lipper 센티널 2종 → NULL | "Index is not provided..." 2,705건 = **실질결측 48%** |
| R17 | 해외ETF | `cu_lev_fector`(100% 결측)·`du_er_1d`(전부 0) 제거 | 무정보 확정 |
| R18 | 해외ETF | `drv_is_etn`, `drv_is_inverse` | Y만 있는 플래그 컬럼 — NULL=N 해석을 명시적 값으로 |
| R20 | 공모펀드 | 도메인 위반 행 격리 (1건) | 컬럼 밀림 행(`itm_no='"'`) — 조용히 적재하면 오염 확산 |
| R21 | 공모펀드 | `kofia_fd_ccd` 전체 0 → NULL | 20자리 '000...0'은 미부여 센티널 |
| R22 | 공모펀드 | `or_attr_desc='06'` **유지** | 파생형 코드 후보 — 매핑 확정 전 NULL 변환 금지 (PROJECT_GUIDE) |
| R23 | 공모펀드 | `drv_risk_grade` | 등급명 표기 오염('높은위험' vs '높은 위험') 대신 코드 사용 |

---

## 3. 왜 이렇게 했는가 — 함정별 상세

### 3-1. 숫자로 저장된 날짜 (채권 R5)

`ISU_DT`·`MAT_DT`·`CRD_GRD_DT`·`PD_STD_INFO_UPDATE`가 `double precision`이다(`20260330.0`). 이대로 적재하면 "잔존만기 3년 이내" 같은 날짜 연산이 전부 불가능하다. ISO 문자열(`2026-03-30`)로 변환했고, 값 0·8자리 미달은 파싱 불가로 NULL 처리 후 리포트에 남겼다. `MAT_DT=99991231`은 날짜가 아니라 **영구채 표기**이므로 날짜는 유지하되 `drv_is_perpetual='Y'`로 분리 표시했다 — 이 플래그 없이 잔존만기를 계산하면 영구채가 "잔존 7,973년"으로 랭킹을 오염시킨다.

### 3-2. 결측이 아닌 척하는 값들 (R2·R3·R6·R16·R21)

이 데이터의 결측은 세 가지 얼굴로 온다: ① 진짜 NULL, ② 공백 40칸 문자열(국내ETF `cu_base_index` 1,551건), ③ 의미 없는 센티널 값(`'000'`, `'NULL'` 문자열 18,416건, 20자리 0, **해외ETF 기초지수의 "Index is not provided by Management Company" 류 2,705건 = 48%**). ②③을 NULL로 통일하지 않으면 `WHERE cu_base_index IS NULL`이 통하지 않아 **"확인할 수 없음"이라고 답해야 할 질의에 쓰레기 값으로 답하게 된다.** 특히 해외ETF 기초지수는 "결측 0%"로 보이지만 실질결측 48%였다 — 센티널 인식이 없으면 커버리지 판정 자체가 틀린다.

### 3-3. 컬럼 밀림 행 (공모펀드 R20)

Excel 84,563행은 컬럼이 한 칸씩 밀려 `itm_no='"'`, `exchdg_yn='00080008'`, `zrin_fd_ivst_risk_gcd='00020054'`가 된 비정상 레코드다. 값 프로파일링에서 발견된 이상값 3종이 전부 이 **한 행**에서 나온 것을 격리 후 확인했다. 도메인 규칙(itm_no 12자리 형식, exchdg_yn∈{Y,N}, 위험등급∈{1..6})을 위반하는 행을 자동 탐지해 `quarantine_*.csv`로 분리한다 — 특정 행 번호 하드코딩이 아니라 규칙 기반이므로 원본이 갱신돼도 동작한다.

### 3-4. NULL의 의미가 'N'인 플래그 컬럼 (해외ETF R18)

`cu_etn_yn`·`cu_inverse_short_yn`은 값이 있으면 전부 'Y'다(각 59건·171건). 이런 컬럼의 NULL은 결측이 아니라 **'N'**이다. 그런데 이 해석을 쿼리 작성자마다 따로 하게 두면 실수가 난다. 원본은 그대로 두고 `drv_is_etn`·`drv_is_inverse`에 Y/N을 명시적으로 채웠다. (반례: 해외ETF `wu_core_yn`은 관측값이 N뿐이라 같은 규칙을 적용할 수 없어 제외 — 컬럼마다 관측 분포를 보고 판단해야 한다.)

### 3-5. 신용등급·위험등급의 표준화 (R9·R10·R11·R14·R23)

- `CRD_GRD`의 `AA0`은 AA와 다른 등급이 아니라 **무부호(플랫) 표기**다(관측 분포와 신평 3사 공식 기호체계로 확인). 정규화 없이 "AA 이상" 필터를 걸면 AA0 1,713건이 누락된다. `drv_crd_grd_rank`(AAA=1~D=20)로 서열 비교를 SQL로 만들었다 — "AA 이상"=rank≤3.
- `PD_EVCO_CRD_GRD`("AAA, AAA, AA+")는 복수 평가사 등급 병기다. 개수·최저등급을 분해했고, 스플릿(평가사 불일치) 시 보수적으로 최저등급을 쓰는 실무 관행을 따랐다.
- 위험등급은 상품군마다 표기가 다르다(`PD_RISK_GCD_11` vs `1` vs 코드 0 포함). 전 상품군 공통 `drv_risk_grade`(1=매우 높은 위험 ~ 6=매우 낮은 위험)로 통일했다. 채권 0(58건)은 미분류이므로 NULL. **주의: "위험등급이 낮다" = 숫자가 크다(5~6등급).** 등급명 문자열은 표기 오염('높은위험'/'높은 위험')이 있어 코드 기준 파생만 신뢰한다.

### 3-6. 전량 무정보 컬럼 제거 (R12·R17)

국내ETF 6컬럼(100% 결측), 해외ETF `cu_lev_fector`(100% 결측)·`du_er_1d`(비결측 전부 0)는 어떤 질의에도 답을 줄 수 없다. 남겨두면 "이 컬럼으로 답할 수 있다"는 착각(특히 Text-to-SQL/도구가 컬럼을 선택할 때)을 유발하므로 제거하되, 스크립트가 **런타임에 전량 결측을 재확인한 후에만** 제거한다. 제거 이력은 리포트와 컬럼 사전('미제공' 표기)에 남는다.

---

## 4. 의도적으로 하지 않은 것

| 항목 | 이유 |
|---|---|
| `or_attr_desc='06'` (5,436행) NULL 변환 | 결측이 아니라 **미변환 코드**일 가능성(파생형 후보). 매핑 확정 전 정보 파괴 금지 |
| `du_chas_errt`·`du_diff_rt`·`pd_dvid_yield` 제거 | 비결측값이 전부 0 — 실제 0인지 미수집 대체값인지 미확인. 유지하되 순위·비교 사용 금지로 리포트 표기 |
| 공모펀드 master/attribute 분리 | DB 적재 단계(fund_master 11,139 / bridge 95,618)의 작업. CSV 단계에서는 행 구조 보존 |
| `du_lpr` 시가/저가 재해석 | 스키마 한글명 '시가'가 low price(저가)의 오기로 의심되나 미확정 — 8/6 확인 전 보류 |
| 결측 값 외부 데이터로 채우기 | 채점 기준은 제공 데이터. 외부 값 주입은 정확도를 올리는 게 아니라 평가 리스크를 만든다 ([외부데이터_수집_방법.md](외부데이터_수집_방법.md) 참조) |

---

## 5. 다음 단계 (DB 적재와의 연결)

이 CSV는 **DB 적재 전의 표준화 스테이징**이다. 적재 단계에서 할 일:

1. 공모펀드 `fund_master`(itm_no 11,139) / `fund_attribute`(bridge) 분리 — 클래스 중복 계상 방지
2. 지역·자산군 taxonomy 매핑 테이블 적용 (상품군 간 표기 불일치: 미국 vs United States of America vs 남미/북미)
3. `drv_*` 컬럼 기반 인덱스 생성 (위험등급, 신용등급 rank, 만기·매수가능 플래그)
4. 적재 assertion: 행수(42,394 / 1,734 / 5,646 / 95,618+1) · 키 유일성 · quarantine 이력

전처리 규칙의 근거가 된 용어·코드 해석은 [`해석_메타데이터/`](해석_메타데이터)의 사전 9종을 참조하라.
